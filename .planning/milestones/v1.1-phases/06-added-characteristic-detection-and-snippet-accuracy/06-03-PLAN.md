---
phase: 06-added-characteristic-detection-and-snippet-accuracy
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - delta_preservation/evaluation/conformance.py
  - tests/test_added_truth_selection.py
  - tests/test_debug_row_identity.py
autonomous: true
requirements: [ADD-01]
test_ids:
  - tests/test_added_truth_selection.py::TestDuplicateAddedTruthSelection
  - tests/test_debug_row_identity.py::test_duplicate_added_truth_rows_claim_distinct_indexes_from_revb_evidence
must_haves:
  truths:
    - "Duplicate canonical added rows remain conservative by default but become claimable when packet Rev B evidence identifies exactly one truth row"
    - "Unique exact-text matching still works unchanged for non-duplicate added rows"
    - "Ambiguous or evidence-free duplicate matches still return truth_ambiguity instead of guessing"
  artifacts:
    - path: "delta_preservation/evaluation/conformance.py"
      provides: "Deterministic duplicate added-truth tie-break using packet Rev B bbox evidence"
      contains: "ADDED_TRUTH_TIEBREAK_MAX_DISTANCE_PT"
    - path: "tests/test_added_truth_selection.py"
      provides: "Focused evaluator coverage for unique, duplicate, and ambiguous added-row selection"
      contains: "class TestDuplicateAddedTruthSelection"
    - path: "tests/test_debug_row_identity.py"
      provides: "Queue-facing regression proving duplicate added rows can claim distinct truth indexes"
      contains: "duplicate_added_truth_rows_claim_distinct_indexes"
---

<objective>
Make duplicate canonical added rows claimable without part-specific hacks. The current evaluator stops at normalized requirement text and turns every duplicate Part 9 case into `truth_ambiguity`. This plan adds a deterministic second-stage tie-break based on packet Rev B evidence while preserving the current conservative behavior when the packet still does not identify one row uniquely.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-CONTEXT.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-RESEARCH.md
@delta_preservation/evaluation/conformance.py
@delta_preservation/evaluation/contracts.py
@delta_preservation/evaluation/snippet_rules.py
@delta_preservation/types.py
@tests/test_debug_row_identity.py
@assets/part9/ground_truth.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add evaluator tests for duplicate added-truth claiming</name>
  <files>tests/test_added_truth_selection.py, tests/test_debug_row_identity.py</files>
  <read_first>delta_preservation/evaluation/conformance.py, delta_preservation/types.py, tests/test_debug_row_identity.py, assets/part9/ground_truth.json, .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-RESEARCH.md</read_first>
  <action>
Create `tests/test_added_truth_selection.py` and extend `tests/test_debug_row_identity.py`.

`TestDuplicateAddedTruthSelection` must cover:
1. a unique exact-text added requirement still selects the only matching truth row
2. duplicate exact-text matches are disambiguated when the packet `revB.bbox` contains exactly one canonical `snippet_center_revB`
3. when no truth center lies inside the packet bbox, a unique nearest-center fallback inside a bounded tolerance selects the row
4. when two truth rows still qualify, the result remains `truth_ambiguity`
5. when `revB` evidence is missing or invalid, duplicate exact-text matches remain ambiguous

In `tests/test_debug_row_identity.py`, add a queue-facing regression that builds two packet added rows for the duplicated Part 9 trio (`Ø.250 ±.008`, `⌖ ∅.015 D H`, or `↧.50 ±.05`) with distinct `revB.bbox` regions and asserts they claim distinct added-pool truth indexes instead of collapsing into ambiguity.

Use checked-in Part 9 ground-truth centers as the exact coordinate source. Do not hardcode truth indexes without also validating the corresponding requirement text and snippet center.
</action>
  <acceptance_criteria>
    - `tests/test_added_truth_selection.py` exists and contains `class TestDuplicateAddedTruthSelection`.
    - `rg -n "truth_ambiguity|snippet_center_revB|Ø\\.250 ±\\.008|⌖ ∅\\.015 D H|↧\\.50 ±\\.05" tests/test_added_truth_selection.py tests/test_debug_row_identity.py` returns duplicate-claim coverage.
    - `uv run pytest tests/test_added_truth_selection.py tests/test_debug_row_identity.py -x` fails before the production edits and exits 0 after them.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add deterministic Rev B evidence tie-break logic to select_truth_row_for_item</name>
  <files>delta_preservation/evaluation/conformance.py</files>
  <read_first>delta_preservation/evaluation/conformance.py, delta_preservation/evaluation/snippet_rules.py, delta_preservation/types.py, .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-CONTEXT.md</read_first>
  <action>
Extend `select_truth_row_for_item()` in `delta_preservation/evaluation/conformance.py` so duplicate exact-text added matches can be resolved deterministically.

Implementation contract:
- Keep the current non-added and unique exact-text logic unchanged
- When `exact_requirement_matches` contains more than one row, inspect packet-side `item.revB.bbox`
- Add local helpers in `conformance.py` for:
  - coercing packet bbox to a validated tuple
  - computing the packet bbox center
  - checking whether a truth center falls inside the packet bbox
  - computing distance from packet bbox center to truth `snippet_center_revB`
- Introduce `ADDED_TRUTH_TIEBREAK_MAX_DISTANCE_PT` as an explicit constant

Tie-break order:
1. if exactly one duplicate truth center falls inside the packet bbox, select it
2. else if exactly one duplicate truth row is the unique nearest center within `ADDED_TRUTH_TIEBREAK_MAX_DISTANCE_PT`, select it
3. else preserve the current conservative ambiguity result

Do not import private helpers from `snippet_rules.py`; keep the selection logic self-contained or factor a new shared geometry utility only if truly necessary for cleanliness.
</action>
  <acceptance_criteria>
    - `rg -n "ADDED_TRUTH_TIEBREAK_MAX_DISTANCE_PT|snippet_center_revB|inside the packet bbox|nearest center" delta_preservation/evaluation/conformance.py` returns the new tie-break logic.
    - `rg -n "multiple canonical added truth rows share the same normalized requirement text" delta_preservation/evaluation/conformance.py` still returns the conservative ambiguity path.
    - `uv run pytest tests/test_added_truth_selection.py tests/test_debug_row_identity.py -x` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Packet added row -> evaluator selector | Persisted packet evidence now influences which canonical added truth row gets reserved. |
| Canonical duplicate added rows -> tie-break | Multiple truth rows share the same normalized requirement text and must not be guessed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | I | `delta_preservation/evaluation/conformance.py` | mitigate | Only select a duplicate truth row when bbox containment or nearest-center evidence identifies exactly one candidate. |
| T-06-08 | T | `delta_preservation/evaluation/conformance.py` | mitigate | Keep the original ambiguity result whenever packet evidence is missing, invalid, or still non-unique. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/test_added_truth_selection.py tests/test_debug_row_identity.py -x`
- `rg -n "ADDED_TRUTH_TIEBREAK_MAX_DISTANCE_PT|snippet_center_revB|truth_ambiguity" delta_preservation/evaluation/conformance.py tests/test_added_truth_selection.py`
</verification>

<success_criteria>
- Duplicate Part 9-style added rows can claim distinct canonical truth rows when packet Rev B evidence identifies them.
- Non-duplicate added rows remain on the current exact-text fast path.
- Ambiguity remains conservative whenever the packet does not identify one truth row clearly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-03-SUMMARY.md`
</output>
