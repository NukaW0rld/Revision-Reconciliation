---
phase: 06-added-characteristic-detection-and-snippet-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - delta_preservation/reconcile/exclusion.py
  - delta_preservation/reconcile/anchors.py
  - delta_preservation/reconcile/match.py
  - delta_preservation/reconcile/classify.py
  - tests/test_phase6_exclusion.py
  - tests/test_alignment_multishift.py
autonomous: true
requirements: [ADD-02, SNP-01]
test_ids:
  - tests/test_phase6_exclusion.py::TestSharedExclusionContract
  - tests/test_phase6_exclusion.py::TestRescueAndAddedDetectionExclusion
  - tests/test_alignment_multishift.py::test_generate_candidates_excludes_tolerance_block_boilerplate
must_haves:
  truths:
    - "Anchor lookup, candidate generation, rescue scans, and added detection all use one shared exclusion contract"
    - "Boilerplate rejection combines geometric zones with normalized-text checks instead of per-file heuristics"
    - "Legitimate edge annotations are not hidden by an over-broad title-block filter"
  artifacts:
    - path: "delta_preservation/reconcile/exclusion.py"
      provides: "Shared page-dimension estimation and annotation-search exclusion helpers"
      contains: "def span_is_excluded_for_annotation_search("
    - path: "delta_preservation/reconcile/anchors.py"
      provides: "Rev A anchor filtering wired to shared exclusion helpers"
      contains: "span_is_excluded_for_annotation_search("
    - path: "delta_preservation/reconcile/classify.py"
      provides: "Keyword rescue and added detection wired to the same exclusion helpers"
      contains: "estimate_page_dimensions("
    - path: "tests/test_phase6_exclusion.py"
      provides: "Phase-6 unit coverage for boilerplate and title-block exclusion"
      contains: "class TestSharedExclusionContract"
---

<objective>
Create the shared exclusion contract that Phase 6 depends on. Right now `match.py` owns the strongest title-block and boilerplate filter, but `anchors.py`, the keyword rescue path in `classify.py`, and `detect_added_characteristics()` still use weaker local heuristics. This plan centralizes the contract and migrates every annotation-search caller onto it before Phase 6 starts changing added-row behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-CONTEXT.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-RESEARCH.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-PATTERNS.md
@delta_preservation/reconcile/anchors.py
@delta_preservation/reconcile/match.py
@delta_preservation/reconcile/classify.py
@tests/test_alignment_multishift.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exclusion-focused unit tests before moving helpers</name>
  <files>tests/test_phase6_exclusion.py, tests/test_alignment_multishift.py</files>
  <read_first>delta_preservation/reconcile/match.py, delta_preservation/reconcile/anchors.py, delta_preservation/reconcile/classify.py, .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-RESEARCH.md</read_first>
  <action>
Create `tests/test_phase6_exclusion.py` with two test classes.

`TestSharedExclusionContract` must cover:
1. bottom-right title-block text such as `UNLESS OTHERWISE SPECIFIED` is excluded
2. bottom-center tolerance-block boilerplate is excluded
3. revision-table style text in the upper-right is excluded
4. a legitimate drawing annotation near the edge is not excluded
5. page-dimension estimation applies the same minimum-width and minimum-height floors used today in `match.py`

`TestRescueAndAddedDetectionExclusion` must cover:
1. the keyword rescue path skips spans in excluded regions
2. `detect_added_characteristics()` skips the same excluded spans instead of re-implementing its own corner filter

Also keep `tests/test_alignment_multishift.py::test_generate_candidates_excludes_tolerance_block_boilerplate` as the cross-file smoke proving candidate generation still rejects the known tolerance-block false-positive input.

Use inline fake span objects or lightweight fixtures; do not read PDF assets in this plan.
</action>
  <acceptance_criteria>
    - `tests/test_phase6_exclusion.py` exists and contains `class TestSharedExclusionContract` and `class TestRescueAndAddedDetectionExclusion`.
    - `rg -n "UNLESS OTHERWISE SPECIFIED|revision|tolerance-block|TestSharedExclusionContract|TestRescueAndAddedDetectionExclusion" tests/test_phase6_exclusion.py` returns the new coverage.
    - `uv run pytest tests/test_phase6_exclusion.py tests/test_alignment_multishift.py::test_generate_candidates_excludes_tolerance_block_boilerplate -x` fails before the production edits and exits 0 after them.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract the shared exclusion module and migrate all callers</name>
  <files>delta_preservation/reconcile/exclusion.py, delta_preservation/reconcile/anchors.py, delta_preservation/reconcile/match.py, delta_preservation/reconcile/classify.py</files>
  <read_first>delta_preservation/reconcile/match.py, delta_preservation/reconcile/anchors.py, delta_preservation/reconcile/classify.py, .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-PATTERNS.md</read_first>
  <action>
Create `delta_preservation/reconcile/exclusion.py` and move the reusable exclusion logic there.

The module must expose at least:
- `estimate_page_dimensions(spans, *, min_width=612.0, min_height=792.0)`
- `is_boilerplate_candidate_text(text: str) -> bool`
- `span_is_excluded_for_annotation_search(span, *, page_width: float, page_height: float) -> bool`

Implementation contract:
- Preserve the existing normalized-text checks from `match.py` (`" ".join(text.strip().upper().split())`)
- Preserve the existing minimum page-size floors so small cropped pages do not collapse exclusion zones
- Keep the contract centered on span centers / bbox centers rather than raw `x0` or `y0` alone
- Do not make unrelated modules import private helpers from `match.py`

Then migrate these callers to the new shared helpers:
- `build_revA_anchors()` in `anchors.py`
- candidate generation in `match.py`
- the keyword rescue scan in `classify_delta()`
- `detect_added_characteristics()` in `classify.py`

Delete duplicated local exclusion helpers once all callers are on the shared module, or reduce them to thin wrappers if a public call surface must be preserved for compatibility.
</action>
  <acceptance_criteria>
    - `rg -n "def estimate_page_dimensions|def is_boilerplate_candidate_text|def span_is_excluded_for_annotation_search" delta_preservation/reconcile/exclusion.py` returns three matches.
    - `rg -n "from delta_preservation.reconcile.exclusion import" delta_preservation/reconcile/anchors.py delta_preservation/reconcile/match.py delta_preservation/reconcile/classify.py` returns matches in all three production files.
    - `rg -n "span_is_excluded_for_annotation_search\\(" delta_preservation/reconcile/anchors.py delta_preservation/reconcile/match.py delta_preservation/reconcile/classify.py` returns the migrated call sites.
    - `uv run pytest tests/test_phase6_exclusion.py tests/test_alignment_multishift.py::test_generate_candidates_excludes_tolerance_block_boilerplate -x` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF text spans -> exclusion helper | Raw drawing text and boilerplate strings enter shared filtering logic that now gates every search surface. |
| Shared helper -> reconcile callers | A bug in the shared contract would affect anchors, matching, rescue scans, and added detection simultaneously. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `delta_preservation/reconcile/exclusion.py` | mitigate | Add explicit tests for title block, revision table, and bottom-center boilerplate plus a legitimate edge annotation negative control. |
| T-06-02 | I | `anchors.py`, `match.py`, `classify.py` | mitigate | Migrate every caller in the same plan so no local heuristic remains to drift from the shared contract. |
| T-06-03 | D | `delta_preservation/reconcile/exclusion.py` | mitigate | Keep the helper pure and single-pass, with anchored normalized-text checks and bounded page-dimension estimation. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/test_phase6_exclusion.py tests/test_alignment_multishift.py::test_generate_candidates_excludes_tolerance_block_boilerplate -x`
- `rg -n "span_is_excluded_for_annotation_search|estimate_page_dimensions|is_boilerplate_candidate_text" delta_preservation/reconcile/exclusion.py delta_preservation/reconcile/anchors.py delta_preservation/reconcile/match.py delta_preservation/reconcile/classify.py`
</verification>

<success_criteria>
- SNP-01 has one reusable exclusion contract instead of three drifting heuristics.
- Added detection and rescue scans cannot re-introduce title-block text after candidate generation filtered it out.
- The shared contract remains conservative enough to keep legitimate edge annotations visible.
</success_criteria>

<output>
After completion, create `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-01-SUMMARY.md`
</output>
