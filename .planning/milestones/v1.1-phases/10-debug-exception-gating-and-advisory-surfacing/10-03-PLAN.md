---
phase: 10-debug-exception-gating-and-advisory-surfacing
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - shop/services/exports.py
  - shop/routers/exports.py
  - shop/templates/exports/audit_packet.html
  - shop/templates/exports/work_order.html
  - tests/test_exports.py
  - tests/test_review.py
autonomous: true
requirements: [CLS-01, ADD-01, ADD-02, SNP-01, VER-01]
test_ids:
  - tests/test_exports.py::test_audit_packet_csv_includes_confidence_flags_and_debug_state_from_signed_snapshot
  - tests/test_exports.py::test_work_order_csv_uses_signed_snapshot_advisories_without_materializing_missing_truth_rows
  - tests/test_exports.py::test_export_routes_require_signed_debug_snapshot_metadata
must_haves:
  truths:
    - "Signed audit/work-order exports render from the sign-off-time debug snapshot rather than current mutable debug verdict files."
    - "Audit/work-order exports surface packet-native advisory flags and signed debug state without replacing the core classification/reviewer-decision contract."
    - "Synthetic missing-added truth rows may appear in signed audit/debug summary context, but they do not become work-order action rows."
  artifacts:
    - path: "shop/services/exports.py"
      provides: "Signed debug snapshot loader and export row merge helpers"
      contains: "def _load_signed_debug_snapshot("
    - path: "shop/templates/exports/audit_packet.html"
      provides: "Audit packet advisory/debug rendering"
      contains: "Confidence Flags"
    - path: "shop/templates/exports/work_order.html"
      provides: "Work-order signed debug summary and advisory surfacing"
      contains: "Signed Debug Summary"
  key_links:
    - from: "Run.packet_versions[].debug_snapshot_path"
      to: "shop/services/exports.py"
      via: "signed debug snapshot lookup"
      pattern: "debug_snapshot_path"
    - from: "signed debug snapshot rows"
      to: "audit/work-order export rows"
      via: "review_item_id merge"
      pattern: "review_item_id"
---

<objective>
Preserve the same advisory and gating state the maintainer cleared during
review inside the signed audit/work-order exports.

Purpose: Phase 10 is not complete if the maintainer sees advisory flags and a
cleared debug gate in review, but the signed CSV/PDF exports omit that state or
recompute it from mutable post-sign-off files. This plan makes signed exports
read from the captured debug snapshot and surface the relevant advisory/debug
state in the exported artifacts.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-CONTEXT.md
@.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-RESEARCH.md
@shop/services/review.py
@shop/services/exports.py
@shop/routers/exports.py
@shop/templates/exports/audit_packet.html
@shop/templates/exports/work_order.html
@tests/test_exports.py
@tests/test_review.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load signed debug snapshot metadata in export services and guard export routes on signed snapshot availability</name>
  <files>
shop/services/exports.py,
shop/routers/exports.py,
tests/test_exports.py
  </files>
  <read_first>
shop/services/exports.py,
shop/routers/exports.py,
tests/test_exports.py,
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-RESEARCH.md
  </read_first>
  <action>
Add a signed-snapshot export contract:

1. In `shop/services/exports.py`, add a helper
   `_load_signed_debug_snapshot(run: Run, version: int | None = None) -> dict`
   that:
   - resolves the matching `packet_versions` entry,
   - reads the stored `debug_snapshot_path`,
   - returns the captured payload plus version metadata.
2. In `shop/routers/exports.py`, tighten `_get_signed_run()` so exports require
   both a signed run and a matching signed debug snapshot contract for the
   requested version/current version.
   - If the run is `signed_off` but the snapshot metadata is missing or unreadable,
     fail clearly (403/409), do not silently fall back to current mutable state.
3. Leave the existing audit packet PDF file-serving behavior intact when the
   stored version exists, but use the signed snapshot as the authoritative
   metadata source for route eligibility and any live fallback rendering.

Add this exact test:
- `tests/test_exports.py::test_export_routes_require_signed_debug_snapshot_metadata`
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_exports.py -k "signed_debug_snapshot_metadata" -x</automated>
  </verify>
  <acceptance_criteria>
    - `shop/services/exports.py` contains `def _load_signed_debug_snapshot(`.
    - `shop/routers/exports.py` contains `debug_snapshot_path` or equivalent signed snapshot gate checks.
    - `uv run pytest -q tests/test_exports.py -k "signed_debug_snapshot_metadata" -x` exits 0.
  </acceptance_criteria>
  <done>Signed export routes now require the sign-off-time debug snapshot contract rather than trusting `run.status` alone.</done>
</task>

<task type="auto">
  <name>Task 2: Surface signed advisory and debug state in audit/work-order exports without turning synthetic rows into work-order tasks</name>
  <files>
shop/services/exports.py,
shop/templates/exports/audit_packet.html,
shop/templates/exports/work_order.html,
tests/test_exports.py
  </files>
  <read_first>
shop/services/exports.py,
shop/templates/exports/audit_packet.html,
shop/templates/exports/work_order.html,
tests/test_exports.py,
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-CONTEXT.md
  </read_first>
  <action>
Update export rendering to consume the signed debug snapshot:

1. Audit packet CSV/PDF:
   - Merge signed snapshot rows onto review/export rows by `review_item_id`.
   - Surface `confidence_flags` and signed debug state (`row_state` /
     `debug_verdict`) for each review item.
   - Add a signed debug summary section that records the cleared unresolved
     count and any captured missing-added summary state.
2. Work-order CSV/PDF:
   - Add `confidence_flags` to actionable changed/added rows.
   - Add a compact signed debug summary/header note so the work order reflects
     the same cleared debug context as sign-off.
   - Do **not** materialize synthetic missing-added truth rows as work-order
     tasks; they belong in the audit/debug summary contract, not the work-order
     action list.

Add these exact tests:
- `tests/test_exports.py::test_audit_packet_csv_includes_confidence_flags_and_debug_state_from_signed_snapshot`
- `tests/test_exports.py::test_work_order_csv_uses_signed_snapshot_advisories_without_materializing_missing_truth_rows`

Prefer CSV assertions and template-rendered string assertions over PDF byte
inspection. The PDF templates must still include the same headings/labels so a
manual export spot-check can confirm layout after the automated tests pass.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_exports.py -k "confidence_flags_and_debug_state_from_signed_snapshot or work_order_csv_uses_signed_snapshot_advisories_without_materializing_missing_truth_rows" -x</automated>
  </verify>
  <acceptance_criteria>
    - `shop/templates/exports/audit_packet.html` contains `Confidence Flags`.
    - `shop/templates/exports/work_order.html` contains `Signed Debug Summary`.
    - `rg -n "confidence_flags|debug_row_state|debug_verdict" shop/services/exports.py shop/templates/exports/audit_packet.html shop/templates/exports/work_order.html` returns matches.
    - `uv run pytest -q tests/test_exports.py -k "confidence_flags_and_debug_state_from_signed_snapshot or work_order_csv_uses_signed_snapshot_advisories_without_materializing_missing_truth_rows" -x` exits 0.
  </acceptance_criteria>
  <done>Signed audit/work-order exports now preserve the sign-off-time advisory and debug state without polluting work-order actions with synthetic debug rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signed packet version metadata -> export rendering | Exports must read the sign-off-time snapshot, not current mutable debug files. |
| Signed debug snapshot rows -> work-order task list | Synthetic debug-only rows must not become actionable work-order rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Traceability | signed export rendering | mitigate | Load export state from `packet_versions[].debug_snapshot_path` and fail clearly if the snapshot contract is missing. |
| T-10-06 | Integrity | work-order export | mitigate | Merge signed snapshot state by `review_item_id` and keep synthetic missing-truth rows in the audit/debug summary layer only. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_exports.py -k "signed_debug_snapshot_metadata" -x`
- [ ] `uv run pytest -q tests/test_exports.py -k "confidence_flags_and_debug_state_from_signed_snapshot or work_order_csv_uses_signed_snapshot_advisories_without_materializing_missing_truth_rows" -x`
</verification>

<success_criteria>

- All tasks completed
- Signed export routes require a valid signed debug snapshot contract
- Audit/work-order exports surface advisory/debug state from the signed snapshot
- Synthetic missing-added rows remain outside the work-order action list

</success_criteria>
