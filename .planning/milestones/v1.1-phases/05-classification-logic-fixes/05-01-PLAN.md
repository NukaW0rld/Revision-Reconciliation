---
phase: 05-classification-logic-fixes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - delta_preservation/reconcile/classify.py
  - delta_preservation/types.py
  - delta_preservation/cli.py
  - tests/test_classify_bugfixes.py
  - tests/test_pipeline_semantic_packet.py
  - tests/test_output_formatting.py
autonomous: true
requirements: [CLS-01, CLS-02, CLS-03]
test_ids:
  - tests/test_classify_bugfixes.py::TestConfidenceFlagsCompatibility
  - tests/test_pipeline_semantic_packet.py
  - tests/test_output_formatting.py
must_haves:
  truths:
    - "Internal and persisted DeltaItem models both accept missing confidence_flags and default to []"
    - "CLI conversion uses getattr(delta_internal, 'confidence_flags', []) so legacy/fake internal items do not crash"
    - "Existing packet/formatting test doubles either define confidence_flags or are safely tolerated"
  artifacts:
    - path: "delta_preservation/reconcile/classify.py"
      provides: "Internal DeltaItem scaffold with confidence_flags default"
      contains: "confidence_flags: List[str] = field(default_factory=list)"
    - path: "delta_preservation/types.py"
      provides: "Persisted DeltaItem scaffold with backward-compatible confidence_flags"
      contains: "confidence_flags: List[str] = Field("
    - path: "delta_preservation/cli.py"
      provides: "Pydantic conversion tolerates delta_internal objects without confidence_flags"
      contains: "confidence_flags=getattr(delta_internal, \"confidence_flags\", [])"
    - path: "tests/test_classify_bugfixes.py"
      provides: "Inline compatibility tests for missing confidence_flags"
      contains: "class TestConfidenceFlagsCompatibility"
---

<objective>
Create the Phase 5 scaffold without the brittle parts called out in review: add the additive `confidence_flags` field to both DeltaItem models, harden the CLI conversion against legacy/fake internal objects, and land concrete compatibility tests instead of empty stubs.

Why this plan exists:
- CLS-01 needs a persisted advisory flag.
- CLS-02 and CLS-03 will reuse the same field for auditability if needed.
- The review found two immediate risks in the original scaffold: `cli.py` would read `delta_internal.confidence_flags` from fake objects that do not define it, and the old backward-compat plan relied on speculative fixtures instead of deterministic inline payloads.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-classification-logic-fixes/05-RESEARCH.md
@.planning/phases/05-classification-logic-fixes/05-PATTERNS.md
@.planning/phases/05-classification-logic-fixes/05-REVIEWS.md
@delta_preservation/reconcile/classify.py
@delta_preservation/types.py
@delta_preservation/cli.py
@tests/test_classify_bugfixes.py
@tests/test_pipeline_semantic_packet.py
@tests/test_output_formatting.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace empty Phase 5 stubs with concrete compatibility tests</name>
  <files>tests/test_classify_bugfixes.py</files>
  <read_first>tests/test_classify_bugfixes.py, delta_preservation/reconcile/classify.py, delta_preservation/types.py, .planning/phases/05-classification-logic-fixes/05-REVIEWS.md</read_first>
  <action>
Extend `tests/test_classify_bugfixes.py` with a new `TestConfidenceFlagsCompatibility` class.

Add exactly these three tests:
1. `test_internal_deltaitem_defaults_to_independent_lists`
2. `test_packet_deltaitem_validates_legacy_payload_without_confidence_flags`
3. `test_packet_deltaitem_model_dump_includes_confidence_flags`

Implementation details:
- Import `DeltaItem` from `delta_preservation.types` under an alias such as `PacketDeltaItem`.
- The legacy payload test must build an inline dict that omits `confidence_flags` but still includes the required `status`, `confidence`, `reasons`, and `scores` keys.
- Do not load `assets/debug_report_part*.json` in this scaffold plan. The review explicitly asked for deterministic inline backward-compat coverage instead of speculative fixture lookups.
</action>
  <acceptance_criteria>
    - `tests/test_classify_bugfixes.py` contains `class TestConfidenceFlagsCompatibility`.
    - `rg -n "test_internal_deltaitem_defaults_to_independent_lists|test_packet_deltaitem_validates_legacy_payload_without_confidence_flags|test_packet_deltaitem_model_dump_includes_confidence_flags" tests/test_classify_bugfixes.py` returns three matches.
    - `pytest tests/test_classify_bugfixes.py::TestConfidenceFlagsCompatibility -x` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add confidence_flags to both DeltaItem models and harden CLI conversion</name>
  <files>delta_preservation/reconcile/classify.py, delta_preservation/types.py, delta_preservation/cli.py</files>
  <read_first>delta_preservation/reconcile/classify.py, delta_preservation/types.py, delta_preservation/cli.py, .planning/phases/05-classification-logic-fixes/05-PATTERNS.md</read_first>
  <action>
Edit the three production files as follows.

In `delta_preservation/reconcile/classify.py`:
- Change `from dataclasses import dataclass` to `from dataclasses import dataclass, field`.
- Add `confidence_flags: List[str] = field(default_factory=list)` as the final field on the internal `DeltaItem` dataclass.

In `delta_preservation/types.py`:
- Add `confidence_flags: List[str] = Field(default_factory=list, description="Classifier advisory flags such as bleed warnings")` immediately after `reasons`.
- Keep the field additive and optional. Do not convert `reasons` or `scores` to optional fields.

In `delta_preservation/cli.py`:
- When constructing the Pydantic `DeltaItem`, pass `confidence_flags=getattr(delta_internal, "confidence_flags", [])`.
- Use `getattr`, not `delta_internal.confidence_flags`, so monkeypatched or legacy fake internal objects without the attribute do not crash packet generation.
</action>
  <acceptance_criteria>
    - `rg -n "confidence_flags: List\\[str\\] = field\\(default_factory=list\\)" delta_preservation/reconcile/classify.py` returns exactly one match.
    - `rg -n "confidence_flags: List\\[str\\] = Field\\(default_factory=list" delta_preservation/types.py` returns exactly one match.
    - `rg -n "confidence_flags=getattr\\(delta_internal, \\\"confidence_flags\\\", \\[\\]\\)" delta_preservation/cli.py` returns exactly one match.
    - `pytest tests/test_classify_bugfixes.py::TestConfidenceFlagsCompatibility -x` exits 0 after the production edits land.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update the fake internal DeltaItem helpers used by CLI-facing tests</name>
  <files>tests/test_pipeline_semantic_packet.py, tests/test_output_formatting.py</files>
  <read_first>tests/test_pipeline_semantic_packet.py, tests/test_output_formatting.py, delta_preservation/cli.py, .planning/phases/05-classification-logic-fixes/05-REVIEWS.md</read_first>
  <action>
Edit both `_FakeInternalDeltaItem` helper classes so they accept an optional `confidence_flags=None` constructor argument and always set `self.confidence_flags` to `list(confidence_flags or [])`.

Keep the CLI `getattr(..., [])` fallback from Task 2 even after updating these fakes. The plan intentionally does both:
- explicit fake-object compatibility for the existing tests
- defensive CLI conversion for future monkeypatches and any ad hoc debugging helpers

Do not add any empty placeholder classes in this task.
</action>
  <acceptance_criteria>
    - `rg -n "confidence_flags=None|self\\.confidence_flags = list\\(confidence_flags or \\[\\]\\)" tests/test_pipeline_semantic_packet.py tests/test_output_formatting.py` returns matches in both files.
    - `pytest tests/test_pipeline_semantic_packet.py tests/test_output_formatting.py -x` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
- `pytest tests/test_classify_bugfixes.py::TestConfidenceFlagsCompatibility -x`
- `pytest tests/test_pipeline_semantic_packet.py tests/test_output_formatting.py -x`
- `python3 - <<'PY'\nfrom delta_preservation.reconcile.classify import DeltaItem as Internal\nfrom delta_preservation.types import DeltaItem as Packet\ninternal = Internal(char_no=1, status='unchanged', confidence=0.5, reasons=[], component_scores={})\npacket = Packet(char_no=1, status='unchanged', confidence=0.5, reasons=[], scores={})\nassert internal.confidence_flags == []\nassert packet.confidence_flags == []\nprint('ok')\nPY`
</verification>

<success_criteria>
- The scaffold is additive: existing callers that omit `confidence_flags` still work.
- CLI packet conversion does not assume every internal item defines `confidence_flags`.
- No empty Phase 5 placeholder tests remain; every new test added in this plan asserts a concrete compatibility property.
</success_criteria>
