---
phase: 12-process-artifact-closure-and-metadata-reconciliation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "REQUIREMENTS.md TST-01 checkbox is `[x]` so the canonical requirements doc matches the 07-VERIFICATION.md SATISFIED finding already on record."
    - "ROADMAP.md Phase 11 progress row shows `2/2` plans complete and `Complete` status with the correct completion date, matching the already-executed Phase 11 work (two plans + SUMMARY files + review fix)."
    - "ROADMAP.md Phase Details section lists both Phase 11 plan checkboxes as `[x]` and the Phase 11 bullet in the milestone summary is `[x]`, so the roadmap no longer shows Phase 11 as planned."
    - "Phase 6 `06-VERIFICATION.md` frontmatter `status` is `passed` because every deferred truth is explicitly closed by Phase 9 per the deferred-items table, and the body records that status transition with a reference to Phase 9 closure evidence."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical requirements doc with TST-01 checkbox aligned to verification reality"
      contains: "[x] **TST-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap with accurate Phase 11 progress, completion, and plan checkboxes"
      contains: "| 11. Web Run-to-Review E2E Automation | v1.1 | 2/2 | Complete |"
    - path: ".planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md"
      provides: "Phase 6 verification record with status reflecting Phase 9 closure of all deferred items"
      contains: "status: passed"
  key_links:
    - from: ".planning/REQUIREMENTS.md TST-01"
      to: ".planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md"
      via: "checkbox now matches SATISFIED finding"
      pattern: "TST-01"
    - from: ".planning/ROADMAP.md Phase 11 row"
      to: ".planning/phases/11-web-run-to-review-e2e-automation/"
      via: "progress/checkboxes reflect executed plans and summaries"
      pattern: "11. Web Run-to-Review E2E Automation"
    - from: ".planning/phases/06-...-snippet-accuracy/06-VERIFICATION.md"
      to: ".planning/phases/09-full-corpus-added-characteristic-closure/"
      via: "deferred items closed by Phase 9 flip status to passed"
      pattern: "CLOSED Phase 9"
---

<objective>
Flip three stale-metadata flags that the v1.1 milestone audit documented as
"stale" but functionally correct:

1. REQUIREMENTS.md `TST-01` checkbox is `[ ]` while `07-VERIFICATION.md` records
   SATISFIED — flip to `[x]`.
2. ROADMAP.md Phase 11 progress row still shows `0/2 | Planned` and the Phase
   Details plan checkboxes are `[ ]`, even though both plans have SUMMARY.md
   files and review fix — flip to `2/2 | Complete` with the right date and mark
   both plan checkboxes `[x]` (and update the top-of-roadmap milestone bullet).
3. Phase 6 `06-VERIFICATION.md` frontmatter `status: human_needed` is stale
   because the deferred items it names were both explicitly closed by Phase 9
   per the v1.1 audit — flip to `status: passed` and document the transition
   with a reference to Phase 9 closure evidence.

Purpose: these three edits are the "stale metadata" portion of the v1.1 audit
gap list. No source code or tests change; only planning docs are updated.
</objective>

<execution_context>
@$HOME/.codeium/windsurf/get-shit-done/workflows/execute-plan.md
@$HOME/.codeium/windsurf/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md
@.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-01-SUMMARY.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip REQUIREMENTS.md TST-01 checkbox from [ ] to [x]</name>
  <files>
.planning/REQUIREMENTS.md
  </files>
  <read_first>
.planning/REQUIREMENTS.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
.planning/milestones/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>
In `.planning/REQUIREMENTS.md`, locate the Regression Tests section line:

```
- [ ] **TST-01**: Each fixed failure cluster has at least one pytest parametrized case that
```

and change the checkbox from `[ ]` to `[x]` — one character change. Do not
modify the TST-01 description text, neighbouring lines, or any other checkbox.

Do not touch TST-02 or VER-01 (both already `[x]` per current file). Do not add
or remove comments; just flip the single checkbox character.

Verify by re-reading the file that only the TST-01 line checkbox changed and
that the traceability table (REQ-ID / Phase / Plan) still lists `TST-01 | Phase
7 | —`.
  </action>
  <verify>
    <automated>grep -cE '^- \[x\] \*\*TST-01\*\*' .planning/REQUIREMENTS.md | grep -qx 1 &amp;&amp; ! grep -qE '^- \[ \] \*\*TST-01\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a line starting with `- [x] **TST-01**:` (exactly once).
    - `.planning/REQUIREMENTS.md` contains no line starting with `- [ ] **TST-01**`.
    - The TST-01 description text ("Each fixed failure cluster has at least one pytest parametrized case that...") is unchanged — verify by grepping for the substring `Each fixed failure cluster has at least one pytest parametrized case`.
    - TST-02 and VER-01 checkboxes are still `[x]` — unchanged by this task.
  </acceptance_criteria>
  <done>REQUIREMENTS.md TST-01 checkbox now matches the SATISFIED finding already recorded in 07-VERIFICATION.md; the audit's "stale checkbox" line item is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md Phase 11 progress row, plan checkboxes, and milestone bullet</name>
  <files>
.planning/ROADMAP.md
  </files>
  <read_first>
.planning/ROADMAP.md,
.planning/milestones/v1.1-MILESTONE-AUDIT.md,
.planning/phases/11-web-run-to-review-e2e-automation/11-01-SUMMARY.md,
.planning/phases/11-web-run-to-review-e2e-automation/11-02-SUMMARY.md
  </read_first>
  <action>
Apply exactly three edits to `.planning/ROADMAP.md`:

1. In the top "🚧 v1.1 Cross-part Characteristic Matching Refinement (In
   Progress)" bullet list, flip the Phase 11 bullet from:
   ```
   - [ ] **Phase 11: Web Run-to-Review E2E Automation** - ...
   ```
   to:
   ```
   - [x] **Phase 11: Web Run-to-Review E2E Automation** - ... (completed <DATE>)
   ```
   where `<DATE>` is the most recent commit date present on either
   `11-01-SUMMARY.md`, `11-02-SUMMARY.md`, or `11-REVIEW-FIX.md` — obtain it
   via `git log -1 --format=%ad --date=short -- .planning/phases/11-web-run-to-review-e2e-automation/`.
   If that command returns empty or errors, fall back to the audit timestamp
   date `2026-04-19`. Do NOT change the hyphen between the title and "completed"
   — mirror the phrasing pattern used by the Phase 10 bullet above it.

2. In the "### Phase 11: Web Run-to-Review E2E Automation" Phase Details
   section, flip both plan checkbox lines:
   ```
   - [ ] 11-01-PLAN.md — ...
   - [ ] 11-02-PLAN.md — ...
   ```
   to:
   ```
   - [x] 11-01-PLAN.md — ...
   - [x] 11-02-PLAN.md — ...
   ```
   (Preserve the existing description text after `—`; change only the two
   checkbox characters.)

3. In the "## Progress" table near the bottom, change the Phase 11 row from:
   ```
   | 11. Web Run-to-Review E2E Automation | v1.1 | 0/2 | Planned | — |
   ```
   to:
   ```
   | 11. Web Run-to-Review E2E Automation | v1.1 | 2/2 | Complete | <DATE> |
   ```
   using the same `<DATE>` from edit 1.

Do NOT touch the Phase 12 row (it is still in-progress by virtue of this very
phase) — leave it as `0/0 | Planned | —` until Phase 12 itself completes.
  </action>
  <verify>
    <automated>grep -q "^- \[x\] \*\*Phase 11: Web Run-to-Review E2E Automation\*\*" .planning/ROADMAP.md &amp;&amp; grep -cE "^\s*- \[x\] 11-0[12]-PLAN\.md" .planning/ROADMAP.md | grep -qx 2 &amp;&amp; grep -Eq "\| 11\. Web Run-to-Review E2E Automation \| v1\.1 \| 2/2 \| Complete" .planning/ROADMAP.md &amp;&amp; ! grep -qE "\| 11\. Web Run-to-Review E2E Automation \| v1\.1 \| 0/2 \| Planned" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - The milestone bullet list contains `- [x] **Phase 11: Web Run-to-Review E2E Automation**` with a `(completed <DATE>)` suffix, exactly once.
    - The Phase 11 Phase Details block has two lines matching `- [x] 11-01-PLAN.md` and `- [x] 11-02-PLAN.md` (no remaining `- [ ] 11-01-PLAN.md` or `- [ ] 11-02-PLAN.md`).
    - The Progress table has exactly one row matching the pattern `| 11. Web Run-to-Review E2E Automation | v1.1 | 2/2 | Complete | <date> |` and no row matching `| 11. ... | v1.1 | 0/2 | Planned |`.
    - Phase 12 row in the Progress table is unchanged (`| 12. Process Artifact Closure and Metadata Reconciliation | v1.1 | 0/0 | Planned | — |`) — verify by grep.
    - No other phase rows (1-10) are modified — verify with `git diff --stat .planning/ROADMAP.md` reports changes confined to the three edits above.
  </acceptance_criteria>
  <done>The ROADMAP now reports Phase 11 as complete with real plan status, matching the executed SUMMARY.md files; the audit's ROADMAP-stale metadata items are closed.</done>
</task>

<task type="auto">
  <name>Task 3: Flip Phase 6 VERIFICATION status from human_needed to passed and record the Phase 9 closure transition</name>
  <files>
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md
  </files>
  <read_first>
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md,
.planning/milestones/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>
In `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md`:

1. Frontmatter — change `status: human_needed` to `status: passed`. Leave
   `deferred:` and `human_verification:` blocks intact (they document history).
   Update `score:` from `2/4 roadmap success criteria verified via code; 2 deferred
   to Phase 7 (end-to-end pipeline run)` to
   `4/4 roadmap success criteria verified (2 verified via code in Phase 6; 2 closed
   by Phase 9 full-corpus rerun)`.
   Add a new frontmatter key `re_verification: true` directly under
   `overrides_applied:` (or append if not already present) to mark this as a
   follow-up reconciliation, matching the pattern used elsewhere.

2. Body — update the "**Status:**" line near the top of the body from
   `**Status:** human_needed` (or equivalent) to `**Status:** PASSED (reconciled
   — Phase 9 closed both deferred items)`.

3. Body — append a new H2 section **at the very end** of the file (after the
   existing Conclusion / last section) titled:
   ```
   ## Phase 9 Closure Reconciliation (Phase 12 update)
   ```
   In that section, add a short table with columns
   `| Previously Deferred Truth | Phase 9 Closure Evidence | Status |`.
   Populate it with one row for each of the two `deferred:` entries currently in
   the frontmatter. In the Evidence column, cite `09-VERIFICATION.md` and any
   specific success criteria (e.g. `09 SC-1`) or summary files that close the
   truth — verify each citation by reading `09-VERIFICATION.md` before writing.
   Mark both rows `Status: CLOSED by Phase 9`.

4. Do NOT delete or rewrite the `deferred:` or `human_verification:` frontmatter
   entries — they remain the historical record. The new reconciliation section
   is additive.
  </action>
  <verify>
    <automated>grep -q "^status: passed$" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md &amp;&amp; ! grep -q "^status: human_needed$" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md &amp;&amp; grep -q "Phase 9 Closure Reconciliation" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md &amp;&amp; grep -q "CLOSED by Phase 9" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-VERIFICATION.md` frontmatter contains exactly one line `status: passed` and no line `status: human_needed`.
    - `06-VERIFICATION.md` frontmatter still contains the original `deferred:` block (at least two entries) and `human_verification:` block — they are preserved as history.
    - `06-VERIFICATION.md` frontmatter `score:` line contains the substring `closed by Phase 9`.
    - `06-VERIFICATION.md` body contains an H2 heading `## Phase 9 Closure Reconciliation (Phase 12 update)`.
    - That new section contains a table header with the literal string `Previously Deferred Truth` and at least two rows containing `CLOSED by Phase 9`.
    - Every evidence cell in the reconciliation table references a real file — `grep -c "09-VERIFICATION.md" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md` returns a value `>= 2`.
  </acceptance_criteria>
  <done>Phase 6 verification status now reflects the closure reality: deferred items are tied to Phase 9 evidence via an additive reconciliation section, and the frontmatter status no longer blocks the milestone re-audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs -> canonical requirements | This plan flips three documentation flags that are currently out of sync with their underlying evidence. Wrongly flipping a checkbox or progress flag would hide real gaps. |
| Stale metadata -> audit aggregation | The milestone re-audit reads these flags directly; a premature flip (without the underlying work being done) would produce a false-positive `passed` result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | REQUIREMENTS/ROADMAP flags | mitigate | Each acceptance criterion grep-pins both the new state AND the absence of the old state (e.g. `grep -q "^- \[x\] \*\*TST-01\*\*"` AND `! grep -q "^- \[ \] \*\*TST-01\*\*"`), preventing accidental duplicate entries or silent undo. |
| T-12-02-02 | Information Disclosure (historical record) | 06-VERIFICATION.md deferred/human_verification blocks | mitigate | Acceptance criteria require the original `deferred:` and `human_verification:` frontmatter blocks to remain intact; the reconciliation section is additive, preserving the audit trail of why the phase was originally `human_needed`. |
</threat_model>

<verification>
- [ ] `grep -cE '^- \[x\] \*\*TST-01\*\*' .planning/REQUIREMENTS.md | grep -qx 1`
- [ ] `! grep -qE '^- \[ \] \*\*TST-01\*\*' .planning/REQUIREMENTS.md`
- [ ] `grep -Eq "\| 11\. Web Run-to-Review E2E Automation \| v1\.1 \| 2/2 \| Complete" .planning/ROADMAP.md`
- [ ] `grep -cE "^\s*- \[x\] 11-0[12]-PLAN\.md" .planning/ROADMAP.md | grep -qx 2`
- [ ] `grep -q "^status: passed$" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md`
- [ ] `grep -q "## Phase 9 Closure Reconciliation" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md`
</verification>

<success_criteria>

- REQUIREMENTS.md TST-01 is `[x]` with no remaining `[ ]` instance.
- ROADMAP.md milestone bullet, Phase Details checkboxes, and Progress table all report Phase 11 as complete with a real completion date.
- Phase 6 `06-VERIFICATION.md` frontmatter is `status: passed`, history is preserved, and a new reconciliation section cites `09-VERIFICATION.md` for each previously-deferred truth.
- No other checkboxes, phase rows, or verification files are modified.

</success_criteria>
