---
phase: 12-process-artifact-closure-and-metadata-reconciliation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md
  - .planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md
  - .planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md
  - .planning/phases/07-regression-tests-and-verification/07-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 6 06-VALIDATION.md frontmatter shows `nyquist_compliant: true` and `wave_0_complete: true`, with a reconciliation note pointing at Phase 9 closure so the flip is auditable, not silent."
    - "Phase 9 09-VALIDATION.md frontmatter shows `nyquist_compliant: true`, with a reconciliation note pointing at 09-VERIFICATION.md evidence."
    - "Phase 10 10-VALIDATION.md frontmatter shows `nyquist_compliant: true`, with a reconciliation note pointing at 10-VERIFICATION.md evidence."
    - "Phase 7 has a new 07-VALIDATION.md that follows the same schema used by sibling validation docs, reflects the final verified state of the phase, and sets `nyquist_compliant: true` with `wave_0_complete: true`."
  artifacts:
    - path: ".planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md"
      provides: "Reconciled Phase 6 Nyquist validation doc"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md"
      provides: "Reconciled Phase 9 Nyquist validation doc"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md"
      provides: "Reconciled Phase 10 Nyquist validation doc"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/07-regression-tests-and-verification/07-VALIDATION.md"
      provides: "New Phase 7 Nyquist validation doc reflecting final verified state"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "06-VALIDATION.md flip"
      to: "09-VERIFICATION.md"
      via: "Phase 9 closure evidence for Phase 6 deferred items"
      pattern: "09-VERIFICATION.md"
    - from: "09-VALIDATION.md flip"
      to: "09-VERIFICATION.md"
      via: "verified status backs Nyquist compliance"
      pattern: "verified"
    - from: "10-VALIDATION.md flip"
      to: "10-VERIFICATION.md"
      via: "passed status backs Nyquist compliance"
      pattern: "passed"
    - from: "new 07-VALIDATION.md"
      to: "07-VERIFICATION.md"
      via: "plan/wave graph and test-infrastructure section mirror sibling validation docs"
      pattern: "Plan / Wave Graph"
---

<objective>
Reconcile Nyquist VALIDATION.md flags across Phases 6, 7, 9, and 10 so the
milestone re-audit reports Nyquist as COMPLIANT instead of PARTIAL.

Four edits:
1. Flip `06-VALIDATION.md` `nyquist_compliant: false` / `wave_0_complete: false`
   to `true` / `true` with a reconciliation note pointing at Phase 9 closure.
2. Flip `09-VALIDATION.md` `nyquist_compliant: false` to `true` with a
   reconciliation note pointing at `09-VERIFICATION.md`.
3. Flip `10-VALIDATION.md` `nyquist_compliant: false` to `true` with a
   reconciliation note pointing at `10-VERIFICATION.md`.
4. Create a new `07-VALIDATION.md` using the same schema as the sibling
   validation docs, reflecting the already-verified Phase 7 state.

Purpose: the v1.1 audit explicitly lists these flags as stale "tech debt"
(`partial_phases: [6, 9, 10]`, `missing_phases: [7]`). The underlying work is
already verified — the flags just haven't been updated. No source code changes.
</objective>

<execution_context>
@$HOME/.codeium/windsurf/get-shit-done/workflows/execute-plan.md
@$HOME/.codeium/windsurf/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md
@.planning/phases/05-classification-logic-fixes/05-VALIDATION.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md
@.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md
@.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md
@.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VERIFICATION.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Phase 6 06-VALIDATION.md Nyquist flags and add Phase 9 closure reconciliation note</name>
  <files>
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md
  </files>
  <read_first>
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md,
.planning/milestones/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>
In `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md`:

1. Frontmatter — change:
   - `nyquist_compliant: false` -> `nyquist_compliant: true`
   - `wave_0_complete: false` -> `wave_0_complete: true`
   - leave `phase`, `slug`, `status`, and `created` keys unchanged.
   - Add a new frontmatter key directly under `created:`:
     `reconciled: <today YYYY-MM-DD>` (matches the date-format used by `created:`).

2. Body — append a new H2 section at the end of the file titled:
   ```
   ## Phase 9 Closure Reconciliation (Phase 12 update)
   ```
   In that section, write one short paragraph stating that the Nyquist flags
   were flipped because the two Phase 6 deferred items were closed by Phase 9
   (full-corpus added-characteristic closure) per `09-VERIFICATION.md`. Include
   an inline citation such as
   `See 09-VERIFICATION.md and v1.1-MILESTONE-AUDIT.md (tech_debt: phase 06)`
   so the transition is traceable.

3. Do not delete the existing "Plan / Wave Graph", "Test Infrastructure",
   "Sampling Rate", or any other historical section — they remain the plan of
   record for Phase 6's validation strategy.
  </action>
  <verify>
    <automated>grep -q "^nyquist_compliant: true$" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md &amp;&amp; grep -q "^wave_0_complete: true$" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md &amp;&amp; ! grep -qE "^nyquist_compliant: false$|^wave_0_complete: false$" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md &amp;&amp; grep -q "Phase 9 Closure Reconciliation" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-VALIDATION.md` frontmatter contains `nyquist_compliant: true` and `wave_0_complete: true`, each exactly once.
    - `06-VALIDATION.md` frontmatter contains neither `nyquist_compliant: false` nor `wave_0_complete: false`.
    - `06-VALIDATION.md` frontmatter contains a new `reconciled:` key with a `YYYY-MM-DD` value.
    - `06-VALIDATION.md` body contains the H2 heading `## Phase 9 Closure Reconciliation (Phase 12 update)`.
    - That new section cites `09-VERIFICATION.md` at least once.
    - `06-VALIDATION.md` still contains the original `## Plan / Wave Graph` (or equivalent) and `## Test Infrastructure` sections — verify by grep for `Test Infrastructure`.
  </acceptance_criteria>
  <done>Phase 6 validation doc now reports Nyquist compliance with an explicit, auditable transition note tied to Phase 9 closure.</done>
</task>

<task type="auto">
  <name>Task 2: Flip Phase 9 09-VALIDATION.md Nyquist flag and add reconciliation note</name>
  <files>
.planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md
  </files>
  <read_first>
.planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md,
.planning/milestones/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>
In `.planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md`:

1. Frontmatter — change `nyquist_compliant: false` -> `nyquist_compliant: true`.
   If `wave_0_complete` exists and is `false`, leave it alone unless the v1.1
   audit explicitly contradicts it; the v1.1 audit notes `wave_0_complete: true`
   for Phase 9, so if the file currently says `false`, flip it to `true`
   (otherwise leave). Add `reconciled: <today YYYY-MM-DD>` directly under
   `created:`.

2. Body — append an H2 section at the end:
   ```
   ## Verification Closure Reconciliation (Phase 12 update)
   ```
   One short paragraph stating the flag was flipped because `09-VERIFICATION.md`
   records the phase as `verified` with 3/3 success criteria met. Include an
   inline citation
   `See 09-VERIFICATION.md and v1.1-MILESTONE-AUDIT.md (tech_debt: phase 09)`.

3. Leave all existing body sections (plan/wave graph, test infrastructure,
   sampling rate, etc.) intact.
  </action>
  <verify>
    <automated>grep -q "^nyquist_compliant: true$" .planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md &amp;&amp; ! grep -q "^nyquist_compliant: false$" .planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md &amp;&amp; grep -q "Verification Closure Reconciliation" .planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `09-VALIDATION.md` frontmatter contains `nyquist_compliant: true` exactly once and no `nyquist_compliant: false` line.
    - `09-VALIDATION.md` frontmatter contains a new `reconciled:` key with a `YYYY-MM-DD` value.
    - `09-VALIDATION.md` body contains the H2 heading `## Verification Closure Reconciliation (Phase 12 update)`.
    - That new section cites `09-VERIFICATION.md` at least once.
    - Pre-existing section headings in `09-VALIDATION.md` are still present (check that no headings were removed by running `grep -c "^## " 09-VALIDATION.md` before and after — the count must strictly increase by 1).
  </acceptance_criteria>
  <done>Phase 9 validation doc now reports Nyquist compliance with an explicit transition note tied to 09-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 3: Flip Phase 10 10-VALIDATION.md Nyquist flag and add reconciliation note</name>
  <files>
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md
  </files>
  <read_first>
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md,
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VERIFICATION.md,
.planning/milestones/v1.1-MILESTONE-AUDIT.md
  </read_first>
  <action>
In `.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md`:

1. Frontmatter — change `nyquist_compliant: false` -> `nyquist_compliant: true`.
   Add `reconciled: <today YYYY-MM-DD>` directly under `created:`.

2. Body — append an H2 section at the end:
   ```
   ## Verification Closure Reconciliation (Phase 12 update)
   ```
   One short paragraph stating the flag was flipped because `10-VERIFICATION.md`
   records the phase as `passed` with `9/9 must-haves verified`. Include an
   inline citation
   `See 10-VERIFICATION.md and v1.1-MILESTONE-AUDIT.md (tech_debt: phase 10)`.

3. Leave all existing sections intact.
  </action>
  <verify>
    <automated>grep -q "^nyquist_compliant: true$" .planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md &amp;&amp; ! grep -q "^nyquist_compliant: false$" .planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md &amp;&amp; grep -q "Verification Closure Reconciliation" .planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `10-VALIDATION.md` frontmatter contains `nyquist_compliant: true` exactly once and no `nyquist_compliant: false` line.
    - `10-VALIDATION.md` frontmatter contains a new `reconciled:` key with a `YYYY-MM-DD` value.
    - `10-VALIDATION.md` body contains the H2 heading `## Verification Closure Reconciliation (Phase 12 update)`.
    - That new section cites `10-VERIFICATION.md` at least once.
    - Pre-existing section headings in `10-VALIDATION.md` are still present (heading count strictly increases by 1).
  </acceptance_criteria>
  <done>Phase 10 validation doc now reports Nyquist compliance with an explicit transition note tied to 10-VERIFICATION.md.</done>
</task>

<task type="auto">
  <name>Task 4: Create 07-VALIDATION.md using sibling validation schema, reflecting Phase 7's already-verified state</name>
  <files>
.planning/phases/07-regression-tests-and-verification/07-VALIDATION.md
  </files>
  <read_first>
.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md,
.planning/phases/05-classification-logic-fixes/05-VALIDATION.md,
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md,
.planning/phases/11-web-run-to-review-e2e-automation/11-VALIDATION.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
  </read_first>
  <action>
Create `.planning/phases/07-regression-tests-and-verification/07-VALIDATION.md`
modeled on the structure of `04-VALIDATION.md` / `10-VALIDATION.md` / `11-VALIDATION.md`.

1. Frontmatter (YAML, in this order):
   - `phase: 07`
   - `slug: regression-tests-and-verification`
   - `status: final` (use `final` rather than `draft` because Phase 7 is already
     verified per 07-VERIFICATION.md — choose the token that sibling validation
     docs use for completed phases; if none use `final`, use the same token
     `10-VALIDATION.md` currently uses)
   - `nyquist_compliant: true`
   - `wave_0_complete: true`
   - `created: <today YYYY-MM-DD>`
   - `reconciled: <today YYYY-MM-DD>`

2. Body — include at minimum these H2 sections, in this order, mirroring
   sibling validation docs:
   - `# Phase 7 — Validation Strategy`
   - A one-line blockquote: `> Per-phase validation contract for feedback sampling during execution.`
   - `## Plan / Wave Graph` with a table that lists all four Phase 7 plans
     (`07-01`, `07-02`, `07-03`, `07-04`) with `Wave`, `Depends On`, and
     `Validation Focus` columns. Obtain wave/depends_on values by grepping each
     `07-0X-PLAN.md` frontmatter before writing (do not invent values). If a
     plan file is missing, note it explicitly in the Validation Focus column.
   - `## Test Infrastructure` with a table matching the
     `| Property | Value |` schema used by sibling docs. Populate:
     - `Framework: pytest >=8 via uv run pytest`
     - `Config file: pyproject.toml`
     - `Quick run command`: pick a narrow Phase 7 test command — use
       `uv run pytest -q tests/test_cross_part_benchmark.py tests/test_phase7_rerun.py -x`
       ONLY if those test files exist in the repo; otherwise pick the Phase 7
       tests listed in `07-VERIFICATION.md` / plan summaries. Confirm by
       `ls tests/` before writing.
     - `Full suite command: uv run pytest -q -x`
     - `Estimated runtime`: copy the estimate from `07-VERIFICATION.md` if it
       records one; otherwise write `~45 seconds full suite`.
   - `## Sampling Rate` with a short bulleted list matching sibling doc tone
     (e.g. "After every task commit: run the narrowest task-local command",
     "Before wave rollover: run the Phase 7 quick command", "Before phase
     close: run the full suite").
   - `## Nyquist Compliance (Phase 12 reconciliation)` — one short paragraph
     stating this VALIDATION.md was authored during Phase 12 process closure to
     backfill the missing Nyquist artifact, and that compliance is backed by
     `07-VERIFICATION.md` (which records Phase 7 as `passed` / verified). Cite
     `07-VERIFICATION.md` and `v1.1-MILESTONE-AUDIT.md (nyquist: missing_phases:
     [7])`.

3. Anti-fabrication: every test file name, wave number, and plan dependency in
   the Plan / Wave Graph MUST come from files read in `<read_first>` or
   confirmed via `ls tests/`. If you cannot confirm a value, leave the cell
   blank with the word `unknown` rather than guessing.
  </action>
  <verify>
    <automated>test -s .planning/phases/07-regression-tests-and-verification/07-VALIDATION.md &amp;&amp; grep -q "^nyquist_compliant: true$" .planning/phases/07-regression-tests-and-verification/07-VALIDATION.md &amp;&amp; grep -q "^wave_0_complete: true$" .planning/phases/07-regression-tests-and-verification/07-VALIDATION.md &amp;&amp; grep -q "^## Plan / Wave Graph$" .planning/phases/07-regression-tests-and-verification/07-VALIDATION.md &amp;&amp; grep -q "^## Test Infrastructure$" .planning/phases/07-regression-tests-and-verification/07-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-regression-tests-and-verification/07-VALIDATION.md` exists and is non-empty.
    - Frontmatter contains `phase: 07`, `slug: regression-tests-and-verification`, `nyquist_compliant: true`, and `wave_0_complete: true`.
    - Body contains the headings `# Phase 7 — Validation Strategy`, `## Plan / Wave Graph`, `## Test Infrastructure`, `## Sampling Rate`, and `## Nyquist Compliance (Phase 12 reconciliation)`.
    - The Plan / Wave Graph table lists all four plan IDs: `07-01`, `07-02`, `07-03`, `07-04`.
    - The Test Infrastructure table contains a `Quick run command` row and a `Full suite command` row, each with a `uv run pytest` command that uses only test file paths that resolve to real files under `tests/`.
    - The reconciliation section cites `07-VERIFICATION.md` and references the v1.1 milestone audit.
  </acceptance_criteria>
  <done>Phase 7 now has a Nyquist VALIDATION.md consistent with sibling phases, backed by the already-verified state — closing the `nyquist: missing_phases: [7]` line item from the v1.1 audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs -> Nyquist audit aggregation | VALIDATION.md frontmatter flags are read directly by the milestone audit. A premature `true` flip without underlying verification would produce a false-positive COMPLIANT reading. |
| Sibling validation schema -> new 07-VALIDATION.md | The new file must match sibling schema so the audit recognizes it; a schema drift would leave Phase 7 effectively still missing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | VALIDATION.md Nyquist flags | mitigate | Every flip is coupled with a reconciliation section that cites the VERIFICATION.md backing it; acceptance criteria require both the flag state and the citation to be present. |
| T-12-03-02 | Information Disclosure (historical record) | VALIDATION.md body sections | mitigate | Acceptance criteria require pre-existing section headings to be preserved — heading counts must strictly increase, never decrease. |
| T-12-03-03 | Integrity | new 07-VALIDATION.md | mitigate | Anti-fabrication clause in the action plus acceptance criterion requiring test commands to reference real files under `tests/` prevents the new file from inventing phantom test paths. |
</threat_model>

<verification>
- [ ] `grep -q "^nyquist_compliant: true$" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md`
- [ ] `grep -q "^nyquist_compliant: true$" .planning/phases/09-full-corpus-added-characteristic-closure/09-VALIDATION.md`
- [ ] `grep -q "^nyquist_compliant: true$" .planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VALIDATION.md`
- [ ] `test -s .planning/phases/07-regression-tests-and-verification/07-VALIDATION.md && grep -q "^nyquist_compliant: true$" .planning/phases/07-regression-tests-and-verification/07-VALIDATION.md`
- [ ] `! grep -lE "^nyquist_compliant: false$" .planning/phases/0{6,9}-*/0{6,9}-VALIDATION.md .planning/phases/10-*/10-VALIDATION.md 2>/dev/null`
</verification>

<success_criteria>

- Phases 6, 9, 10 VALIDATION.md files all show `nyquist_compliant: true`, each with a visible reconciliation section citing the backing VERIFICATION.md.
- Phase 7 has a new 07-VALIDATION.md whose schema matches sibling validation docs.
- No VALIDATION.md body sections were removed; all reconciliation edits are additive.
- No source code, test files, REQUIREMENTS.md, or ROADMAP.md rows are touched by this plan.

</success_criteria>
