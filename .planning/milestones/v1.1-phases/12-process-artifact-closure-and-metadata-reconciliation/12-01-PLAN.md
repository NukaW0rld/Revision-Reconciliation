---
phase: 12-process-artifact-closure-and-metadata-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md
  - .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 8 has a substantive 08-VERIFICATION.md that traces each Phase 8 success criterion and the restored GDT-01/02/03 traceability to concrete evidence (04-VERIFICATION.md, plan summaries, and commits), so the milestone re-audit no longer sees Phase 8 as unverified."
    - "Phase 11 has a substantive 11-VERIFICATION.md that traces each Phase 11 success criterion to concrete evidence (tests/test_web_run_review_e2e.py, plan summaries, review fix, and commits), so the milestone re-audit no longer sees Phase 11 as unverified."
    - "Both new VERIFICATION.md files use the existing phase-VERIFICATION frontmatter schema (`phase`, `verified`, `status`, `score`, `overrides_applied`, `re_verification`) and declare `status: passed` only when the referenced evidence actually supports it."
  artifacts:
    - path: ".planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md"
      provides: "Phase 8 verification record that reconciles GDT-01/02/03 traceability via the 04-VERIFICATION.md restoration"
      contains: "status: passed"
    - path: ".planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md"
      provides: "Phase 11 verification record that ties the live-run E2E proof to the Phase 11 success criteria"
      contains: "tests/test_web_run_review_e2e.py"
  key_links:
    - from: ".planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md"
      to: ".planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md"
      via: "GDT-01/02/03 evidence references"
      pattern: "04-VERIFICATION.md"
    - from: ".planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md"
      to: "tests/test_web_run_review_e2e.py"
      via: "E2E proof artifact reference"
      pattern: "test_web_run_review_e2e.py"
---

<objective>
Close the two "unverified_phases" blockers flagged by the v1.1 milestone audit by
writing a dedicated VERIFICATION.md for Phase 8 and Phase 11 that documents the
already-completed work against concrete evidence.

Purpose: the v1.1 audit explicitly lists both phases as LOW functional impact but
MISSING process artifact. This plan creates those two artifacts so the milestone
re-audit stops counting them as gaps. No source code is modified.
</objective>

<execution_context>
@$HOME/.codeium/windsurf/get-shit-done/workflows/execute-plan.md
@$HOME/.codeium/windsurf/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.1-MILESTONE-AUDIT.md
@.planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md
@.planning/phases/08-gd-t-verification-recovery/08-01-SUMMARY.md
@.planning/phases/08-gd-t-verification-recovery/08-02-SUMMARY.md
@.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VERIFICATION.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-01-PLAN.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-01-SUMMARY.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-02-PLAN.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-02-SUMMARY.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-REVIEW-FIX.md
@tests/test_web_run_review_e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 08-VERIFICATION.md documenting Phase 8 success criteria against restored GD&T evidence</name>
  <files>
.planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md
  </files>
  <read_first>
.planning/ROADMAP.md,
.planning/REQUIREMENTS.md,
.planning/milestones/v1.1-MILESTONE-AUDIT.md,
.planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md,
.planning/phases/08-gd-t-verification-recovery/08-01-SUMMARY.md,
.planning/phases/08-gd-t-verification-recovery/08-02-SUMMARY.md,
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VERIFICATION.md
  </read_first>
  <action>
Create `.planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md` using the
same frontmatter+section structure as `10-VERIFICATION.md`. Concretely:

1. Frontmatter (YAML):
   - `phase: 08-gd-t-verification-recovery`
   - `verified: <today UTC ISO8601 timestamp>` (use the same timestamp format as
     `10-VERIFICATION.md`, e.g. `2026-04-19T00:00:00Z`)
   - `status: passed`
   - `score: 3/3 roadmap success criteria verified` (Phase 8 has 3 roadmap success
     criteria in ROADMAP.md — confirm by re-reading the Phase 8 section before
     writing)
   - `overrides_applied: 0`
   - `re_verification: false`

2. Body sections (required, in this order):
   - H1: `# Phase 8: GD&T Verification Recovery — Verification Report`
   - `**Phase Goal:**` block copied verbatim from ROADMAP.md Phase 8
   - `**Verified:**` + today's date
   - `**Status:** PASSED`
   - `**Re-verification:** No — initial verification`
   - `## Goal Achievement` section with an `### Observable Truths` subsection
   - `#### ROADMAP Success Criteria` table with columns
     `| # | Truth | Status | Evidence |` — one row per Phase 8 success criterion.
     For each row, Evidence MUST cite at least one of:
     - `.planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md` (restored chain)
     - `08-01-SUMMARY.md` / `08-02-SUMMARY.md`
     - Concrete commit hashes from the v1.1 audit
       (`c82ad97`, `f18a2ed`, `8c1da64`, `6d79277`, `955ff1f`) — copy only those
       that are confirmed by the Phase 8 SUMMARY.md files; do NOT invent commits
   - `### Required Artifacts` table with columns
     `| Artifact | Expected | Status | Details |` listing:
     - `04-VERIFICATION.md` — Expected: substantive GDT-01/02/03 verification;
       Status: VERIFIED; Details: quote the file header + score line.
     - `08-01-SUMMARY.md` and `08-02-SUMMARY.md` — Expected: present; Status:
       VERIFIED; Details: one-line summary of each.
   - `### Key Link Verification` table showing:
     `From: Phase 8 plans -> To: 04-VERIFICATION.md -> Via: GDT-01/02/03 evidence
     references -> Status: WIRED`
   - `## Requirement Traceability` table: `| REQ-ID | Phase | Status | Evidence |`
     with one row each for GDT-01, GDT-02, GDT-03 — Evidence points at
     `04-VERIFICATION.md`.
   - `## Conclusion`: one paragraph confirming Phase 8's recovery goal met and
     naming the audit gap this verification closes
     (v1.1-MILESTONE-AUDIT.md "unverified_phases -> Phase 8").

3. Anti-fabrication rule: every evidence string MUST be a literal citation from
   files that you read in `<read_first>`. If a claimed commit or line does not
   actually appear in the read files, drop that row rather than inventing it.

4. Language: mirror the tone and formatting of `10-VERIFICATION.md`. Tables only
   — no prose bullet lists inside the Observable Truths sections.
  </action>
  <verify>
    <automated>test -s .planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md &amp;&amp; grep -q "^status: passed$" .planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md &amp;&amp; grep -q "04-VERIFICATION.md" .planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md &amp;&amp; grep -Eq "GDT-01.*GDT-02.*GDT-03|GDT-0[123]" .planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md` exists and is non-empty.
    - Frontmatter contains `phase: 08-gd-t-verification-recovery`, `status: passed`, and `re_verification: false`.
    - Body contains the headings `# Phase 8: GD&T Verification Recovery — Verification Report`, `## Goal Achievement`, `### Required Artifacts`, `### Key Link Verification`, `## Requirement Traceability`, and `## Conclusion`.
    - The Requirement Traceability table contains one row each matching the literal strings `GDT-01`, `GDT-02`, `GDT-03`.
    - At least one evidence cell contains the substring `04-VERIFICATION.md`.
    - At least one evidence cell contains the substring `08-01-SUMMARY.md` and at least one contains `08-02-SUMMARY.md`.
    - `grep -c "| VERIFIED" 08-VERIFICATION.md` returns a value `>= 3` (one per roadmap success criterion).
  </acceptance_criteria>
  <done>Phase 8 has a dedicated verification artifact that reconciles its success criteria and GDT traceability via real evidence, closing the "Phase 8 missing VERIFICATION.md" gap in the v1.1 audit.</done>
</task>

<task type="auto">
  <name>Task 2: Create 11-VERIFICATION.md documenting Phase 11 success criteria against the live-run E2E test file</name>
  <files>
.planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md
  </files>
  <read_first>
.planning/ROADMAP.md,
.planning/milestones/v1.1-MILESTONE-AUDIT.md,
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-VERIFICATION.md,
.planning/phases/11-web-run-to-review-e2e-automation/11-01-PLAN.md,
.planning/phases/11-web-run-to-review-e2e-automation/11-01-SUMMARY.md,
.planning/phases/11-web-run-to-review-e2e-automation/11-02-PLAN.md,
.planning/phases/11-web-run-to-review-e2e-automation/11-02-SUMMARY.md,
.planning/phases/11-web-run-to-review-e2e-automation/11-REVIEW-FIX.md,
tests/test_web_run_review_e2e.py
  </read_first>
  <action>
Create `.planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md`
using the same structure as `10-VERIFICATION.md`. Concretely:

1. Frontmatter (YAML):
   - `phase: 11-web-run-to-review-e2e-automation`
   - `verified: <today UTC ISO8601 timestamp>`
   - `status: passed`
   - `score: 3/3 roadmap success criteria verified` (Phase 11 has 3 roadmap
     success criteria)
   - `overrides_applied: 0`
   - `re_verification: false`

2. Body sections (required, in this order):
   - H1: `# Phase 11: Web Run-to-Review E2E Automation — Verification Report`
   - `**Phase Goal:**` block copied verbatim from ROADMAP.md Phase 11
   - `**Verified:**` + today's date
   - `**Status:** PASSED`
   - `**Re-verification:** No — initial verification`
   - `## Goal Achievement` section with an `### Observable Truths` subsection
   - `#### ROADMAP Success Criteria` table — one row per Phase 11 success
     criterion. Evidence MUST cite:
     - Exact test function names from `tests/test_web_run_review_e2e.py`
       (`test_live_run_submission_persists_packet_and_loads_review_surfaces`,
       `test_live_run_blocks_signoff_until_debug_queue_is_cleared`)
     - `11-01-SUMMARY.md` / `11-02-SUMMARY.md` / `11-REVIEW-FIX.md`
     - Commit hashes `b70c38a`, `d1d666d`, `1324c81` as listed in the v1.1 audit
       — include only those that the SUMMARY/REVIEW-FIX files confirm
   - `#### Plan 01 Must-Haves` and `#### Plan 02 Must-Haves` tables — extract the
     `must_haves.truths` entries from `11-01-PLAN.md` and `11-02-PLAN.md` frontmatter
     and render each as one row with Status and Evidence. If a PLAN.md must-have
     lacks clear confirming evidence in the SUMMARY/test file, mark
     `Status: PARTIAL` with an honest note instead of `VERIFIED`.
   - `### Required Artifacts` table listing at minimum:
     - `tests/test_web_run_review_e2e.py` — Expected: both test function names
       present; Status: VERIFIED; Details: cite test names and what they assert.
     - `11-01-SUMMARY.md` and `11-02-SUMMARY.md` — Expected: present; Status:
       VERIFIED.
     - `11-REVIEW-FIX.md` — Expected: present; Status: VERIFIED.
   - `### Key Link Verification` table showing the live-flow wiring:
     `/runs/new` -> `shop/tasks.py::run_pipeline_task` -> `delta_packet.json` ->
     `GET /review/{id}` -> blocked sign-off -> all WIRED; cite the test function
     that proves each link.
   - `## Conclusion`: one paragraph confirming Phase 11 E2E coverage is in place
     and naming the audit gap this verification closes
     (v1.1-MILESTONE-AUDIT.md "unverified_phases -> Phase 11").

3. Anti-fabrication rule: every test function name MUST be confirmed by a grep
   against `tests/test_web_run_review_e2e.py`; every commit hash MUST appear in
   the v1.1 audit or a SUMMARY file you read. Do not invent either.
  </action>
  <verify>
    <automated>test -s .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md &amp;&amp; grep -q "^status: passed$" .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md &amp;&amp; grep -q "test_live_run_submission_persists_packet_and_loads_review_surfaces" .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md &amp;&amp; grep -q "test_live_run_blocks_signoff_until_debug_queue_is_cleared" .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md &amp;&amp; grep -q "tests/test_web_run_review_e2e.py" .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md` exists and is non-empty.
    - Frontmatter contains `phase: 11-web-run-to-review-e2e-automation`, `status: passed`, and `re_verification: false`.
    - Body contains the headings `# Phase 11: Web Run-to-Review E2E Automation — Verification Report`, `## Goal Achievement`, `#### Plan 01 Must-Haves`, `#### Plan 02 Must-Haves`, `### Required Artifacts`, `### Key Link Verification`, and `## Conclusion`.
    - Both exact test function names appear in the file: `test_live_run_submission_persists_packet_and_loads_review_surfaces` AND `test_live_run_blocks_signoff_until_debug_queue_is_cleared`.
    - At least one evidence cell contains `11-01-SUMMARY.md` and at least one contains `11-02-SUMMARY.md`.
    - The file references `tests/test_web_run_review_e2e.py` at least twice (artifacts table + at least one evidence cell).
    - `grep -Ec "^\| (VERIFIED|PARTIAL) " 11-VERIFICATION.md` returns a value `>= 3` (one per roadmap success criterion).
  </acceptance_criteria>
  <done>Phase 11 has a dedicated verification artifact that proves the live run-to-review-to-blocked-sign-off path via real test functions and summaries, closing the "Phase 11 missing VERIFICATION.md" gap in the v1.1 audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs -> source tree | This plan modifies only `.planning/phases/**/*-VERIFICATION.md`. No source code, tests, configs, or runtime artifacts are touched. |
| Audit evidence -> new verification docs | New VERIFICATION.md evidence cells must cite real files/commits/test names; fabricated evidence would break the audit trail both tasks are meant to restore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | Audit trail | mitigate | Acceptance criteria require grep-verifiable citation strings (commit hashes, test function names, SUMMARY filenames) that must come from files read in `read_first`, preventing fabricated verification. |
| T-12-01-02 | Repudiation | Phase close-out | mitigate | Each VERIFICATION.md carries explicit frontmatter (`verified`, `status`, `re_verification`) so a later auditor can tell this was a process-closure record, not the original verification pass. |
</threat_model>

<verification>
- [ ] `test -s .planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md && grep -q "^status: passed$" .planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md`
- [ ] `test -s .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md && grep -q "^status: passed$" .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md`
- [ ] `grep -q "test_live_run_submission_persists_packet_and_loads_review_surfaces" .planning/phases/11-web-run-to-review-e2e-automation/11-VERIFICATION.md`
- [ ] `grep -q "04-VERIFICATION.md" .planning/phases/08-gd-t-verification-recovery/08-VERIFICATION.md`
</verification>

<success_criteria>

- `08-VERIFICATION.md` exists, `status: passed`, cites 04-VERIFICATION.md + both Phase 8 plan summaries.
- `11-VERIFICATION.md` exists, `status: passed`, cites both live-run test functions and both Phase 11 plan summaries.
- Neither file contains fabricated commits, file names, or test names — every evidence cell is traceable to files read during this plan.

</success_criteria>
