---
phase: 09-full-corpus-added-characteristic-closure
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - delta_preservation/reconcile/classify.py
  - tests/test_added_detection_phase6.py
autonomous: true
requirements: [ADD-01, ADD-02]
test_ids:
  - tests/test_added_detection_phase6.py::TestPhase9ExemplarFamilies::test_plain_integer_800_detected_when_tolerance_block_is_far_away
  - tests/test_added_detection_phase6.py::TestPhase9ExemplarFamilies::test_plain_integer_in_general_tolerance_block_still_suppressed
must_haves:
  truths:
    - "Plain-integer candidates such as Part 5 truth_index 16 (`800`) that sit in the drawing body are no longer suppressed as 'near-boilerplate' when the candidate itself is outside the title/tolerance zone, even if a boilerplate phrase happens to sit within 120 pt of the span centre."
    - "Plain-integer candidates that are themselves inside the bottom-centre general-tolerance zone, or that are clearly clustered with the tolerance block, remain suppressed so the Phase 6 fragment guardrail is preserved."
    - "The refined filter is content- or zone-aware — not a plain numeric tightening of `120.0` — so it behaves predictably across all 9 parts rather than masking one specific pair."
  artifacts:
    - path: "delta_preservation/reconcile/classify.py"
      provides: "Near-boilerplate suppressor narrowed to use the shared exclusion zone contract"
      contains: "span_is_excluded_for_annotation_search"
    - path: "tests/test_added_detection_phase6.py"
      provides: "Positive and negative regression coverage for the refined filter"
      contains: "test_plain_integer_800_detected_when_tolerance_block_is_far_away"
  key_links:
    - from: "delta_preservation/reconcile/classify.py"
      to: "delta_preservation/reconcile/exclusion.py"
      via: "span_is_excluded_for_annotation_search / is_boilerplate_candidate_text"
      pattern: "shared boilerplate zone contract"
---

<objective>
Close Part 5 truth_index 16 (`800`) by refining the plain-integer
near-boilerplate suppressor in `detect_added_characteristics()` so it fires only
when the candidate span itself sits in (or is directly clustered with) a
boilerplate zone, instead of unconditionally suppressing any plain integer whose
centre is within 120 pt of any span that contains a boilerplate keyword.

Purpose: `09-VERIFICATION.md` flags `800` as a genuine added characteristic that
is currently filtered by the blunt 120 pt proximity check. `UNLESS OTHERWISE
SPECIFIED` sits ~54 pt away from the `800` callout on Part 5, even though the
tolerance block itself is in the bottom-centre notes zone and `800` is not.
The shared `span_is_excluded_for_annotation_search()` helper already owns the
"candidate is in the tolerance/title zone" decision — the fix delegates to that
contract instead of constructing a local proximity loop, keeping the behaviour
shared and consistent with every other exclusion caller.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-CONTEXT.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-02-SUMMARY.md
@delta_preservation/reconcile/classify.py
@delta_preservation/reconcile/exclusion.py
@tests/test_added_detection_phase6.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the 120 pt plain-integer near-boilerplate loop with a zone-aware check</name>
  <files>
delta_preservation/reconcile/classify.py
  </files>
  <read_first>
delta_preservation/reconcile/classify.py,
delta_preservation/reconcile/exclusion.py,
.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-02-SUMMARY.md
  </read_first>
  <action>
In `delta_preservation/reconcile/classify.py`, locate the plain-integer
near-boilerplate guard inside `detect_added_characteristics()` (currently around
lines 1961–1985, the block that starts with the comment `# Suppress plain-integer
candidates that sit near general-tolerance boilerplate`).

Replace the 120 pt proximity loop with the following zone-aware logic that
reuses the shared exclusion contract from `delta_preservation/reconcile/exclusion.py`:

1. Compute the page dimensions once outside the loop using
   `estimate_page_dimensions(revB_spans)` from `delta_preservation.reconcile.exclusion`
   (import if not already imported).
2. For each plain-integer candidate span reaching this guard, suppress it only
   when BOTH of the following are true:
   - `span_is_excluded_for_annotation_search(span, page_width=page_width_est, page_height=page_height_est)`
     returns `True` for the candidate itself (meaning the candidate sits in a
     title-block, revision-table, bottom-centre tolerance zone, or its own text
     is recognised as boilerplate); OR
   - The candidate span is vertically co-linear (centre-y within 10 pt) AND
     horizontally adjacent (centre-to-centre distance ≤ 40 pt) with at least
     one span whose text matches `is_boilerplate_candidate_text(span.text)`.
     This preserves the Phase 6 "same-row companion of a boilerplate phrase"
     guard without the old 120 pt omni-directional sweep.

Concrete replacement code (the exact new block that must be present):

```python
        # Suppress plain-integer candidates that sit in the shared boilerplate
        # zone, or that are direct same-row companions of a boilerplate phrase.
        # The previous 120 pt omni-directional proximity sweep was too broad —
        # it suppressed real added callouts (e.g. Part 5 truth_index 16 "800")
        # merely because "UNLESS OTHERWISE SPECIFIED" happened to sit within
        # 120 pt of the span centre even though the tolerance block itself is
        # confined to the bottom-centre notes zone.  Symbol-bearing and
        # count-bearing spans remain exempt (handled by the outer guard).
        if is_plain_integer_dimension and not fp.symbol_tokens and not fp.count_tokens:
            if span_is_excluded_for_annotation_search(
                span,
                page_width=page_width_est,
                page_height=page_height_est,
            ):
                continue
            sx0, sy0, sx1, sy1 = span.bbox_pdf
            scx, scy = (sx0 + sx1) / 2, (sy0 + sy1) / 2
            _is_same_row_boilerplate_companion = False
            for _other in revB_spans:
                if not is_boilerplate_candidate_text(_other.text):
                    continue
                _ox0, _oy0, _ox1, _oy1 = _other.bbox_pdf
                _ocx, _ocy = (_ox0 + _ox1) / 2, (_oy0 + _oy1) / 2
                if abs(_ocy - scy) <= 10.0 and math.hypot(_ocx - scx, _ocy - scy) <= 40.0:
                    _is_same_row_boilerplate_companion = True
                    break
            if _is_same_row_boilerplate_companion:
                continue
```

Update imports at the top of `classify.py` so `span_is_excluded_for_annotation_search`,
`is_boilerplate_candidate_text`, and `estimate_page_dimensions` are all imported
from `delta_preservation.reconcile.exclusion`. If
`span_is_excluded_for_annotation_search` is already imported (it is used
elsewhere in the file — see line ~437), import the other two from the same
module alongside it; do NOT duplicate the import.

Compute `page_width_est, page_height_est` once near the top of
`detect_added_characteristics()` (wherever `revB_spans` is first available) and
reuse the values. Do NOT add per-call `estimate_page_dimensions(revB_spans)`
invocations inside hot loops.

Remove the `_boilerplate_kws` local tuple and the `120.0` literal from this
block entirely. The new logic must not contain the string `120.0` or the
`_boilerplate_kws` list; the shared contract is the single source of truth.

Do NOT touch any other exclusion caller or the `is_near_matched_span()` guard
directly above this block. Do NOT add part-specific conditionals, a truth-index
exception, or special-case logic for the string `"800"`.
  </action>
  <verify>
    <automated>rg -n "_boilerplate_kws\s*=" delta_preservation/reconcile/classify.py
rg -n "120\.0" delta_preservation/reconcile/classify.py
rg -n "span_is_excluded_for_annotation_search|is_boilerplate_candidate_text|estimate_page_dimensions" delta_preservation/reconcile/classify.py
uv run pytest -q tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "_boilerplate_kws\s*=" delta_preservation/reconcile/classify.py` returns no matches.
    - `rg -n "<=\s*120\.0" delta_preservation/reconcile/classify.py` returns no matches (the 120 pt radius is gone from the plain-integer guard).
    - `rg -n "is_boilerplate_candidate_text\(" delta_preservation/reconcile/classify.py` returns at least one match inside `detect_added_characteristics()` (the same-row companion branch).
    - `rg -n "span_is_excluded_for_annotation_search" delta_preservation/reconcile/classify.py` returns at least one match inside the plain-integer guard region (the zone-aware branch).
    - `rg -n "from delta_preservation\.reconcile\.exclusion import" delta_preservation/reconcile/classify.py` returns a line that imports at least `span_is_excluded_for_annotation_search`, `is_boilerplate_candidate_text`, and `estimate_page_dimensions`.
    - `uv run pytest -q tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x` exits 0 (the 51 existing Phase 6 regression tests still pass).
    - `git diff --name-only -- assets/debug_report_part1.json assets/debug_report_part2.json assets/debug_report_part3.json assets/debug_report_part4.json assets/debug_report_part5.json assets/debug_report_part6.json assets/debug_report_part7.json assets/debug_report_part8.json assets/debug_report_part9.json` produces no output (historical Phase 6 assets untouched).
  </acceptance_criteria>
  <done>The plain-integer near-boilerplate guard now delegates to the shared exclusion zone contract plus a tight same-row companion check, and every Phase 6 regression test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add positive and negative regression tests for the refined filter</name>
  <files>
tests/test_added_detection_phase6.py
  </files>
  <read_first>
tests/test_added_detection_phase6.py,
delta_preservation/reconcile/classify.py,
delta_preservation/reconcile/exclusion.py
  </read_first>
  <action>
Add two new tests to the `TestPhase9ExemplarFamilies` class in
`tests/test_added_detection_phase6.py`:

1. **Positive case — drawing-body plain integer survives.** Define
   `test_plain_integer_800_detected_when_tolerance_block_is_far_away`. The test
   synthesises a Rev B span set containing:
   - A plain-integer span with text `"800"` at a drawing-body bbox whose centre
     is roughly `(615, 581)` on a standard `(1224, 792)` page (i.e. outside the
     bottom-centre tolerance zone which spans 78 %–92 % height × 25 %–72 %
     width).
   - A boilerplate span with text `"UNLESS OTHERWISE SPECIFIED"` whose centre
     sits in the bottom-centre tolerance zone (e.g. around `(500, 650)` so its
     centre is >= 40 pt from the `"800"` centre and not vertically co-linear
     with it within 10 pt).
   - An empty Rev A span list (or a single unrelated Rev A span far away) so the
     `800` span is not classifiable as Rev-A-equivalent.
   Call the added-characteristic detection entry point (`detect_added_characteristics`
   or the wrapping helper already used by this test file) and assert that at
   least one returned candidate has grouped text equal to `"800"` (exact match
   after `.strip()`).

2. **Negative case — same-row companion still suppressed.** Define
   `test_plain_integer_in_general_tolerance_block_still_suppressed`. The test
   synthesises:
   - A plain-integer span with text `"150"` whose centre sits inside the
     bottom-centre tolerance zone (e.g. `(420, 650)` on a `(1224, 792)` page),
     AND a boilerplate span with text `"UNLESS OTHERWISE SPECIFIED"` whose
     centre is within 10 pt in y and 40 pt overall of the `"150"` span centre.
   - Empty Rev A.
   Assert that `detect_added_characteristics` returns no candidate with grouped
   text equal to `"150"`.

Both tests must use the same construction helpers (e.g. `_make_span`,
`detect_added_characteristics`, whatever imports are already at the top of the
file) that the surrounding Phase 9 tests already use. Do NOT introduce a new
fixture harness. Do NOT test behaviour on real part fixtures in this plan —
full-corpus fixture updates are the responsibility of Plan 06.

The literal strings `"800"`, `"150"`, and `"UNLESS OTHERWISE SPECIFIED"` must
appear verbatim in the test source so the grep acceptance checks below succeed.
  </action>
  <verify>
    <automated>rg -n "def test_plain_integer_800_detected_when_tolerance_block_is_far_away|def test_plain_integer_in_general_tolerance_block_still_suppressed" tests/test_added_detection_phase6.py
rg -n "UNLESS OTHERWISE SPECIFIED" tests/test_added_detection_phase6.py
uv run pytest -q tests/test_added_detection_phase6.py -k "plain_integer_800_detected_when_tolerance_block_is_far_away or plain_integer_in_general_tolerance_block_still_suppressed" -x
uv run pytest -q tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def test_plain_integer_800_detected_when_tolerance_block_is_far_away\(" tests/test_added_detection_phase6.py` returns exactly one match.
    - `rg -n "def test_plain_integer_in_general_tolerance_block_still_suppressed\(" tests/test_added_detection_phase6.py` returns exactly one match.
    - `rg -n "UNLESS OTHERWISE SPECIFIED" tests/test_added_detection_phase6.py` returns at least two matches (one per new test).
    - `rg -n "\"800\"" tests/test_added_detection_phase6.py` includes at least one match inside the new positive test.
    - `uv run pytest -q tests/test_added_detection_phase6.py -k "plain_integer_800_detected_when_tolerance_block_is_far_away or plain_integer_in_general_tolerance_block_still_suppressed" -x` exits 0 with 2 passed.
    - `uv run pytest -q tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x` exits 0 (original 51 + new 2 = 53 tests pass; no Phase 6 regressions).
  </acceptance_criteria>
  <done>Two new TestPhase9ExemplarFamilies tests prove the refined filter lets drawing-body plain integers through while keeping same-row tolerance-block companions suppressed, and the full Phase 6/9 detection suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rev B span text -> added-candidate suppression | Plain-integer candidates must survive unless the candidate itself is in a known boilerplate zone or is a same-row companion of a boilerplate phrase. |
| Shared exclusion contract -> added detection | `span_is_excluded_for_annotation_search` is the single source of truth for "is this span in a boilerplate zone"; added detection must consume it rather than duplicate the decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Integrity | plain-integer near-boilerplate guard | mitigate | Delegate the zone decision to `span_is_excluded_for_annotation_search`; keep the new same-row companion check narrow (10 pt y, 40 pt centre-to-centre) and cover both the positive and negative cases with regression tests. |
| T-09-10 | Repudiation | phase-9 corpus reproducibility | mitigate | No part-specific conditionals, no truth-index exceptions, no direct reference to the string `"800"` in production code. Behaviour is fully expressed by shared geometry/text contracts. |
| T-09-11 | Denial of Service | detect_added_characteristics hot loop | mitigate | Page dimensions are estimated once per call, not per candidate; the same-row check scans `revB_spans` with a tight early-exit (`break` on first companion), matching the previous worst-case cost. |
</threat_model>

<verification>
- [ ] `rg -n "_boilerplate_kws\s*=" delta_preservation/reconcile/classify.py` -> no matches
- [ ] `rg -n "<=\s*120\.0" delta_preservation/reconcile/classify.py` -> no matches
- [ ] `rg -n "span_is_excluded_for_annotation_search|is_boilerplate_candidate_text" delta_preservation/reconcile/classify.py` -> both symbols present
- [ ] `uv run pytest -q tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x`
- [ ] `uv run pytest -q tests/test_added_detection_phase6.py -k "plain_integer_800_detected_when_tolerance_block_is_far_away or plain_integer_in_general_tolerance_block_still_suppressed" -x`
- [ ] `git diff --name-only -- assets/debug_report_part*.json` -> no output
</verification>

<success_criteria>

- All tasks completed
- Plain-integer `800` callouts in the drawing body are no longer suppressed when a boilerplate phrase merely sits within 120 pt of the span centre
- Same-row general-tolerance companions remain suppressed (no new false positives)
- The refined behaviour is shared-layer (classify + exclusion), never part-specific
- Phase 6 regression suite stays green; historical Phase 6 assets are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-corpus-added-characteristic-closure/09-04-SUMMARY.md`
</output>
