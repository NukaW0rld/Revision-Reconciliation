---
phase: 08-gd-t-verification-recovery
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md
  - .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GDT-01, GDT-02, GDT-03]
must_haves:
  truths:
    - "`04-VERIFICATION.md` exists and verifies current Phase 04 behavior using current code, current tests, and current evidence rather than only historical summaries."
    - "The stale manual-only Phase 04 validation checks are either closed with current evidence or explicitly re-scoped to the roadmap phase that already owns the remaining gap."
    - "GDT-01, GDT-02, and GDT-03 are no longer orphaned in `.planning/REQUIREMENTS.md` and point at Phase 08 plan coverage."
  artifacts:
    - path: ".planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md"
      provides: "Substantive Phase 04 verification artifact with requirement-level evidence"
      contains: "## Requirements Coverage"
    - path: ".planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md"
      provides: "Phase 08 reconciliation note for stale manual-only checks"
      contains: "## Phase 8 Reconciliation"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated GDT requirement completion and traceability"
      contains: "| GDT-01 | Phase 8 |"
---

<objective>
Rebuild the missing Phase 04 verification chain and restore GDT traceability
without widening the milestone scope.

Purpose: Phase 04 shipped working plans and follow-up review fixes, but it
never produced `04-VERIFICATION.md`. This plan converts current code, current
test, and current fixture evidence into a substantive verification artifact,
reconciles the stale manual-only validation notes, and updates the GDT rows in
`.planning/REQUIREMENTS.md` so the milestone audit no longer treats them as
orphaned.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/08-gd-t-verification-recovery/08-RESEARCH.md
@.planning/phases/08-gd-t-verification-recovery/08-VALIDATION.md
@.planning/phases/04-gd-t-parser-fixes/04-01-SUMMARY.md
@.planning/phases/04-gd-t-parser-fixes/04-02-SUMMARY.md
@.planning/phases/04-gd-t-parser-fixes/04-REVIEW-FIX.md
@.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md
@.planning/phases/04-gd-t-parser-fixes/04-UAT.md
@.planning/phases/04-gd-t-parser-fixes/04-SECURITY.md
@.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
@tests/fixtures/phase7_algorithm_only/part8-debug-report.json
@assets/part8/ground_truth.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the stale Phase 04 manual-only validation notes against current evidence</name>
  <files>.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md</files>
  <read_first>
.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md,
.planning/phases/08-gd-t-verification-recovery/08-RESEARCH.md,
.planning/v1.1-MILESTONE-AUDIT.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
tests/fixtures/phase7_algorithm_only/part8-debug-report.json,
assets/part8/ground_truth.json
  </read_first>
  <action>
Append a new `## Phase 8 Reconciliation` section to
`.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md`.

The new section must do all of the following with concrete text:
- State that the original manual-only validation rows were never closed by a
  Phase 04 verification artifact and are being reconciled in Phase 08.
- Record that the current automated evidence set now includes the Phase 04
  regression suites plus the milestone-level `tests/test_phase7_regression.py`
  GDT cluster checks.
- Explicitly mention the current corpus-symbol discrepancy discovered during
  Phase 08 research: `assets/part8/ground_truth.json` uses `↗` and `⌰`, while
  the current Phase 04 parser/test contract proves `⌿` and `⟃`.
- Make a bounded scope decision in writing:
  - If execution did **not** add `↗` / `⌰` aliases in Plan 08-01, document that
    this alias gap is deferred to Phase 09 / `ADD-01` because it overlaps the
    remaining added-characteristic closure work.
  - If execution **did** add those aliases, document that the manual note is
    closed by the new automated tests and cite the exact test file and test names.

Do not delete the historical manual-only table. Preserve it as historical
context and add the reconciliation note below it.
  </action>
  <verify>
    <automated>rg -n "^## Phase 8 Reconciliation|tests/test_phase7_regression.py|↗|⌰|Phase 09|ADD-01" .planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^## Phase 8 Reconciliation" .planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md` returns one match.
    - `.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md` contains the literals `tests/test_phase7_regression.py`, `↗`, `⌰`, `Phase 09`, and `ADD-01`.
    - The file still contains the original `## Manual-Only Verifications` section — the historical rows were not deleted.
  </acceptance_criteria>
  <done>Phase 04 validation history now has an explicit Phase 8 reconciliation note that preserves the original manual checks while recording the current evidence boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Write a substantive current-state Phase 04 verification report</name>
  <files>.planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md</files>
  <read_first>
.planning/phases/04-gd-t-parser-fixes/04-01-SUMMARY.md,
.planning/phases/04-gd-t-parser-fixes/04-02-SUMMARY.md,
.planning/phases/04-gd-t-parser-fixes/04-REVIEW-FIX.md,
.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md,
.planning/phases/04-gd-t-parser-fixes/04-UAT.md,
.planning/phases/04-gd-t-parser-fixes/04-SECURITY.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
.planning/v1.1-MILESTONE-AUDIT.md,
.planning/REQUIREMENTS.md,
.planning/phases/08-gd-t-verification-recovery/08-RESEARCH.md,
delta_preservation/reconcile/normalize.py,
tests/test_semantic_extraction.py,
tests/test_semantic_comparison.py,
tests/test_semantic_types.py,
tests/test_pipeline_semantic_packet.py,
tests/test_phase7_regression.py
  </read_first>
  <action>
Create `.planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md` as a
substantive verification artifact for Phase 04.

Use the house verification style already established in
`07-VERIFICATION.md`. The new file must include:
- frontmatter with Phase 04 identifier, current verified timestamp, `status`,
  `score`, `overrides_applied`, and `re_verification: true`
- `## Goal Achievement`
- `## Required Artifacts`
- `## Key Link Verification`
- `## Requirements Coverage`
- `## Gaps Summary`
- a clear phase closure statement

Content rules:
- Verify the current Phase 04 contract using current evidence, not only the
  historical summaries.
- Cite `04-REVIEW-FIX.md` explicitly so the verification reflects the
  post-review-fix codebase, not just the original 04-01 / 04-02 summaries.
- Use the current targeted command set as evidence:
  - `uv run pytest -q tests/test_semantic_extraction.py tests/test_semantic_comparison.py tests/test_semantic_types.py tests/test_pipeline_semantic_packet.py -x`
  - `uv run pytest -q tests/test_phase7_regression.py -x`
- Requirement coverage must have separate rows for `GDT-01`, `GDT-02`, and
  `GDT-03`.
- The report must explicitly state whether `↗` / `⌰` alias handling is
  verified in Phase 08 or deferred to Phase 09. Do not imply full support
  unless execution evidence actually added it.

The verification report must be strong enough that a later milestone audit can
point to it directly instead of treating the Phase 04 requirements as orphaned.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md && rg -n "## Goal Achievement|## Required Artifacts|## Key Link Verification|## Requirements Coverage|## Gaps Summary|04-REVIEW-FIX|tests/test_phase7_regression.py|GDT-01|GDT-02|GDT-03" .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md` exits 0.
    - `rg -n "## Goal Achievement|## Required Artifacts|## Key Link Verification|## Requirements Coverage|## Gaps Summary|04-REVIEW-FIX|tests/test_phase7_regression.py|GDT-01|GDT-02|GDT-03" .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md` returns matches for all listed headings/strings.
    - `rg -n "status:\\s*(passed|human_needed)" .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md` returns one match in frontmatter.
  </acceptance_criteria>
  <done>Phase 04 now has a substantive current-state verification artifact that audits can cite directly instead of treating the requirement set as orphaned.</done>
</task>

<task type="auto">
  <name>Task 3: Restore GDT requirement traceability after verification lands</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
.planning/REQUIREMENTS.md,
.planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md,
.planning/v1.1-MILESTONE-AUDIT.md,
.planning/ROADMAP.md
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` only after `04-VERIFICATION.md` exists.

Make these exact traceability updates:
- Change the checklist entries for `GDT-01`, `GDT-02`, and `GDT-03` from
  unchecked to checked (`- [x]`).
- Update the Traceability table rows for `GDT-01`, `GDT-02`, and `GDT-03` so
  the `Plan` column is no longer `—`.
- Use these exact plan mappings:
  - `GDT-01` -> `08-01-PLAN.md, 08-02-PLAN.md`
  - `GDT-02` -> `08-01-PLAN.md, 08-02-PLAN.md`
  - `GDT-03` -> `08-01-PLAN.md, 08-02-PLAN.md`

Leave every non-GDT requirement row unchanged in this plan. Phase 08 is a
targeted orphan-recovery pass, not a global requirements hygiene sweep.
  </action>
  <verify>
    <automated>rg -n "^- \\[x\\] \\*\\*GDT-01\\*\\*|^- \\[x\\] \\*\\*GDT-02\\*\\*|^- \\[x\\] \\*\\*GDT-03\\*\\*|^\\| GDT-01 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$|^\\| GDT-02 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$|^\\| GDT-03 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^- \\[x\\] \\*\\*GDT-01\\*\\*|^- \\[x\\] \\*\\*GDT-02\\*\\*|^- \\[x\\] \\*\\*GDT-03\\*\\*" .planning/REQUIREMENTS.md` returns three matches.
    - `rg -n "^\\| GDT-01 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$|^\\| GDT-02 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$|^\\| GDT-03 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$" .planning/REQUIREMENTS.md` returns three matches.
    - `test -f .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md` exits 0 before this task is considered complete.
  </acceptance_criteria>
  <done>The GDT requirement rows are only marked complete after a real verification artifact exists and now point to the Phase 8 recovery plans for traceability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical Phase 04 docs -> current verification artifact | Old summaries and validation notes must be transformed into a current-state verification report without overstating what the current code proves. |
| Current evidence -> requirements traceability | Requirements must only be marked complete after substantive verification exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I | `04-VALIDATION.md` | mitigate | Preserve historical manual-only notes and add an explicit reconciliation section rather than silently rewriting history. |
| T-08-02 | I | `04-VERIFICATION.md` | mitigate | Require current commands, current files, and `04-REVIEW-FIX.md` citations so verification is evidence-backed. |
| T-08-03 | T | `.planning/REQUIREMENTS.md` | mitigate | Update only the three GDT rows and only after `04-VERIFICATION.md` exists. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_semantic_extraction.py tests/test_semantic_comparison.py tests/test_semantic_types.py tests/test_pipeline_semantic_packet.py tests/test_phase7_regression.py -x`
- [ ] `test -f .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md`
- [ ] `rg -n "## Phase 8 Reconciliation|↗|⌰|Phase 09|ADD-01" .planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md`
- [ ] `rg -n "## Goal Achievement|## Requirements Coverage|04-REVIEW-FIX|tests/test_phase7_regression.py|GDT-01|GDT-02|GDT-03" .planning/phases/04-gd-t-parser-fixes/04-VERIFICATION.md`
- [ ] `rg -n "^- \\[x\\] \\*\\*GDT-01\\*\\*|^- \\[x\\] \\*\\*GDT-02\\*\\*|^- \\[x\\] \\*\\*GDT-03\\*\\*|^\\| GDT-01 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$|^\\| GDT-02 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$|^\\| GDT-03 \\| Phase 8 \\| 08-01-PLAN.md, 08-02-PLAN.md \\|$" .planning/REQUIREMENTS.md`
</verification>

<success_criteria>

- All tasks completed
- Phase 04 has a substantive current-state verification artifact
- Stale manual-only validation notes are reconciled instead of left hanging
- `GDT-01`, `GDT-02`, and `GDT-03` are no longer orphaned in `.planning/REQUIREMENTS.md`
</success_criteria>

<output>
After completion, create `.planning/phases/08-gd-t-verification-recovery/08-02-SUMMARY.md`
</output>
