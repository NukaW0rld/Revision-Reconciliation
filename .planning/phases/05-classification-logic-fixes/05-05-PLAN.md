---
phase: 05-classification-logic-fixes
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - tests/test_classify_phase5_regression.py
autonomous: true
requirements: [CLS-01, CLS-02, CLS-03]
test_ids:
  - tests/test_classify_phase5_regression.py::TestPhase5SnapshotExemplars
  - tests/test_classify_phase5_regression.py::TestPhase5SnapshotSweep
  - tests/test_classify_phase5_regression.py::TestPhase5SyntheticReconciliation
must_haves:
  truths:
    - "The regression harness uses checked-in snapshot rows and explicit exemplar strings, not speculative fixture generation"
    - "CLS-02 gets at least one synthetic packet-level regression in addition to the mechanism tests from Plan 04"
    - "The harness stays read-only with respect to assets and ground_truth.json"
  artifacts:
    - path: "tests/test_classify_phase5_regression.py"
      provides: "Snapshot exemplar guard plus synthetic CLS-02 regression coverage"
      contains: "class TestPhase5SnapshotExemplars"
---

<objective>
Add a Phase 5 regression harness that is stronger than the original count-cap proxy. The review asked for explicit exemplar assertions and a CLS-02 synthetic regression. This plan uses the checked-in `assets/debug_report_part*.json` files for snapshot lookups and keeps the logic deterministic by matching on exact exemplar strings, not on guessed part-level counts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-classification-logic-fixes/05-RESEARCH.md
@.planning/phases/05-classification-logic-fixes/05-REVIEWS.md
@delta_preservation/reconcile/classify.py
@tests/test_classify_bugfixes.py
@assets/debug_report_part1.json
@assets/debug_report_part4.json
@assets/debug_report_part7.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a read-only snapshot harness keyed by exact exemplar strings</name>
  <files>tests/test_classify_phase5_regression.py</files>
  <read_first>delta_preservation/reconcile/classify.py, assets/debug_report_part1.json, assets/debug_report_part4.json, assets/debug_report_part7.json, .planning/phases/05-classification-logic-fixes/05-REVIEWS.md</read_first>
  <action>
Create `tests/test_classify_phase5_regression.py` with three test classes.

`TestPhase5SnapshotExemplars` must:
- assert the required snapshot files exist
- load the snapshot rows by exact `requirement_revB` text
- positively assert:
  - `_looks_like_adjacency_bleed("4 x Ø8 THRU ALL / ⌴ Ø13.5 ↧ 8.5", corresponding requirement_revA)` is True
  - `_looks_like_adjacency_bleed("2X Ø.201 ↧ 0.50 / 1/4-20 UNC - 2B", corresponding requirement_revA)` is True
  - `_looks_like_adjacency_bleed("70 / 30", corresponding requirement_revA)` is False
  - `_ASYMMETRIC_SHAPE_RE` matches `2X 22.0° +0.3° / −0.1°`

`TestPhase5SnapshotSweep` must:
- iterate every snapshot item where `evaluation.status == "conforming"` and `pipeline_classification == "unchanged"`
- run the bleed helper against `(requirement_revB, requirement_revA)`
- assert it is False unless the row's exact `requirement_revB` string is in an explicit allowlist
- run `_ASYMMETRIC_SHAPE_RE` on `requirement_revB`
- assert it is False unless the row's exact `requirement_revB` string is in an explicit asymmetric allowlist

Do not use count caps. Use explicit allowlists of exact strings.
</action>
  <acceptance_criteria>
    - `tests/test_classify_phase5_regression.py` exists and contains `class TestPhase5SnapshotExemplars` and `class TestPhase5SnapshotSweep`.
    - `rg -n "4 x Ø8 THRU ALL / ⌴ Ø13.5 ↧ 8.5|2X Ø\\.201 ↧ 0\\.50 / 1/4-20 UNC - 2B|70 / 30|22\\.0° \\+0\\.3° / −0\\.1°" tests/test_classify_phase5_regression.py` returns the exemplar strings.
    - `pytest tests/test_classify_phase5_regression.py::TestPhase5SnapshotExemplars tests/test_classify_phase5_regression.py::TestPhase5SnapshotSweep -x` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add a synthetic CLS-02 packet-level regression to the same harness</name>
  <files>tests/test_classify_phase5_regression.py</files>
  <read_first>tests/test_classify_phase5_regression.py, delta_preservation/reconcile/classify.py, delta_preservation/reconcile/anchors.py, .planning/phases/05-classification-logic-fixes/05-04-PLAN.md</read_first>
  <action>
Extend the new file with `class TestPhase5SyntheticReconciliation`.

Add at least these two tests:
1. grouped compatible added item near a removed anchor -> `reconcile_removed_added_pairs(...)` returns one `changed` row
2. same geometry but `added_page` differs -> items stay separate

Build the inputs inline using the internal `DeltaItem` dataclass and `Anchor`. Reuse the new internal metadata contract from Plan 04 (`added_requirement_text`, `added_bbox`, `added_page`). This is the packet-level regression the review asked for; it proves the post-pass still behaves correctly once the grouped-item metadata exists.
</action>
  <acceptance_criteria>
    - `tests/test_classify_phase5_regression.py` contains `class TestPhase5SyntheticReconciliation`.
    - `rg -n "added_requirement_text|added_bbox|added_page|reconcile_removed_added_pairs" tests/test_classify_phase5_regression.py` returns matches.
    - `pytest tests/test_classify_phase5_regression.py::TestPhase5SyntheticReconciliation -x` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
- `pytest tests/test_classify_phase5_regression.py -x`
- `python3 - <<'PY'\nfrom pathlib import Path\nfor name in ['debug_report_part1.json', 'debug_report_part4.json', 'debug_report_part7.json']:\n    assert (Path('assets') / name).exists(), name\nprint('ok')\nPY`
</verification>

<success_criteria>
- Phase 5 regression coverage is based on explicit snapshot exemplars and exact-string allowlists, not vague count caps.
- CLS-02 has a packet-level synthetic regression in addition to Plan 04's mechanism tests.
- The harness remains read-only with respect to `assets/` and `ground_truth.json`.
</success_criteria>
