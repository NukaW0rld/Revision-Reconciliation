---
phase: 01-evaluation-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - delta_preservation/evaluation/snippet_rules.py
  - delta_preservation/evaluation/conformance.py
  - delta_preservation/types.py
  - delta_preservation/cli.py
  - shop/services/review.py
  - tests/test_snippet_evaluation.py
  - tests/test_debug_internals.py
autonomous: true
requirements: [EVAL-03, EVAL-04, EVAL-05]
user_setup: []
must_haves:
  truths:
    - "A packet row becomes `conforming` only when classification, requirement, and snippet checks all pass under deterministic truth rules."
    - "Snippet evaluation accepts visually valid context using explicit bbox math instead of exact center equality."
    - "Nonconforming rows retain ordered machine-readable mismatch entries that downstream review helpers can read without manual debug verdict data."
  artifacts:
    - path: "delta_preservation/evaluation/snippet_rules.py"
      provides: "Deterministic snippet tolerance checks for single and grouped callouts"
      min_lines: 80
      contains: "def evaluate_snippet_evidence("
    - path: "delta_preservation/evaluation/conformance.py"
      provides: "Final per-row conforming vs review_needed verdicts"
      contains: "snippet_conforms"
    - path: "shop/services/review.py"
      provides: "Downstream access to evaluation and mismatch data"
      contains: "\"evaluation\""
  key_links:
    - from: "delta_preservation/evaluation/conformance.py"
      to: "delta_preservation/evaluation/snippet_rules.py"
      via: "evaluate_snippet_evidence for revA/revB centers"
      pattern: "evaluate_snippet_evidence"
    - from: "shop/services/review.py"
      to: "delta_packet.json"
      via: "raw evaluation envelope copied into debug internals or export payload"
      pattern: "\"evaluation\""
---

<objective>
Complete the Phase 1 evaluator with deterministic snippet tolerance and ordered mismatch output.

Purpose: The phase goal is only achieved when classification, requirement, and snippet evidence together produce stable `conforming` vs `review_needed` results that downstream review/export code can consume.
Output: snippet-tolerance utilities, final evaluator verdicts, and additive downstream access to machine-readable mismatch entries.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evaluation-foundation/01-CONTEXT.md
@.planning/phases/01-evaluation-foundation/01-RESEARCH.md
@delta_preservation/cli.py
@delta_preservation/evaluation/conformance.py
@delta_preservation/vision/bbox_utils.py
@shop/services/review.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic snippet tolerance and final evaluator verdicts</name>
  <files>delta_preservation/evaluation/snippet_rules.py, delta_preservation/evaluation/conformance.py, delta_preservation/types.py, delta_preservation/cli.py, tests/test_snippet_evaluation.py</files>
  <read_first>delta_preservation/evaluation/conformance.py, delta_preservation/vision/bbox_utils.py, delta_preservation/cli.py, .planning/phases/01-evaluation-foundation/01-CONTEXT.md</read_first>
  <action>Per D-14, D-15, D-16, D-17, and D-18, create `delta_preservation/evaluation/snippet_rules.py` with `evaluate_snippet_evidence(...)` implementing two explicit rules. Single-callout rule: contract each evidence bbox edge by `max(12.0, 0.10 * min(width, height))` PDF points, then require the truth center to fall inside the contracted box and at least `6.0` PDF points from every surviving edge. Grouped-callout rule: when the row is flagged as grouped or notes-like, contract the union bbox by `6.0` PDF points only and accept if the truth center is inside that contracted union. Null-truth rule: if the truth center for a side is `null`, that side is optional. Missing-evidence rule: if truth expects a center and the packet has no evidence on that side, emit `snippet_missing_revA` or `snippet_missing_revB`. Integrate the snippet result into `delta_preservation/evaluation/conformance.py` so `ItemEvaluation.status` becomes exactly `conforming` or `review_needed`, `snippet_conforms` is boolean, and final mismatch ordering is `classification`, `requirement`, `snippet`, then `ambiguity`. Add `tests/test_snippet_evaluation.py` covering single-callout pass/fail, grouped-callout pass, null-center exemptions, and missing-evidence mismatches.</action>
  <verify>
    <automated>uv run pytest tests/test_snippet_evaluation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/evaluation/snippet_rules.py` contains `def evaluate_snippet_evidence(`
    - `delta_preservation/evaluation/snippet_rules.py` contains `max(12.0, 0.10 * min(width, height))`
    - `delta_preservation/evaluation/conformance.py` contains `conforming`
    - `tests/test_snippet_evaluation.py` contains `def test_grouped_callout_`
  </acceptance_criteria>
  <done>Snippet tolerance is deterministic, side-aware, and now participates in the final `conforming` vs `review_needed` verdict.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve ordered mismatch details for downstream review consumers</name>
  <files>shop/services/review.py, tests/test_debug_internals.py</files>
  <read_first>shop/services/review.py, tests/test_debug_internals.py, delta_preservation/types.py</read_first>
  <action>Per D-19, D-20, D-21, and D-22, extend `shop/services/review.py` so packet-backed helpers copy the additive evaluation envelope into downstream structures without changing queue shape. Update `debug_internals_by_char()` to include `evaluation` and ordered `mismatches`, and update `assemble_debug_report_payload()` to carry the same packet-side values without depending on reviewer-entered debug verdicts. Keep the existing human-readable `reasons` array intact, but do not collapse multiple mismatch entries into a single dominant reason. Add `tests/test_debug_internals.py` assertions that nonconforming rows expose side-specific snippet mismatch codes and preserve the original mismatch order.</action>
  <verify>
    <automated>uv run pytest tests/test_snippet_evaluation.py tests/test_debug_internals.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `shop/services/review.py` contains `"evaluation":`
    - `shop/services/review.py` contains `"mismatches":`
    - `tests/test_debug_internals.py` contains `snippet_missing_revB`
    - `tests/test_debug_internals.py` contains `mismatches`
  </acceptance_criteria>
  <done>Ordered machine-readable mismatch data is available to downstream review/export helpers while preserving existing packet reasons.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_snippet_evaluation.py -q`
- [ ] `uv run pytest tests/test_snippet_evaluation.py tests/test_debug_internals.py -q`
- [ ] `rg -n "evaluate_snippet_evidence|snippet_missing_revA|snippet_missing_revB|\"evaluation\"|\"mismatches\"" delta_preservation/evaluation/snippet_rules.py delta_preservation/evaluation/conformance.py shop/services/review.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Packet rows resolve to final `conforming` or `review_needed` verdicts
- Ordered mismatch entries are exposed for downstream review/export consumers
</success_criteria>

<output>
After completion, create `.planning/phases/01-evaluation-foundation/01-03-SUMMARY.md`
</output>
