---
phase: 01-evaluation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - delta_preservation/evaluation/__init__.py
  - delta_preservation/evaluation/contracts.py
  - delta_preservation/evaluation/loader.py
  - delta_preservation/cli.py
  - shop/tasks.py
  - tests/test_ground_truth_loader.py
  - tests/test_pipeline_task.py
autonomous: true
requirements: [GTRU-01, GTRU-02, GTRU-03]
user_setup: []
must_haves:
  truths:
    - "A benchmark run resolves `assets/{fixture_key}/ground_truth.json` using the exact fixture key already passed as `part_name` / `Run.part_number` and fails before review when the file is missing or malformed."
    - "`ground_truth.json` remains read-only; Phase 1 code only loads and validates fixture data."
  artifacts:
    - path: "delta_preservation/evaluation/contracts.py"
      provides: "Status-aware ground truth schema models and contract errors"
      min_lines: 60
      contains: "class GroundTruthPacket"
    - path: "delta_preservation/evaluation/loader.py"
      provides: "Strict fixture resolution and ground truth loading"
      min_lines: 50
      contains: "def load_ground_truth_packet("
    - path: "shop/tasks.py"
      provides: "Run failure handling for truth contract errors"
      contains: "Ground truth evaluation"
  key_links:
    - from: "shop/tasks.py"
      to: "delta_preservation/evaluation/loader.py"
      via: "load_ground_truth_packet before successful completion"
      pattern: "load_ground_truth_packet"
    - from: "delta_preservation/cli.py"
      to: "DeltaPacket.inputs"
      via: "truth_fixture_key persisted into packet inputs"
      pattern: "truth_fixture_key"
---

<objective>
Create the strict ground-truth contract and loader that every benchmark run uses.

Purpose: Phase 1 cannot evaluate anything deterministically until the correct immutable truth fixture is loaded and validated with clear failure behavior.
Output: exact fixture lookup, status-aware truth validation, and run/task wiring that fails loudly on missing or malformed truth files.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evaluation-foundation/01-CONTEXT.md
@.planning/phases/01-evaluation-foundation/01-RESEARCH.md
@assets/part1/ground_truth.json
@assets/part5_NIST_CTC1/ground_truth.json
@delta_preservation/cli.py
@shop/tasks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create strict truth-fixture contracts and loader</name>
  <files>delta_preservation/evaluation/__init__.py, delta_preservation/evaluation/contracts.py, delta_preservation/evaluation/loader.py, tests/test_ground_truth_loader.py</files>
  <read_first>assets/part1/ground_truth.json, assets/part5_NIST_CTC1/ground_truth.json, .planning/phases/01-evaluation-foundation/01-CONTEXT.md, delta_preservation/types.py</read_first>
  <action>Per D-01, D-02, D-03, D-06, D-07, and D-08, create `GroundTruthContractError`, `GroundTruthCharacteristic`, and `GroundTruthPacket` Pydantic models in `delta_preservation/evaluation/contracts.py` and expose them from `delta_preservation/evaluation/__init__.py`. Enforce these concrete rules: `classification` must be one of `unchanged`, `changed`, `removed`, or `added`; non-`added` rows require `char_no`; `requirement_revB` may be `null` only when `classification == "removed"`; `snippet_center_revA` may be `null` for `added`; `snippet_center_revB` may be `null` for `removed`; structurally valid coverage mismatches are allowed to flow into evaluation instead of raising. In `delta_preservation/evaluation/loader.py`, add `load_ground_truth_packet(truth_fixture_key: str, repo_root: Path | None = None)` that resolves only `assets/{truth_fixture_key}/ground_truth.json`, performs exact directory lookup with no fuzzy fallback, and raises `GroundTruthContractError` with path-specific messages when the directory, file, or JSON structure is invalid. Add `tests/test_ground_truth_loader.py` covering missing fixture directories, malformed JSON, status-aware nullable fields, and canonical added rows without `char_no`.</action>
  <verify>
    <automated>uv run pytest tests/test_ground_truth_loader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/evaluation/contracts.py` contains `class GroundTruthPacket`
    - `delta_preservation/evaluation/loader.py` contains `def load_ground_truth_packet(`
    - `delta_preservation/evaluation/loader.py` contains `assets/{truth_fixture_key}/ground_truth.json`
    - `tests/test_ground_truth_loader.py` contains `def test_missing_fixture_directory_`
  </acceptance_criteria>
  <done>Exact-key fixture loading works and invalid truth files raise deterministic contract errors without mutating fixture data.</done>
</task>

<task type="auto">
  <name>Task 2: Wire truth loading into pipeline and run failure handling</name>
  <files>delta_preservation/cli.py, shop/tasks.py, tests/test_pipeline_task.py</files>
  <read_first>delta_preservation/cli.py, shop/tasks.py, delta_preservation/evaluation/loader.py, tests/test_pipeline_task.py</read_first>
  <action>Per D-01, D-02, and D-03, call `load_ground_truth_packet(part_name)` inside `delta_preservation/cli.py` before packet serialization, and persist the exact fixture key in `DeltaPacket.inputs["truth_fixture_key"]`. In `shop/tasks.py`, catch `GroundTruthContractError` explicitly and set `run.status = "failed"`, `run.failure_stage = "Ground truth evaluation"`, and `run.failure_message` to the loader error so benchmarked runs fail clearly when truth data is missing or malformed. Do not add any code path that writes back to `ground_truth.json`; the only allowed access in this plan is read/validate. Extend `tests/test_pipeline_task.py` with task-level failure cases for missing fixtures and malformed truth files so the failure state is observable through the existing Huey task wrapper.</action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_task.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/cli.py` contains `truth_fixture_key`
    - `delta_preservation/cli.py` contains `load_ground_truth_packet(part_name)`
    - `shop/tasks.py` contains `Ground truth evaluation`
    - `shop/tasks.py` contains `GroundTruthContractError`
  </acceptance_criteria>
  <done>Runs fail with explicit truth-fixture errors before entering review, and successful packets retain the exact truth fixture key that was evaluated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_ground_truth_loader.py -q`
- [ ] `uv run pytest tests/test_pipeline_task.py -q`
- [ ] `rg -n "truth_fixture_key|Ground truth evaluation|GroundTruthContractError" delta_preservation/cli.py shop/tasks.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Exact-key truth loading and contract validation are wired into the run flow
- Ground truth stays read-only
</success_criteria>

<output>
After completion, create `.planning/phases/01-evaluation-foundation/01-01-SUMMARY.md`
</output>
