---
phase: 01-evaluation-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - delta_preservation/evaluation/conformance.py
  - delta_preservation/types.py
  - delta_preservation/cli.py
  - tests/test_ground_truth_evaluation.py
  - tests/test_output_formatting.py
autonomous: true
requirements: [EVAL-01, EVAL-02]
user_setup: []
must_haves:
  truths:
    - "Each packet row records whether its canonical classification matches the corresponding ground-truth expectation."
    - "Each packet row records whether its Rev B requirement conforms using semantic equivalence first and normalized-text fallback second."
    - "Canonical added rows are matched from an unordered truth pool instead of by packet ordering."
  artifacts:
    - path: "delta_preservation/evaluation/conformance.py"
      provides: "Classification and requirement comparison against canonical truth"
      min_lines: 120
      contains: "def evaluate_packet_against_truth("
    - path: "delta_preservation/types.py"
      provides: "Evaluation envelope attached to DeltaItem rows"
      contains: "class ItemEvaluation"
    - path: "tests/test_ground_truth_evaluation.py"
      provides: "Evaluator regression coverage for classification and requirement matching"
      contains: "def test_added_truth_rows_are_matched_as_unordered_pool"
  key_links:
    - from: "delta_preservation/cli.py"
      to: "delta_preservation/evaluation/conformance.py"
      via: "evaluate_packet_against_truth after packet rows are assembled"
      pattern: "evaluate_packet_against_truth"
    - from: "delta_preservation/evaluation/conformance.py"
      to: "delta_preservation/types.py"
      via: "ItemEvaluation and EvaluationMismatch serialization"
      pattern: "ItemEvaluation|EvaluationMismatch"
---

<objective>
Implement automatic classification and requirement conformance against canonical truth.

Purpose: Phase 1 needs deterministic row-by-row comparison before snippet tolerance can decide final auto-pass vs review-needed.
Output: evaluator models plus pipeline wiring that records classification and requirement comparison results on every packet row.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evaluation-foundation/01-CONTEXT.md
@.planning/phases/01-evaluation-foundation/01-RESEARCH.md
@delta_preservation/reconcile/semantic_compare.py
@delta_preservation/reconcile/normalize.py
@delta_preservation/cli.py
@delta_preservation/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add evaluation models and truth-to-packet comparison logic</name>
  <files>delta_preservation/evaluation/conformance.py, delta_preservation/types.py, tests/test_ground_truth_evaluation.py</files>
  <read_first>delta_preservation/types.py, delta_preservation/reconcile/semantic_compare.py, delta_preservation/reconcile/normalize.py, .planning/phases/01-evaluation-foundation/01-CONTEXT.md</read_first>
  <action>Per D-04, D-05, D-08, D-09, D-10, D-11, D-12, and D-13, add `EvaluationMismatch`, `GroundTruthMatch`, and `ItemEvaluation` models to `delta_preservation/types.py` with concrete fields `status`, `matched_truth_char_no`, `classification_conforms`, `requirement_conforms`, `snippet_conforms`, and ordered `mismatches`. In `delta_preservation/evaluation/conformance.py`, implement `select_truth_row_for_item(...)` and `evaluate_item_against_truth(...)` using these rules: non-`added` rows match by `char_no`; canonical `added` rows are matched as an unordered pool using normalized requirement text and reserved snippet comparison slots; requirement comparison uses `compare_semantic_callouts()` when both sides are parsed and comparable, otherwise falls back to `parse_requirement(...).norm_text`; missing semantic parsing alone does not fail conformance; missing usable requirement evidence when truth expects a non-null `requirement_revB` emits a `requirement_missing` mismatch and blocks auto-pass. Add `tests/test_ground_truth_evaluation.py` for unchanged, changed, removed, added, semantic-equivalent, fallback-equivalent, and missing-requirement cases.</action>
  <verify>
    <automated>uv run pytest tests/test_ground_truth_evaluation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/types.py` contains `class ItemEvaluation`
    - `delta_preservation/types.py` contains `class EvaluationMismatch`
    - `delta_preservation/evaluation/conformance.py` contains `def evaluate_packet_against_truth(`
    - `tests/test_ground_truth_evaluation.py` contains `def test_added_truth_rows_are_matched_as_unordered_pool`
  </acceptance_criteria>
  <done>Packet-side evaluation models exist and evaluator logic can compare classification and requirement evidence against canonical truth without relying on snippet rules yet.</done>
</task>

<task type="auto">
  <name>Task 2: Persist classification and requirement evaluation into delta packets</name>
  <files>delta_preservation/cli.py, tests/test_output_formatting.py</files>
  <read_first>delta_preservation/cli.py, delta_preservation/evaluation/conformance.py, tests/test_output_formatting.py</read_first>
  <action>Per D-04, D-05, D-08, D-09, D-10, D-11, D-12, and D-13, call `evaluate_packet_against_truth(...)` in `delta_preservation/cli.py` after `delta_items_pydantic` are assembled and before `DeltaPacket` is serialized. Write the returned `ItemEvaluation` object into each `DeltaItem.evaluation`, preserve packet ordering, and include `matched_truth_char_no` or an added-pool match token for canonical added rows. Use mismatch codes `classification_mismatch`, `requirement_missing`, `requirement_mismatch`, and `truth_ambiguity` in deterministic order, and leave `snippet_conforms` as `null` until Plan 03. Extend `tests/test_output_formatting.py` so serialized packet rows contain the `evaluation` object and the exact mismatch codes for classification and requirement failures.</action>
  <verify>
    <automated>uv run pytest tests/test_ground_truth_evaluation.py tests/test_output_formatting.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/cli.py` contains `evaluate_packet_against_truth`
    - `delta_preservation/cli.py` contains `evaluation=`
    - `tests/test_output_formatting.py` contains `\"evaluation\"`
    - `tests/test_output_formatting.py` contains `classification_mismatch`
  </acceptance_criteria>
  <done>Serialized packet rows now carry canonical classification and requirement comparison results that later Phase 1 work can finish with snippet tolerance.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_ground_truth_evaluation.py -q`
- [ ] `uv run pytest tests/test_ground_truth_evaluation.py tests/test_output_formatting.py -q`
- [ ] `rg -n "ItemEvaluation|EvaluationMismatch|evaluate_packet_against_truth|classification_mismatch" delta_preservation/types.py delta_preservation/evaluation/conformance.py delta_preservation/cli.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Every packet row now carries truth-backed classification and requirement comparison results
- Added truth rows are matched without relying on packet ordering
</success_criteria>

<output>
After completion, create `.planning/phases/01-evaluation-foundation/01-02-SUMMARY.md`
</output>
