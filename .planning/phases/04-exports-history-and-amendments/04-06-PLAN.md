---
phase: 04-exports-history-and-amendments
plan: "06"
type: execute
wave: 4
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
  - "04-04"
  - "04-05"
files_modified:
  - tests/test_exports.py
  - tests/test_history.py
  - tests/test_amendments.py
autonomous: false
requirements:
  - PACKET-01
  - PACKET-02
  - PACKET-03
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - HISTORY-01
  - HISTORY-02
  - HISTORY-03
  - HISTORY-04
  - AMEND-01
  - AMEND-02
  - AMEND-03

must_haves:
  truths:
    - "pytest -q shows 0 failed tests and 0 xfail in test_exports, test_history, test_amendments"
    - "Full test suite (pytest -q) is green"
    - "Docker image builds successfully with WeasyPrint and Pango deps"
    - "In the running Docker container, the complete sign-off + download + amendment flow works end-to-end"
  artifacts:
    - path: "tests/test_exports.py"
      provides: "all 7 tests passing (no xfail)"
    - path: "tests/test_history.py"
      provides: "all 4 tests passing (no xfail)"
    - path: "tests/test_amendments.py"
      provides: "all 3 tests passing (no xfail)"
  key_links:
    - from: "pytest -q"
      to: "all tests"
      via: "full suite run"
      pattern: "0 failed"
---

<objective>
Phase 4 gate: fix any remaining test failures, run the full test suite to green, build Docker with WeasyPrint, and do a human-verified end-to-end flow covering all four feature areas.

Purpose: Confirms the entire phase is production-ready before the milestone closes.
Output: Green test suite, passing Docker build, human-verified e2e flow.
</objective>

<execution_context>
@/home/khoa2/.claude/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-exports-history-and-amendments/04-01-SUMMARY.md
@.planning/phases/04-exports-history-and-amendments/04-02-SUMMARY.md
@.planning/phases/04-exports-history-and-amendments/04-03-SUMMARY.md
@.planning/phases/04-exports-history-and-amendments/04-04-SUMMARY.md
@.planning/phases/04-exports-history-and-amendments/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite — fix any failures and verify all Phase 4 tests pass</name>
  <files>tests/test_exports.py, tests/test_history.py, tests/test_amendments.py</files>
  <action>
Run the full test suite and fix any remaining failures:
```bash
cd /home/khoa2/delta-preservation && pytest -q 2>&1
```

For each failure:
1. Read the traceback to identify the failing assertion or import error
2. Fix the minimal code needed — do not rewrite entire tests
3. Common failure patterns to look for:
   - `conftest` fixture shape mismatch (e.g., `client.app.dependency_overrides.get(get_db)` returns different type)
   - WeasyPrint import failing in test environment (mock or skip PDF tests if system libs unavailable)
   - `run.packet_versions` being `None` vs `[]` in SQLAlchemy JSON column
   - `db.get(Run, run_id)` vs `db.query(Run).filter(Run.id == run_id).first()` — use whichever pattern matches the SQLAlchemy version in use
   - Missing `run_id` in TemplateResponse context for review queue (established pattern from Phase 3)

Fix approach for WeasyPrint in tests: If WeasyPrint system libraries are not installed in the test environment (WSL), wrap WeasyPrint calls in a try/except or add `pytest.importorskip("weasyprint")` at the top of tests that call render_audit_packet_pdf.

After all fixes: `pytest -q` must show 0 failures, 0 errors. xfail is acceptable only if a test was intentionally marked xfail for system-lib-dependent behavior.

Verify Phase 4 test counts:
- `pytest tests/test_exports.py -v` → 7 tests (3 audit packet + 4 work order), all pass or skip
- `pytest tests/test_history.py -v` → 4 tests, all pass
- `pytest tests/test_amendments.py -v` → 3 tests, all pass
  </action>
  <verify>
    <automated>cd /home/khoa2/delta-preservation && pytest -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest -q exits 0 with no failures; all Phase 4 tests visible and passing (or skip-not-xfail for system-lib tests).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4 complete implementation:
- Audit packet PDF (cover page + summary table + per-item detail cards) and CSV download from signed-off run status page
- Partial FAI work order PDF and CSV listing only changed/added characteristics with RE-MEASURE/NEW labels
- Run history date filter on /runs list
- Signed-off run review queue in read-only mode
- Admin retention settings panel and Huey daily cleanup task
- Amendment flow: Amend button -> modal -> new review run pre-filled with parent decisions -> sign-off produces v2 packet
- Versioned packet list on run status page
  </what-built>
  <how-to-verify>
Build and start Docker:
```bash
cd /home/khoa2/delta-preservation/docker && docker compose up --build
```

Verify build succeeds (WeasyPrint + Pango system packages install without error).

After container is up, open http://localhost:8000 and verify:

1. **Audit packet download (PACKET-01..03):**
   - Log in, open a signed-off run's status page
   - Click "Download Audit Packet (PDF)" — verify PDF downloads with cover page, summary table, and item detail cards
   - Click "Download Audit Packet (CSV)" — verify CSV downloads with correct column headers
   - Refresh the page and re-download — verify same file is served (no re-generation needed)

2. **Work order (WORK-01..04):**
   - On the same signed-off run status page, click "Generate Work Order (PDF)" — verify PDF downloads with RE-MEASURE/NEW labels
   - Click "Generate Work Order (CSV)" — verify CSV has correct rows (changed and added only)
   - Confirm unchanged characteristics are NOT in the work order

3. **Run history (HISTORY-01..02):**
   - Go to /runs, enter a date in "From Date" field, click Filter — verify only runs from that date appear
   - Clear filter, click "View" on a signed-off run, then navigate to /review/{id} — verify read-only banner is shown and approve/override buttons are hidden

4. **Retention settings (HISTORY-04):**
   - Go to /admin/settings — verify "Run Retention" section is present with days input
   - Change the days value and save — verify it persists on page reload

5. **Amendment flow (AMEND-01..03):**
   - On a signed-off run status page, click "Amend" — verify confirmation modal appears with immutability statement
   - Click "Create Amendment" — verify redirect to the amendment's review queue with amendment banner
   - Change one item's decision (override instead of approve)
   - Sign off on the amendment — verify redirect to status page showing "v1 (original)" and "v2 (amendment)" in versioned packet list
   - Click v1 link — verify original PDF downloads unchanged
   - Click v2 link — verify amendment PDF downloads

Type "approved" when all flows work, or describe any issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full test suite green + human Docker e2e approval.
</verification>

<success_criteria>
- pytest -q: 0 failures, 0 errors
- Docker image builds with WeasyPrint Pango libraries
- All 5 e2e flows verified by human: audit packet PDF/CSV, work order PDF/CSV, history filter, read-only queue, amendment with versioned packets
- Phase 4 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-exports-history-and-amendments/04-06-SUMMARY.md`
</output>
