---
phase: 02-pipeline-bridge
plan: 08
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
  - 02-06
  - 02-07
files_modified: []
autonomous: false
requirements:
  - UPLOAD-01
  - UPLOAD-02
  - UPLOAD-03
  - UPLOAD-04
  - UPLOAD-05
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-06
  - PIPE-11
must_haves:
  truths:
    - "All tests in tests/test_runs.py pass (no xfail, no error)"
    - "Full test suite is green"
    - "Docker container starts successfully with docker compose up"
    - "Engineer can submit a run end-to-end in Docker and see stage progress"
    - "Failed run shows correct error state; warning run shows correct warning UI"
    - "Alert bell badge reflects unread alert count after run failure"
  artifacts:
    - path: "tests/test_runs.py"
      provides: "Implemented test suite for all Phase 2 requirements"
  key_links: []
---

<objective>
Implement remaining test stubs, run the full test suite, and perform human Docker verification of the Phase 2 pipeline bridge.

Purpose: This is the Phase 2 gate plan — automated suite must be green and a human must verify the end-to-end submission flow in Docker before Phase 3 begins.
Output: All test_runs.py stubs implemented and passing, full test suite green, Docker e2e verification approved by engineer.
</objective>

<execution_context>
@/home/khoa2/.claude/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pipeline-bridge/02-CONTEXT.md
@.planning/phases/02-pipeline-bridge/02-01-SUMMARY.md
@.planning/phases/02-pipeline-bridge/02-02-SUMMARY.md
@.planning/phases/02-pipeline-bridge/02-03-SUMMARY.md
@.planning/phases/02-pipeline-bridge/02-04-SUMMARY.md
@.planning/phases/02-pipeline-bridge/02-05-SUMMARY.md
@.planning/phases/02-pipeline-bridge/02-06-SUMMARY.md
@.planning/phases/02-pipeline-bridge/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement all test_runs.py stubs as passing tests</name>
  <files>tests/test_runs.py</files>
  <behavior>
    Replace all xfail stubs with real test implementations. Each test must pass (not xfail).
    Use the existing conftest fixtures: client, db_engine, admin_user, engineer_user, huey_immediate.

    Key tests to implement:
    - test_upload_creates_run: POST /runs/new with valid files + metadata → 302 redirect; Run record exists in DB with correct part_number, rev_a_label, rev_b_label
    - test_raster_pdf_rejected: POST /runs/validate-pdf with a raster PDF → response contains error message ("scanned image" or similar)
    - test_multipage_pdf_page_selector: POST /runs/validate-pdf with a multi-page PDF → response contains page selector HTML
    - test_invalid_excel_rejected: POST /runs/new with unreadable Excel bytes → 422 or form re-render with error
    - test_rev_labels_stored: POST /runs/new → Run.rev_a_label and Run.rev_b_label match submitted values
    - test_pipeline_task_enqueued: POST /runs/new with huey_immediate fixture → run_pipeline_task executes; Run.status transitions from queued
    - test_stage_progress_updates: Direct call to run_pipeline_task with huey_immediate → Run.current_stage changes
    - test_run_status_lifecycle: GET /runs/{id} → returns 200 with run data
    - test_revA_balloon_failure: Simulate pipeline failure → Run.status="failed", Run.failure_stage set
    - test_revB_balloon_warning: Simulate low confidence result → Run.status="warning", Run.warning_type="low_confidence"
    - test_low_confidence_warning: Same as above — check confidence_summary JSON is set
    - test_failure_alert_created: Simulate failure → RunAlert created with correct user_id and message

    For PDF/Excel test fixtures, create minimal in-memory files:
    - Valid single-page PDF: Use PyMuPDF to create a tiny vector PDF with text in the test
    - Multi-page PDF: fitz.open() with 2 pages
    - Raster PDF: Skip real raster detection test (too complex); mock the validate_pdf_bytes function
    - Invalid Excel: Use b"not an excel file" bytes

    Implement create_minimal_pdf() helper in test_runs.py:
    ```python
    def create_minimal_pdf(num_pages=1, with_text=True) -> bytes:
        doc = fitz.new()
        for _ in range(num_pages):
            page = doc.new_page()
            if with_text:
                page.insert_text((50, 50), "BALLOON 1 Ø 1.25 ± 0.05")
        return doc.tobytes()
    ```

    For the pipeline task tests that call run_pipeline() with real files, mock run_pipeline to return a temp directory with a minimal delta_packet.json:
    ```python
    from unittest.mock import patch, MagicMock
    import tempfile, json, pathlib

    def mock_run_pipeline(**kwargs):
        # Create minimal output
        run_dir = pathlib.Path(tempfile.mkdtemp())
        (run_dir / "delta_packet.json").write_text(json.dumps({"run_id": "test", "items": []}))
        return run_dir
    ```

    Use `with patch("shop.tasks.run_pipeline", mock_run_pipeline):` in task-related tests.
  </behavior>
  <action>
    Implement all 12 tests in tests/test_runs.py. Remove the @pytest.mark.xfail decorator from each stub and add real test logic.

    For each test that requires an authenticated session, use the existing auth pattern from test_auth.py: create user, POST /login, get session cookie, then make requests.

    Create an `authenticated_client` helper or fixture that handles login:
    ```python
    def login_as(client, email, password):
        resp = client.post("/login", data={"email": email, "password": password}, follow_redirects=False)
        return resp.cookies.get("session_token")
    ```

    The `client` fixture already has setup_complete=True and handles DB override. Use it as the base.

    Tests that need an engineer user: use the engineer_user fixture from conftest.py (added in Plan 01) to get a seeded engineer, then authenticate as that engineer.

    For file upload tests, use the BytesIO pattern:
    ```python
    from io import BytesIO
    response = client.post(
        "/runs/validate-pdf",
        files={"file": ("test.pdf", BytesIO(pdf_bytes), "application/pdf")},
        data={"field": "revA"},
        cookies={"session_token": token},
    )
    ```

    After implementing all tests: run `uv run pytest tests/test_runs.py -v` and fix failures.
  </action>
  <verify>
    <automated>cd /home/khoa2/delta-preservation && uv run pytest tests/test_runs.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <done>All 12 test_runs.py tests pass (no xfail, no error); uv run pytest tests/ shows all green</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite green + Tailwind rebuild</name>
  <files></files>
  <action>
    1. Run the full test suite and fix any remaining failures:
    `uv run pytest -q`

    2. Rebuild Tailwind CSS to ensure all new DaisyUI classes (steps, alert-warning, badge-info) are included:
    `npm run build:css`

    If npm run build:css fails (wrong directory), run from the project root where package.json lives:
    `cd /home/khoa2/delta-preservation && npm run build:css`

    3. Verify that the new template classes appear in static/dist/output.css:
    - Check for "steps" (stage checklist)
    - Check for "badge-warning" (status badges)
    - Check for "alert-warning" (warning banners)

    4. Fix any import errors discovered during the full test suite run.

    5. Commit all changes with message "feat(02-pipeline-bridge): implement phase 2 pipeline bridge"
  </action>
  <verify>
    <automated>cd /home/khoa2/delta-preservation && uv run pytest -q 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite green (0 failures, all xfail removed from test_runs.py); Tailwind rebuilt with Phase 2 classes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 2 Docker end-to-end human verification</name>
  <what-built>
    Complete Phase 2 Pipeline Bridge:
    - Upload form at /runs/new (3 file pickers + part metadata)
    - PDF validation: raster detection, multi-page page selector
    - Huey task queue (SqliteHuey) + supervisord in Docker
    - Stage-by-stage run progress via SSE
    - Three run outcome states: failed (hard fail), warning (revB_balloon), warning (low_confidence)
    - Run list at /runs with filter
    - Dashboard with recent runs + Submit New Run CTA
    - Persistent nav bar on all pages
    - In-app failure alerts with dismiss
  </what-built>
  <how-to-verify>
    **Build and start the Docker stack:**
    ```bash
    cd /home/khoa2/delta-preservation
    docker compose -f docker/docker-compose.yml build
    docker compose -f docker/docker-compose.yml up
    ```

    **Verify both processes start:**
    - Docker logs should show both uvicorn startup and huey_consumer startup messages
    - No "huey.db not found" or import errors in logs

    **Complete setup wizard** (if first run):
    1. Visit http://localhost:8000 — redirects to /login
    2. Login as admin@shop.local / changeme
    3. Complete setup wizard steps 1-4

    **Test upload form:**
    4. Visit /dashboard — should see "Submit New Run" button and persistent nav bar
    5. Click "Submit New Run" — form at /runs/new with 3 file pickers + metadata
    6. Upload a multi-page PDF for Rev A — page selector should appear inline without page reload
    7. Upload a valid single-page Rev B PDF — no selector shown
    8. Upload a valid .xlsx — no error
    9. Fill in part metadata (part number, revision letters, customer, job number)
    10. Submit — redirect to /runs/{id} status page

    **Test run status page:**
    11. On /runs/{id} — see "Queued" status
    12. Within seconds — stage checklist should advance (spinner on current stage, checkmarks on completed)
    13. After completion — status changes to "Completed" with "Open Review Queue" button
    14. Refresh the page — status still shows Completed

    **Test run list:**
    15. Visit /runs — see list of all runs
    16. Filter by part number — filtered list appears

    **Test nav bar:**
    17. Nav bar visible on all pages (dashboard, /runs, /runs/new, /runs/{id})
    18. "New Run" link works; "My Runs" link works

    **Test alert flow (requires triggering a failure):**
    19. Submit a run that will fail (e.g., corrupt PDF after initial validation passes — or trigger artificially)
    20. Wait for failure — bell badge should show count; dashboard banner should appear
    21. Dismiss banner — it disappears; badge count decrements

    **Confirm air-gapped operation:**
    22. All functionality works with no outbound internet (Docker network is internal)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe issues found.</resume-signal>
  <action>Human verification: follow the steps in how-to-verify above. Build Docker image, start stack, verify upload form, run progress, warning states, alert flow, and nav bar. Report any issues found.</action>
  <files>docker/docker-compose.yml</files>
  <verify><automated>MISSING — human verification required for Docker e2e flow</automated></verify>
  <done>Human confirms end-to-end Docker flow works: submit run, stage progress visible, run completes or fails with correct UI state</done>
</task>

</tasks>

<verification>
Full automated suite must be green before human checkpoint:
`uv run pytest -q`

Expected result: all tests pass, no failures, no xfail in test_runs.py
</verification>

<success_criteria>
- All 12 tests in tests/test_runs.py pass (not xfail)
- Full test suite green (all phases)
- Docker compose up starts both uvicorn and huey_consumer
- Human verifies end-to-end submission flow in Docker
- Nav bar visible on all pages with correct alert badge
- Run status page shows stage progress via SSE
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-bridge/02-08-SUMMARY.md`
</output>
