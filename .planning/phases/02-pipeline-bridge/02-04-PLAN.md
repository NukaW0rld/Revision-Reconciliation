---
phase: 02-pipeline-bridge
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - shop/templates/base.html
  - shop/templates/dashboard.html
  - shop/routers/runs.py
  - shop/templates/runs/list.html
autonomous: true
requirements:
  - UPLOAD-01
must_haves:
  truths:
    - "Every page has a persistent nav bar with: shop name, New Run link, My Runs link, alert bell icon, user email, logout"
    - "Dashboard shows 5-10 most recent runs as compact cards with part number, revision letters, status badge, date, reviewer name"
    - "Dashboard has a prominent Submit New Run CTA button linking to /runs/new"
    - "Run list at /runs shows all runs filterable by part number and date"
  artifacts:
    - path: "shop/templates/base.html"
      provides: "Persistent nav bar with all required elements"
      contains: "New Run"
    - path: "shop/templates/dashboard.html"
      provides: "Updated dashboard with recent runs and Submit New Run CTA"
      contains: "Submit New Run"
    - path: "shop/templates/runs/list.html"
      provides: "Full run list with filter controls"
    - path: "shop/routers/runs.py"
      provides: "GET /runs route and GET /dashboard updates"
  key_links:
    - from: "shop/templates/base.html"
      to: "/runs/new"
      via: "nav bar New Run link"
      pattern: "/runs/new"
    - from: "shop/templates/dashboard.html"
      to: "shop/routers/runs.py"
      via: "recent runs context variable from GET /dashboard"
      pattern: "recent_runs"
---

<objective>
Add the persistent nav bar and update the dashboard with run visibility and submission CTA.

Purpose: Engineers need consistent navigation and run visibility from every page. The nav bar (locked decision) and dashboard update are foundational UI changes that all other Phase 2 templates extend.
Output: Updated base.html with nav bar, updated dashboard.html with run cards and Submit CTA, new runs/list.html, GET /runs route in runs router.
</objective>

<execution_context>
@/home/khoa2/.claude/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-pipeline-bridge/02-CONTEXT.md
@.planning/phases/02-pipeline-bridge/02-01-SUMMARY.md

<interfaces>
<!-- Current base.html (has no nav bar yet) -->
```html
<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Delta Preservation{% endblock %}</title>
    <link rel="stylesheet" href="/static/dist/output.css">
    <script src="/static/js/htmx.min.js"></script>
    <script src="/static/js/htmx-sse.js"></script>
</head>
<body class="min-h-screen bg-base-200">
    <div id="main-content">
        {% block content %}{% endblock %}
    </div>
</body>
</html>
```

<!-- Locked decisions from CONTEXT.md -->
Nav bar content (Phase 2 adds to base.html):
- Shop name/logo on left
- "New Run" link (to /runs/new)
- "My Runs" link (to /runs)
- Alert bell icon with red badge (count of unread alerts; 0 = no badge shown)
- Logged-in user email + logout button

Run card content:
- Part number (prominent)
- Revision letters: Rev A→B labels
- Status badge (DaisyUI badge with color)
- Submitted date
- Assigned reviewer name

Dashboard run list: 5-10 most recent runs

<!-- Run model (available after Plan 01) -->
```python
class Run(Base):
    id: int
    part_number: str
    rev_a_label: str
    rev_b_label: str
    status: str  # queued/running/completed/failed/warning
    submitted_at: datetime
    reviewer: Optional[User]  # relationship
```

<!-- Auth router: /dashboard route exists in auth.py already -->
# The existing /dashboard GET route in auth.py needs to pass recent_runs to dashboard template
# Plan 04 updates that route to include Run query
```python
# In shop/routers/auth.py, GET /dashboard currently:
return templates.TemplateResponse(request, "dashboard.html", {"user": user})
# Needs to become:
return templates.TemplateResponse(request, "dashboard.html", {"user": user, "recent_runs": recent_runs})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent nav bar to base.html</name>
  <files>shop/templates/base.html</files>
  <action>
    Update base.html to add a persistent DaisyUI navbar that appears on every page.

    Structure:
    - DaisyUI `navbar bg-base-100 shadow-sm` fixed at top
    - Left side: shop name or "Delta Preservation" as a text logo, linking to /dashboard
    - Center/right: navigation links and user controls
    - Only show nav bar if user is in context (templates that set no user, like login, should skip nav)
    - Use Jinja2 conditional: `{% if request.state.user is defined %}` — but since request.state.user may not be set in all contexts, pass a `current_user` variable from the router or use a default

    Approach: Add a `{% block nav %}{% endblock %}` that individual templates can suppress if needed, but provide a default nav block that checks for `user` variable.

    Nav bar HTML:
    ```html
    <div class="navbar bg-base-100 shadow-sm sticky top-0 z-50">
      <div class="navbar-start">
        <a href="/dashboard" class="text-lg font-bold px-4">{{ shop_name | default("Delta Preservation") }}</a>
      </div>
      <div class="navbar-end gap-2 pr-4">
        {% if user %}
          <a href="/runs/new" class="btn btn-primary btn-sm">New Run</a>
          <a href="/runs" class="btn btn-ghost btn-sm">My Runs</a>
          <!-- Alert bell -->
          <div class="indicator">
            <a href="/alerts" class="btn btn-ghost btn-sm btn-circle">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-2.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
              </svg>
            </a>
            {% if unread_alert_count and unread_alert_count > 0 %}
            <span class="badge badge-error badge-xs indicator-item">{{ unread_alert_count }}</span>
            {% endif %}
          </div>
          <span class="text-sm text-base-content/70">{{ user.email }}</span>
          <form method="post" action="/logout">
            <button type="submit" class="btn btn-outline btn-xs">Log out</button>
          </form>
        {% endif %}
      </div>
    </div>
    ```

    The `user`, `shop_name`, and `unread_alert_count` variables must be passed from each route. For routes that don't have them, the nav bar gracefully hides the user section. Templates that want to hide the nav bar entirely (e.g., login page) can override `{% block nav %}{% endblock %}`.

    Update base.html structure:
    ```html
    <body class="min-h-screen bg-base-200">
      {% block nav %}
      <!-- nav bar HTML above -->
      {% endblock %}
      <div id="main-content" class="container mx-auto p-6">
        {% block content %}{% endblock %}
      </div>
    </body>
    ```

    Auth templates (login, setup wizard) should override `{% block nav %}{% endblock %}` to suppress the nav bar since the user is not yet authenticated.

    The login template (shop/templates/auth/login.html) should add `{% block nav %}{% endblock %}` at top of content block or use `{% extends %}` to suppress.
  </action>
  <verify>
    <automated>cd /home/khoa2/delta-preservation && grep -q "New Run" shop/templates/base.html && grep -q "My Runs" shop/templates/base.html && echo "nav bar present"</automated>
  </verify>
  <done>base.html has nav bar with shop name, New Run link, My Runs link, alert bell, user email, logout; login page suppresses nav bar</done>
</task>

<task type="auto">
  <name>Task 2: Update dashboard route and template + create GET /runs route and runs/list.html</name>
  <files>shop/routers/auth.py, shop/templates/dashboard.html, shop/routers/runs.py, shop/templates/runs/list.html</files>
  <action>
    1. Update shop/routers/auth.py GET /dashboard to pass recent runs and alert count:

    ```python
    from shop.models import Run, RunAlert
    # In the dashboard handler:
    recent_runs = db.query(Run).order_by(Run.submitted_at.desc()).limit(10).all()
    unread_count = db.query(RunAlert).filter(
        RunAlert.user_id == user.id, RunAlert.is_read == False
    ).count()
    # Also fetch shop name for nav bar
    from shop.models import ShopConfig
    config = db.query(ShopConfig).filter(ShopConfig.id == 1).first()
    shop_name = config.shop_name if config else "Delta Preservation"
    return templates.TemplateResponse(request, "dashboard.html", {
        "user": user,
        "recent_runs": recent_runs,
        "unread_alert_count": unread_count,
        "shop_name": shop_name,
    })
    ```

    2. Update shop/templates/dashboard.html:
    - Replace current logout-only header with:
      - Prominent "Submit New Run" CTA button (btn btn-primary btn-lg) linking to /runs/new
      - Recent runs section below: "Recent Runs" heading + DaisyUI cards grid
    - Remove the standalone logout button (now in nav bar)
    - Keep admin management links for admin role
    - Run card template for each run in recent_runs:
      ```html
      <a href="/runs/{{ run.id }}" class="card bg-base-100 shadow hover:shadow-md transition-shadow">
        <div class="card-body">
          <div class="flex items-start justify-between">
            <h3 class="card-title text-base">{{ run.part_number }}</h3>
            <span class="badge badge-{{ run.status | status_badge_class }}">{{ run.status }}</span>
          </div>
          <p class="text-sm text-base-content/70">Rev {{ run.rev_a_label }} → Rev {{ run.rev_b_label }}</p>
          <p class="text-xs text-base-content/50">{{ run.submitted_at.strftime("%Y-%m-%d %H:%M") }} · Reviewer: {{ run.reviewer.email if run.reviewer else "Unassigned" }}</p>
        </div>
      </a>
      ```
    - Add Jinja2 custom filter for status badge class in app.py:
      ```python
      # In create_app(), after templates declaration:
      def status_badge_class(status):
          return {"completed": "success", "failed": "error", "warning": "warning", "running": "info", "queued": "ghost"}.get(status, "ghost")
      templates.env.filters["status_badge_class"] = status_badge_class
      ```
    - If recent_runs is empty: show "No runs yet. Submit your first run above." message

    3. Add GET /runs to shop/routers/runs.py:
    ```python
    @router.get("/", response_class=HTMLResponse)
    def list_runs(
        request: Request,
        part_number: str = None,
        db: Session = Depends(get_db),
        user: User = Depends(get_current_user),
    ):
        query = db.query(Run).order_by(Run.submitted_at.desc())
        if part_number:
            query = query.filter(Run.part_number.ilike(f"%{part_number}%"))
        runs = query.all()
        # alert count for nav bar
        unread_count = db.query(RunAlert).filter(
            RunAlert.user_id == user.id, RunAlert.is_read == False
        ).count()
        from shop.models import ShopConfig
        config = db.query(ShopConfig).filter(ShopConfig.id == 1).first()
        shop_name = config.shop_name if config else "Delta Preservation"
        return templates.TemplateResponse(request, "runs/list.html", {
            "user": user, "runs": runs, "part_number": part_number or "",
            "unread_alert_count": unread_count, "shop_name": shop_name,
        })
    ```
    Note: RunAlert import inside function to avoid circular imports at module load.

    4. Create shop/templates/runs/list.html:
    - Extends base.html
    - Title: "All Runs"
    - Filter form: GET form with part_number text input + "Filter" button + "Clear" link
    - Table or card grid of all runs: part number, revA→revB, status badge, submitted date, reviewer
    - Each row/card links to /runs/{id}
    - Empty state: "No runs match your filter" or "No runs yet"
    - "View all runs" link on dashboard points here

    Also import RunAlert in runs.py (deferred inside functions to avoid circular imports).
  </action>
  <verify>
    <automated>cd /home/khoa2/delta-preservation && uv run pytest tests/test_auth.py tests/test_admin.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard renders with Submit New Run CTA + recent run cards; GET /runs returns 200 with run list; nav bar visible on all authenticated pages; existing auth/admin tests pass</done>
</task>

</tasks>

<verification>
Run existing tests to confirm no regressions:
`uv run pytest -q`

Manual check:
- Visit /dashboard — should see nav bar, Submit New Run CTA, recent runs section
- Visit /runs — should see all runs list with filter
- Nav bar on all pages: New Run, My Runs, alert bell, user email, logout
</verification>

<success_criteria>
- base.html has persistent nav bar (shop name, New Run, My Runs, alert bell, user email, logout)
- Dashboard shows Submit New Run CTA + up to 10 recent run cards
- GET /runs returns run list filterable by part_number query param
- runs/list.html template renders with DaisyUI table/cards
- Existing test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-bridge/02-04-SUMMARY.md`
</output>
