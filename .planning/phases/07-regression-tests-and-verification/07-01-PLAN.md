---
phase: 07-regression-tests-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_classify_bugfixes.py
  - tests/test_classify_phase5_regression.py
  - .planning/phases/07-regression-tests-and-verification/07-01-AUDIT.md
autonomous: true
requirements: [TST-01]
test_ids:
  - tests/test_classify_bugfixes.py::TestAdjacencyBleed
  - tests/test_classify_phase5_regression.py::TestAsymmetricToleranceRegex
must_haves:
  truths:
    - "Every fix cluster from Phases 4-6 has at least one pytest parametrized case that provably fails on the pre-fix code and passes on the fixed code"
    - "Gap-filling cases are added to the existing phase-specific test files, never to a new consolidating file"
    - "The audit produces an explicit per-cluster verdict table written to 07-01-AUDIT.md"
  artifacts:
    - path: ".planning/phases/07-regression-tests-and-verification/07-01-AUDIT.md"
      provides: "Per-cluster audit table documenting existing exemplars, pre-fix failure reasoning, and gap verdicts"
      contains: "## Cluster audit table"
    - path: "tests/test_classify_bugfixes.py"
      provides: "Phase 7 gap-filling parametrized case for CLS-03 leading-decimal asymmetric tolerance shape"
      contains: "leading_decimal"
---

<objective>
Audit every Phase 4-6 fix cluster against its existing pytest coverage,
produce a written audit table, and add any missing parametrized cases to the
existing phase-specific test files. The goal is provable per-cluster
regression coverage without creating a redundant consolidating test file.

This plan is the TST-01 gap-closure step. The consolidating milestone file
(`tests/test_phase7_regression.py`) is written later in plan 07-02 so that
it imports from already-stable phase-specific modules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-regression-tests-and-verification/07-CONTEXT.md
@.planning/phases/07-regression-tests-and-verification/07-RESEARCH.md
@tests/test_semantic_extraction.py
@tests/test_classify_bugfixes.py
@tests/test_classify_phase5_regression.py
@tests/test_phase6_exclusion.py
@tests/test_phase6_asset_regression.py
@tests/test_added_detection_phase6.py
@delta_preservation/reconcile/classify.py
@delta_preservation/reconcile/normalize.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce the per-cluster audit document</name>
  <files>.planning/phases/07-regression-tests-and-verification/07-01-AUDIT.md</files>
  <read_first>
.planning/phases/07-regression-tests-and-verification/07-CONTEXT.md,
.planning/phases/07-regression-tests-and-verification/07-RESEARCH.md,
tests/test_semantic_extraction.py,
tests/test_classify_bugfixes.py,
tests/test_classify_phase5_regression.py,
tests/test_phase6_exclusion.py,
tests/test_phase6_asset_regression.py,
tests/test_added_detection_phase6.py
  </read_first>
  <action>
Create `.planning/phases/07-regression-tests-and-verification/07-01-AUDIT.md`
with the exact table below. For each of the 9 clusters listed, fill the
"Existing exemplar" column with the fully-qualified
`tests/<file>::Class::test_name` (or the module-level function name) that
proves the fix cluster is covered, the "Pre-fix failure reasoning" column
with a single-sentence argument why that exemplar would fail on the pre-fix
code path, and the "Gap verdict" column with one of
`covered | gap_filling_required | stronger_guard_recommended`.

```markdown
# Phase 7 TST-01 Audit

**Audit date:** YYYY-MM-DD
**Auditor:** <agent or developer name>
**Source of truth:** .planning/phases/07-regression-tests-and-verification/07-RESEARCH.md §1

## Cluster audit table

| # | Cluster | Requirement | Existing exemplar | Pre-fix failure reasoning | Gap verdict |
|---|---------|-------------|-------------------|---------------------------|-------------|
| 1 | GDT-01 compact token splitting | GDT-01 | `tests/test_semantic_extraction.py::test_extract_semantic_callout_gdt_parsed_compact_token_variants` | ... | covered |
| 2 | GDT-02 word-name normalization | GDT-02 | ... | ... | ... |
| 3 | GDT-03 composite FCF capture | GDT-03 | ... | ... | ... |
| 4 | CLS-01 adjacency bleed suppression | CLS-01 | ... | ... | ... |
| 5 | CLS-02 removed+added reconciliation | CLS-02 | ... | ... | ... |
| 6 | CLS-03 asymmetric tolerance detection | CLS-03 | ... | ... | ... |
| 7 | ADD-01 missing added rows | ADD-01 | ... | ... | ... |
| 8 | ADD-02 false-positive suppression | ADD-02 | ... | ... | ... |
| 9 | SNP-01 title block / revision exclusion | SNP-01 | ... | ... | ... |

## Gap-filling actions

For every row with verdict `gap_filling_required` or
`stronger_guard_recommended`, list the proposed new test:
- **File:** `tests/<existing phase-specific test file>`
- **Test name:** `test_<descriptive_snake_case>`
- **Input(s):** exact parametrize values
- **Expected output:** exact assertion
- **Reason:** why the existing exemplar is insufficient

## Zero-gap statement (if applicable)

If all nine clusters are covered with no gap-filling required, include
this statement signed with the audit date and git HEAD SHA:

> All nine Phase 4-6 fix clusters are covered by at least one pre-fix-failing
> parametrized or class-based pytest case as of &lt;git sha&gt;. No TST-01
> gap-filling is required.
```

Populate every row. Leave no ellipsis in the final committed file. Use the
cluster-to-exemplar mapping in `07-RESEARCH.md` §1 as the starting point,
but verify each referenced test name exists via
`rg -n "def test_<name>" tests/` before writing it to the audit.
  </action>
  <acceptance_criteria>
    - `.planning/phases/07-regression-tests-and-verification/07-01-AUDIT.md` exists.
    - The file contains the string `## Cluster audit table` and exactly 9 data rows under that header.
    - No row's `Existing exemplar` column contains `...` or `TBD` — every row names a real test via `tests/<file>::<identifier>`.
    - Every named test exists: `rg -n "def test_<each name>" tests/` returns at least one match per named test.
    - Every row's `Gap verdict` column contains one of `covered`, `gap_filling_required`, or `stronger_guard_recommended`.
    - If every row is `covered`, the document must include the "Zero-gap statement" block with the current git HEAD short SHA filled in.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the CLS-03 leading-decimal asymmetric tolerance gap-filling case</name>
  <files>tests/test_classify_phase5_regression.py</files>
  <read_first>
tests/test_classify_phase5_regression.py,
tests/test_classify_bugfixes.py,
delta_preservation/reconcile/classify.py,
.planning/phases/07-regression-tests-and-verification/07-RESEARCH.md
  </read_first>
  <action>
Research note §1 Cluster 6 flags that the `_ASYMMETRIC_SHAPE_RE` regex path
is exercised by the full-form `+0.3° / −0.1°` exemplar but not by the
leading-decimal form `+.3° / −.1°` (no leading zero on the tolerance
values). This task pins the leading-decimal variant as an explicit
parametrized guard.

In `tests/test_classify_phase5_regression.py`, inside the existing
`TestAsymmetricToleranceRegex` class (or an equivalent class that contains
`test_asymmetric_shape_re_matches_part7_exemplar`), add a new test:

```python
@pytest.mark.parametrize(
    "revB_text",
    [
        "2X 22.0° +.3° / −.1°",
        "2X 22.0° +.3°/−.1°",
        "22.0° +.5° / −.2°",
    ],
)
def test_asymmetric_shape_re_matches_leading_decimal_variants(self, revB_text):
    """The _ASYMMETRIC_SHAPE_RE guard must also fire on leading-decimal forms
    (no leading zero on the tolerance magnitudes)."""
    from delta_preservation.reconcile.classify import _ASYMMETRIC_SHAPE_RE
    assert _ASYMMETRIC_SHAPE_RE.search(revB_text) is not None, (
        f"Expected asymmetric shape regex to match leading-decimal form {revB_text!r}; "
        "CLS-03 fix must cover the shape without a leading zero"
    )
```

Also add a negative guard in the same class that pins a non-asymmetric
leading-decimal form (a benign fractional ratio):

```python
@pytest.mark.parametrize(
    "revB_text",
    [
        ".3 / .1",
        "70 / 30",
    ],
)
def test_asymmetric_shape_re_does_not_match_plain_fractional_ratios(self, revB_text):
    """Plain decimal ratios without the +/− markers must NOT match the
    asymmetric shape regex — otherwise a benign fractional ratio would be
    misclassified as an asymmetric tolerance change."""
    from delta_preservation.reconcile.classify import _ASYMMETRIC_SHAPE_RE
    assert _ASYMMETRIC_SHAPE_RE.search(revB_text) is None, (
        f"Plain ratio {revB_text!r} must not match asymmetric shape regex"
    )
```

Import `pytest` at the top of the file if not already imported. Do not
rename or delete any existing test. Keep the new tests inside the class
that already hosts `test_asymmetric_shape_re_matches_part7_exemplar` so the
regression harness remains a single discoverable class per cluster.

**Skip this task** (document it explicitly in the final SUMMARY.md) only if
Task 1's audit concluded the CLS-03 cluster is fully covered and the
leading-decimal shape is already exercised elsewhere. In that case, leave
`tests/test_classify_phase5_regression.py` untouched.
  </action>
  <acceptance_criteria>
    - Either: (a) `rg -n "test_asymmetric_shape_re_matches_leading_decimal_variants" tests/test_classify_phase5_regression.py` returns at least one match AND `rg -n "test_asymmetric_shape_re_does_not_match_plain_fractional_ratios" tests/test_classify_phase5_regression.py` returns at least one match; OR (b) the plan's SUMMARY.md documents that the audit in Task 1 closed this gap before this task ran and the file was intentionally not modified.
    - If (a): `uv run pytest tests/test_classify_phase5_regression.py -k "asymmetric_shape_re" -x` exits 0.
    - `uv run pytest tests/test_classify_phase5_regression.py -x` exits 0 — no other tests are broken.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Execute any additional gap-filling cases identified in the audit</name>
  <files>tests/test_semantic_extraction.py, tests/test_classify_bugfixes.py, tests/test_classify_phase5_regression.py, tests/test_phase6_exclusion.py, tests/test_phase6_asset_regression.py, tests/test_added_detection_phase6.py</files>
  <read_first>
.planning/phases/07-regression-tests-and-verification/07-01-AUDIT.md,
tests/test_semantic_extraction.py,
tests/test_classify_bugfixes.py,
tests/test_classify_phase5_regression.py,
tests/test_phase6_exclusion.py,
tests/test_phase6_asset_regression.py,
tests/test_added_detection_phase6.py
  </read_first>
  <action>
Re-read `07-01-AUDIT.md` produced in Task 1. For every row where the
verdict is `gap_filling_required` or `stronger_guard_recommended` AND the
cluster is not the CLS-03 leading-decimal case already handled in Task 2,
add the proposed parametrized case exactly as spelled out in the audit's
"Gap-filling actions" section.

Rules:
- Gap-filling cases MUST be added to the existing phase-specific test file
  named in the audit. Never create a new test module in this task.
- Each new case MUST be parametrized with explicit input strings and
  explicit expected outputs — no loops over fixtures, no dynamic strings.
- Do not rename, delete, or otherwise modify any existing test. Additive
  changes only.

If Task 1's audit produced a zero-gap outcome (all 9 clusters `covered`
with the signed zero-gap statement), skip this task entirely. Record the
skip in the plan's SUMMARY.md.
  </action>
  <acceptance_criteria>
    - Either: (a) for each `gap_filling_required` or `stronger_guard_recommended` row in `07-01-AUDIT.md` other than CLS-03 leading-decimal, the named test function exists in the target file (`rg -n "def test_<name>" tests/<target_file>` returns >= 1 match); OR (b) the audit's zero-gap statement is present in `07-01-AUDIT.md` and the plan's SUMMARY.md records the skip.
    - `uv run pytest tests/ -x` exits 0 — no existing test is broken by the additions.
    - `git diff --stat tests/` shows additions only to the files listed in `files_modified` above; no existing tests are renamed or deleted.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit document -> gap-filling actions | The `07-01-AUDIT.md` document is the sole source of truth for which gap-filling tests Task 3 writes. A wrong audit produces wrong or missing coverage. |
| New parametrized cases -> existing test suite | New additive tests must not change the behavior of any existing passing test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (tampering) | 07-01-AUDIT.md | mitigate | Audit must name every referenced test with a file path so the gap-filling step is grounded in real, greppable identifiers. |
| T-07-02 | R (repudiation) | 07-01-AUDIT.md | mitigate | Zero-gap outcomes must be signed with the current git HEAD short SHA so future audits can trace what state was considered "covered". |
| T-07-03 | I (information disclosure) | tests/* | n/a | No secrets or credentials touched by this plan. |
| T-07-04 | D (denial of service) | test suite runtime | mitigate | Gap-filling cases must be parametrized and fast (< 100ms per case). Do not introduce any test that loads a large fixture or runs the full pipeline. |
| T-07-05 | E (elevation of privilege) | tests/* | n/a | Tests do not touch file permissions, auth, or external systems. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/ -x` (all existing tests still pass)
- `uv run pytest tests/test_classify_phase5_regression.py -k "asymmetric_shape_re" -x` (new leading-decimal case, if applicable)
- `rg -n "^## Cluster audit table" .planning/phases/07-regression-tests-and-verification/07-01-AUDIT.md` (audit document has the required section)
- `python3 -c "import re; txt=open('.planning/phases/07-regression-tests-and-verification/07-01-AUDIT.md').read(); rows=[l for l in txt.splitlines() if l.startswith('|') and not l.startswith('|---') and not l.startswith('| #')]; assert len(rows) >= 9, f'expected >=9 cluster rows, got {len(rows)}'; print('ok')"`
</verification>

<success_criteria>
- Every Phase 4-6 fix cluster has a documented, greppable, pre-fix-failing pytest exemplar.
- Any identified gaps are closed with parametrized cases added to the existing phase-specific test files.
- The CLS-03 leading-decimal shape is either explicitly covered by a new parametrized case or recorded as already-covered in the audit.
- The full test suite still passes after the gap-filling additions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-regression-tests-and-verification/07-01-SUMMARY.md`
</output>
