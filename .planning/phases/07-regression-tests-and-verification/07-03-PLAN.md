---
phase: 07-regression-tests-and-verification
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part1-run.txt
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part2-run.txt
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part3-run.txt
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part4-run.txt
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part5-run.txt
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part6-run.txt
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part7-run.txt
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part8-run.txt
  - .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part9-run.txt
autonomous: false
requirements: [VER-01]
test_ids: []
must_haves:
  truths:
    - "All 9 parts were re-run through the current algorithm via `python run.py <part>` and their per-part conformance results were captured verbatim"
    - "Each per-part verdict is compared against a documented pre-Phase-4 baseline and any regression on a previously-passing characteristic is flagged explicitly"
    - "Phase 7 is not closeable until 07-VERIFICATION.md is committed with the summary table fully populated"
    - "ground_truth.json files are not edited by this plan — they remain canonical references"
  artifacts:
    - path: ".planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md"
      provides: "Human-readable verification report covering all 9 parts with pre/post comparison and regression flags"
      contains: "## Summary table"
    - path: ".planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/"
      provides: "Raw per-part pipeline output captures used as the evidence trail for VERIFICATION.md"
      contains: "part1-run.txt"
---

<objective>
Complete the VER-01 verification step: run `python run.py <part_name>` for
all 9 parts against the current (post-Phase-6) algorithm, capture the
per-part ground-truth evaluation results as evidence artifacts, compare
each part against a documented pre-Phase-4 baseline, and produce
`07-VERIFICATION.md` as the committed closure gate for Phase 7.

This plan is marked `autonomous: false` because CONTEXT.md D-06 locks VER-01
as a manual developer action. The executor should prepare everything that
CAN be automated (artifact directory, baseline-derivation command,
VERIFICATION.md template) and then stop to let the developer run the
pipeline. Once raw run outputs exist in
`07-03-VER-ARTIFACTS/`, the executor can resume and fill the summary table
mechanically.

Phase 7 is not complete and must not be marked closed until
`07-VERIFICATION.md` exists, is committed, and documents zero regressions
vs the pre-Phase-4 baseline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-regression-tests-and-verification/07-CONTEXT.md
@.planning/phases/07-regression-tests-and-verification/07-RESEARCH.md
@.planning/phases/07-regression-tests-and-verification/07-02-BASELINE-DERIVATION.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@run.py
@delta_preservation/evaluation/conformance.py
@assets/part1/ground_truth.json
@assets/part9/ground_truth.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the VER-01 artifact directory and VERIFICATION.md template</name>
  <files>
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/README.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
  </files>
  <read_first>
.planning/phases/07-regression-tests-and-verification/07-CONTEXT.md,
.planning/phases/07-regression-tests-and-verification/07-RESEARCH.md,
run.py
  </read_first>
  <action>
Create the directory
`.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/`
and inside it write a short `README.md` describing the artifact shape.

```markdown
# VER-01 Per-Part Run Artifacts

Each `partN-run.txt` file is the verbatim stdout + stderr capture of:

```bash
python run.py part<N> 2>&1 | tee 07-03-VER-ARTIFACTS/part<N>-run.txt
```

These captures are the evidence trail for `07-VERIFICATION.md`. Do not
edit them after capture. If a part needs to be re-run, overwrite the file
with the new full capture and re-derive the summary table.

The captures intentionally include the run's:
- pipeline stage log lines
- per-part conforming / review_needed / missing_added_truth_indexes output
- any exception traces or warnings

Do not redact. Do not truncate.
```

Then create the Phase 7 VERIFICATION.md template at
`.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md`
with the exact structure below, leaving placeholder sentinels
(`<TODO_FILL>`) in every field that depends on the manual run outputs.

```markdown
# Phase 7 Verification: Full 9-Part Ground-Truth Re-Run (VER-01)

**Run date:** <TODO_FILL>
**Developer:** <TODO_FILL>
**Algorithm baseline commit (pre-Phase-4):** <TODO_FILL>
**Algorithm current commit (Phase 7 head):** <TODO_FILL>
**Raw run artifacts:** [07-03-VER-ARTIFACTS/](./07-03-VER-ARTIFACTS/)

## Summary table

| Part | Conforming (pre) | Conforming (post) | Review-Needed (pre) | Review-Needed (post) | Missing Added (pre) | Missing Added (post) | Previously-passing regressions | Verdict |
|------|------------------|-------------------|---------------------|----------------------|---------------------|----------------------|--------------------------------|---------|
| 1    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |
| 2    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |
| 3    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |
| 4    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |
| 5    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |
| 6    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |
| 7    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |
| 8    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |
| 9    | <TODO_FILL>      | <TODO_FILL>       | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>         | <TODO_FILL>          | <TODO_FILL>                    | <TODO_FILL> |

**Verdict column values:** `pass` | `improved` | `regression`.
- `pass` — post counts equal pre counts; no previously-passing characteristic regressed.
- `improved` — post conforming >= pre AND (post missing_added <= pre OR post conforming > pre).
- `regression` — any previously-passing characteristic is no longer conforming, OR post conforming < pre.

## Per-part notes

### Part 1
- Raw output: `07-03-VER-ARTIFACTS/part1-run.txt`
- Conforming: pre=<TODO_FILL>, post=<TODO_FILL>
- Review-needed: pre=<TODO_FILL>, post=<TODO_FILL>
- missing_added_truth_indexes: pre=<TODO_FILL>, post=<TODO_FILL>
- Previously-passing characteristics that regressed: <TODO_FILL or "none">
- Notes: <TODO_FILL>

### Part 2
<repeat same block>

<...through Part 9...>

## Pre-Phase-4 baseline source

<TODO_FILL: choose exactly ONE>

- [ ] **Option A — v1.0 milestone audit:** counts sourced from
      `.planning/milestones/v1.0-MILESTONE-AUDIT.md`
      (section `<section name>`, row `<row>`).
- [ ] **Option B — Git-historic snapshot:** pre-Phase-4 counts extracted
      via `git show <pre-phase-4-sha>:assets/debug_report_part<N>.json`
      with `<pre-phase-4-sha>` being the commit immediately before
      Phase 4 landed.
- [ ] **Option C — Checkout + re-run:** pipeline re-run against
      `main@<pre-phase-4-sha>`; raw output in
      `07-03-VER-ARTIFACTS/baseline-part<N>-run.txt`.

## Phase closure statement

<TODO_FILL after all 9 rows are populated>

> I confirm that all 9 parts have been re-run through the current pipeline,
> all per-part verdicts are recorded above, and zero previously-passing
> characteristics regressed versus the pre-Phase-4 baseline. Phase 7 is
> ready to close.
>
> — Signed: <developer name>, <YYYY-MM-DD>, git <short sha>
```

After writing both files, commit the scaffold so the phase directory
reflects that VER-01 preparation is complete. The actual measurements are
filled in Task 2 once the developer has produced the raw captures.
  </action>
  <acceptance_criteria>
    - The directory `.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/` exists with a committed `README.md` inside.
    - `.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` exists.
    - `rg -n "^## Summary table" .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` returns a match.
    - `rg -n '^\\| [0-9] ' .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md | wc -l` equals 9 (one row per part).
    - `rg -n "### Part [1-9]" .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md | wc -l` equals 9.
    - `rg -n "^## Phase closure statement" .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` returns a match.
    - `rg -n "<TODO_FILL>" .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` returns at least 50 matches — the template is intentionally unfilled at this task's completion.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 2: Developer-driven 9-part pipeline re-run (autonomous=false)</name>
  <files>
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part1-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part2-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part3-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part4-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part5-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part6-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part7-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part8-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part9-run.txt
  </files>
  <read_first>
run.py,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/README.md
  </read_first>
  <action>
This task is a manual developer checkpoint. The executor agent MUST NOT run
the pipeline itself — CONTEXT.md D-06 locks VER-01 as a human verification
step. Instead, the executor surfaces the exact commands the developer
needs to run and then waits.

Developer run script (copy-pasteable):

```bash
set -e
mkdir -p .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS
for i in 1 2 3 4 5 6 7 8 9; do
  echo "=== part${i} ==="
  python run.py part${i} 2>&1 \
    | tee .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part${i}-run.txt
done
```

The developer reviews each capture for:
- absence of unhandled exceptions (`Traceback` lines)
- presence of the per-part summary block (conforming count, review_needed
  count, missing_added_truth_indexes list)
- the pipeline ran against the expected revA.pdf + revB.pdf
  (cross-check `assets/part<N>/` contents)

When all 9 captures exist in `07-03-VER-ARTIFACTS/`, the developer signals
the executor to resume at Task 3.

**Executor stopping condition for this task:** emit a `## CHECKPOINT REACHED`
marker in the execution summary with the exact developer run script above,
then stop. Do not fabricate pipeline output. Do not guess counts. Do not
skip to Task 3 without real captures.
  </action>
  <acceptance_criteria>
    - Files `part1-run.txt` through `part9-run.txt` exist in `.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/`.
    - Each capture is non-empty: `wc -c 07-03-VER-ARTIFACTS/part<N>-run.txt` returns >= 500 bytes.
    - Each capture contains the string `conforming` (`rg -c "conforming" 07-03-VER-ARTIFACTS/part<N>-run.txt` returns >= 1).
    - No capture contains `Traceback (most recent call last)` unless the accompanying VERIFICATION.md Part <N> section explicitly documents the exception and its disposition.
    - The executor's summary explicitly marks this task as a `## CHECKPOINT REACHED` handoff if captures are missing at agent-run time.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Derive post counts from raw captures and populate 07-VERIFICATION.md</name>
  <files>.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md</files>
  <read_first>
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/README.md,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part1-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part2-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part3-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part4-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part5-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part6-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part7-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part8-run.txt,
.planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/part9-run.txt,
.planning/milestones/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <action>
Once Task 2's 9 captures exist, mechanically extract the per-part counts
from the freshly-produced `out/part<N>/debug_report.json` files (run.py
writes these as a side effect). Use this derivation command:

```bash
python3 - <<'PY'
import json, collections
from pathlib import Path
for i in range(1, 10):
    path = Path(f"out/part{i}/debug_report.json")
    if not path.exists():
        print(f"part{i}: MISSING debug_report.json — developer did not run this part")
        continue
    d = json.load(path.open())
    items = d.get("items", [])
    statuses = collections.Counter(
        (it.get("evaluation") or {}).get("status", "(no_eval)") for it in items
    )
    conforming = statuses.get("conforming", 0)
    review = statuses.get("review_needed", 0)
    missing_idx = d.get("missing_added_truth_indexes", [])
    print(
        f"part{i}: conforming={conforming} review_needed={review} "
        f"missing_added_truth_indexes={missing_idx}"
    )
PY
```

Fill the `(post)` columns of the summary table in
`07-VERIFICATION.md` verbatim from this output.

Fill the `(pre)` columns using the baseline source chosen in the
"Pre-Phase-4 baseline source" section of the template. If Option A
(milestone audit) is selected, cite the exact section and row. If Option
B (git-historic snapshot) is selected, record the SHA used and capture
the per-part baseline counts via:

```bash
for i in 1 2 3 4 5 6 7 8 9; do
  git show <pre-phase-4-sha>:assets/debug_report_part${i}.json 2>/dev/null \
    | python3 -c "
import json, sys, collections
d = json.load(sys.stdin)
items = d.get('items', [])
statuses = collections.Counter((it.get('evaluation') or {}).get('status', '(no_eval)') for it in items)
print(f\"part${i}: conforming={statuses.get('conforming',0)} review_needed={statuses.get('review_needed',0)} missing={d.get('missing_added_truth_indexes', [])}\")
"
done
```

For each part, compute the `Previously-passing regressions` column by:
1. Loading the pre-Phase-4 debug_report JSON for that part.
2. Collecting the set of `(char_no, pipeline_classification)` tuples from
   items with `evaluation.status == "conforming"` in pre.
3. Loading the post debug_report JSON and collecting the set of
   `(char_no, pipeline_classification)` tuples from items with
   `evaluation.status == "conforming"` in post.
4. The regression set is `pre_conforming - post_conforming`.
5. If the set is empty, write `none`. Otherwise list each regressed char_no
   with a one-line reason derived from the post item's `mismatches` field.

Fill the Verdict column per the legend in the template.

Finally, populate the Phase closure statement with the developer's name,
the date, and the current git short SHA. Do NOT sign the closure statement
on the developer's behalf if any part's verdict is `regression` — in that
case, surface the regressions, leave the closure statement unfilled, and
emit `## VERIFICATION BLOCKED` with the regression details so the user
can investigate.
  </action>
  <acceptance_criteria>
    - `rg -n "<TODO_FILL>" .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` returns 0 matches (every template placeholder is replaced).
    - The summary table contains exactly 9 data rows, each with numeric values for conforming pre/post, review-needed pre/post, missing-added pre/post.
    - Each of the 9 `### Part N` sections has its four bullet points filled with concrete numeric values and text — no empty bullets.
    - The "Pre-Phase-4 baseline source" section has exactly one `[x]` (or equivalent) marker selected.
    - The Phase closure statement is either fully signed (no regressions) OR the executor returned `## VERIFICATION BLOCKED` with the regression list instead of signing.
    - `python3 -c "import re; txt=open('.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md').read(); assert 'TODO_FILL' not in txt, 'template placeholders still present'; print('ok')"` prints `ok`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer manual run -> 07-03-VER-ARTIFACTS/ captures | The executor agent must not fabricate pipeline output. Real captures are the only acceptable evidence. |
| Pre-Phase-4 baseline source -> summary table `(pre)` columns | The baseline numbers drive the regression verdict. Sourcing them from a wrong commit silently shifts the gate. |
| VERIFICATION.md -> phase closure | Phase 7 cannot close without a signed closure statement. A premature sign-off hides regressions. |
| ground_truth.json files -> pipeline input | These are canonical references. This plan's tasks must never edit them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | T (tampering) | 07-03-VER-ARTIFACTS/part*-run.txt | mitigate | Captures are raw stdout+stderr from `tee`; any edit is a human action, and the acceptance criteria require the executor never fabricate them. |
| T-07-12 | T (tampering) | assets/part*/ground_truth.json | mitigate | Plan files_modified list does not include any ground_truth.json path; acceptance criteria in the SUMMARY.md must confirm no such file changed during the plan. |
| T-07-13 | R (repudiation) | 07-VERIFICATION.md closure | mitigate | Closure statement carries developer name, date, and git short SHA — a forensic trail. |
| T-07-14 | I (information disclosure) | tests/* and assets/* | n/a | No customer PII or secrets touched. |
| T-07-15 | D (denial of service) | manual pipeline runs | accept | The 9-part re-run takes minutes per part; this is inherent to the manual verification gate and is not mitigated. |
| T-07-16 | E (elevation of privilege) | pipeline invocations | n/a | `python run.py` runs with the same privileges as the developer shell. No sudo, no network writes. |
</threat_model>

<verification>
Run:
- `ls -la .planning/phases/07-regression-tests-and-verification/07-03-VER-ARTIFACTS/ | wc -l` (>= 11: README + 9 part files + `.` + `..`)
- `test -f .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md`
- `! grep -q "TODO_FILL" .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` (zero unfilled placeholders at plan close)
- `git status --porcelain assets/*/ground_truth.json` (empty output — no ground truth changes)
- `uv run pytest tests/ -x` (full suite still passes after the manual run)
</verification>

<success_criteria>
- All 9 parts have a committed raw-capture artifact.
- 07-VERIFICATION.md is fully populated with pre/post numeric counts and a per-part verdict.
- Any previously-passing regression is flagged and the closure statement is withheld until the regression is resolved.
- No `ground_truth.json` file is modified by this plan.
- Phase 7 is closeable once the closure statement is signed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-regression-tests-and-verification/07-03-SUMMARY.md`
</output>
