---
phase: 07-regression-tests-and-verification
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - tests/fixtures/phase7_algorithm_only/README.md
  - tests/fixtures/phase7_algorithm_only/part1-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part2-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part3-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part4-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part5-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part6-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part7-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part8-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part9-debug-report.json
  - .planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md
  - tests/test_phase7_benchmark.py
  - .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
autonomous: true
requirements: [TST-02, VER-01]
test_ids:
  - tests/test_phase7_benchmark.py::TestCrossPartBenchmark
  - tests/test_phase6_asset_regression.py::TestPhase6AssetInvariants
must_haves:
  truths:
    - "Phase 7's benchmark and verification baseline is derived from algorithm-only standalone reruns at commit c5c19f0, not from mixed-mode web debug-report exports"
    - "Phase 6 historical debug-report assets remain untouched so Phase 6 asset-backed regression tests keep their original evidence corpus"
    - "07-VERIFICATION.md explicitly separates parity artifacts from any remaining true regression after the algorithm-only baseline is applied"
  artifacts:
    - path: "tests/fixtures/phase7_algorithm_only/"
      provides: "Dedicated algorithm-only debug-report fixtures for part1 through part9 captured from commit c5c19f0"
      contains: "part1-debug-report.json"
    - path: ".planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md"
      provides: "Benchmark and verification provenance for the corrected algorithm-only baseline"
      contains: "## Baseline counts"
    - path: ".planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md"
      provides: "Parity-corrected verification report that identifies which parts remain genuinely regressed"
      contains: "## Verification parity restored"
---

<objective>
Repair the Phase 7 baseline contract without rewriting the earlier plans.
The current benchmark and verification comparison were anchored to
`assets/debug_report_partN.json`, but those files mix historical web-export
behavior with Phase 6 corpus evidence and therefore are not a clean
algorithm-only baseline for Part 1 and Part 8.

This plan creates a dedicated algorithm-only baseline fixture set from commit
`c5c19f0`, rewires the cross-part benchmark to use that fixture set instead
of the shared Phase 6 debug-report assets, and updates `07-VERIFICATION.md`
so parity artifacts are distinguished from any still-real regression.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-regression-tests-and-verification/07-CONTEXT.md
@.planning/phases/07-regression-tests-and-verification/07-RESEARCH.md
@.planning/phases/07-regression-tests-and-verification/07-02-PLAN.md
@.planning/phases/07-regression-tests-and-verification/07-02-SUMMARY.md
@.planning/phases/07-regression-tests-and-verification/07-02-BASELINE-DERIVATION.md
@.planning/phases/07-regression-tests-and-verification/07-03-PLAN.md
@.planning/phases/07-regression-tests-and-verification/07-03-SUMMARY.md
@.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
@run.py
@delta_preservation/cli.py
@shop/tasks.py
@delta_preservation/evaluation/conformance.py
@tests/test_phase7_benchmark.py
@tests/test_phase6_asset_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture a dedicated algorithm-only baseline fixture set from commit c5c19f0</name>
  <files>
tests/fixtures/phase7_algorithm_only/README.md,
tests/fixtures/phase7_algorithm_only/part1-debug-report.json,
tests/fixtures/phase7_algorithm_only/part2-debug-report.json,
tests/fixtures/phase7_algorithm_only/part3-debug-report.json,
tests/fixtures/phase7_algorithm_only/part4-debug-report.json,
tests/fixtures/phase7_algorithm_only/part5-debug-report.json,
tests/fixtures/phase7_algorithm_only/part6-debug-report.json,
tests/fixtures/phase7_algorithm_only/part7-debug-report.json,
tests/fixtures/phase7_algorithm_only/part8-debug-report.json,
tests/fixtures/phase7_algorithm_only/part9-debug-report.json,
.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md
  </files>
  <read_first>
run.py,
delta_preservation/cli.py,
delta_preservation/evaluation/conformance.py,
delta_preservation/types.py,
shop/services/review.py,
.planning/phases/07-regression-tests-and-verification/07-CONTEXT.md,
.planning/phases/07-regression-tests-and-verification/07-RESEARCH.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
tests/test_phase6_asset_regression.py
  </read_first>
  <action>
Create a new fixture directory `tests/fixtures/phase7_algorithm_only/` that is
owned by Phase 7 and does not reuse or overwrite `assets/debug_report_partN.json`.

1. Add `tests/fixtures/phase7_algorithm_only/README.md` documenting that these
   fixtures are algorithm-only standalone benchmark inputs produced from
   historical commit `c5c19f0` using `uv run python run.py partN`, then
   normalized into a Phase 7-owned debug-report-like shape.

2. Materialize a detached temporary worktree at commit `c5c19f0` and, from that
   worktree, run the standalone pipeline for every part `part1` through `part9`.
   For each resulting `delta_packet.json`, generate a committed Phase 7 fixture
   at `tests/fixtures/phase7_algorithm_only/partN-debug-report.json` with these
   exact top-level keys:
   - `baseline_commit`
   - `packet_run_id`
   - `part_name`
   - `items`
   - `missing_added_truth_indexes`
   - `conforming_count`
   - `review_needed_count`

   Build `missing_added_truth_indexes` from the packet itself by validating each
   raw item as `DeltaItem` and reusing the review-service truth-claiming helper
   (`shop.services.review._load_missing_added_truth_items`) against the packet's
   `truth_fixture_key` / `part_name`. Do not require a database or web-run
   export to generate the fixture. The committed artifact must therefore remain
   algorithm-only while still exposing the `missing_added_truth_indexes` signal
   that `tests/test_phase7_benchmark.py` asserts.

3. Write
   `.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md`
   with:
   - the exact provenance line `Baseline commit: c5c19f0`
   - the exact capture method (`detached git worktree` + `uv run python run.py partN`)
   - a `## Baseline counts` section listing `part1` through `part9` with
     `conforming`, `review_needed`, and `missing_added_truth_indexes` counts
     derived from the new Phase 7 fixture files
   - an explicit statement that `assets/debug_report_part8.json` and
     `assets/debug_report_part9.json` remain historical Phase 6 corpus evidence
     and were intentionally left untouched

4. Remove the temporary worktree after the fixture set and baseline doc are
   created. Do not modify any `ground_truth.json` file. Do not modify
   `assets/debug_report_partN.json`.
  </action>
  <acceptance_criteria>
    - `tests/fixtures/phase7_algorithm_only/README.md` exists and contains the string `Baseline commit: c5c19f0`.
    - `tests/fixtures/phase7_algorithm_only/` contains exactly 9 fixture files named `part1-debug-report.json` through `part9-debug-report.json`.
    - Every fixture file contains the JSON keys `"items"` and `"missing_added_truth_indexes"`.
    - Every fixture file contains the JSON keys `"conforming_count"` and `"review_needed_count"`.
    - `.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md` exists and contains the heading `## Baseline counts`.
    - The baseline doc contains exactly 9 part rows, one for each of `part1` through `part9`.
    - `git diff --name-only -- assets/debug_report_part1.json assets/debug_report_part2.json assets/debug_report_part3.json assets/debug_report_part4.json assets/debug_report_part5.json assets/debug_report_part6.json assets/debug_report_part7.json assets/debug_report_part8.json assets/debug_report_part9.json` produces no output.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewire the benchmark and verification report to the algorithm-only baseline</name>
  <files>
tests/test_phase7_benchmark.py,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
  </files>
  <read_first>
tests/test_phase7_benchmark.py,
tests/test_phase6_asset_regression.py,
tests/fixtures/phase7_algorithm_only/README.md,
tests/fixtures/phase7_algorithm_only/part1-debug-report.json,
tests/fixtures/phase7_algorithm_only/part8-debug-report.json,
tests/fixtures/phase7_algorithm_only/part9-debug-report.json,
.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
.planning/phases/07-regression-tests-and-verification/07-03-SUMMARY.md
  </read_first>
  <action>
Update `tests/test_phase7_benchmark.py` so it no longer reads from
`assets/debug_report_partN.json`. Replace the input path with
`tests/fixtures/phase7_algorithm_only/partN-debug-report.json`, keep the
benchmark snapshot-based and fast, and update the module docstring plus the
baseline provenance comment to cite
`07-04-ALGORITHM-ONLY-BASELINE.md` instead of `07-02-BASELINE-DERIVATION.md`.
Preserve the directional assertions (`conforming >= min_conforming`,
`missing_added_truth_indexes <= max_missing_added`) using the dedicated Phase 7
fixture set so Phase 6 asset-backed tests remain decoupled.

Then update `07-VERIFICATION.md` so its baseline source and verdict logic are
anchored to the new algorithm-only fixture set from Task 1. Add a new section
named `## Verification parity restored` that:
  - states that the authoritative baseline is the standalone `c5c19f0`
    fixture set, not the web-exported `assets/debug_report_partN.json`
  - explicitly names whether Part 1 and Part 8 were parity artifacts under the
    old mixed-mode baseline
  - leaves any still-real regression called out by part number, with Part 3
    remaining blocked if it still regresses against the corrected baseline

Do not change the Phase 6 historical asset tests or their asset inputs.
Do not rebaseline away a genuine regression. The corrected verification report
must distinguish `parity artifact resolved` from `true regression remains`.
  </action>
  <acceptance_criteria>
    - `tests/test_phase7_benchmark.py` contains the string `phase7_algorithm_only`.
    - `tests/test_phase7_benchmark.py` does not contain the string `assets/debug_report_part`.
    - `tests/test_phase7_benchmark.py` cites `07-04-ALGORITHM-ONLY-BASELINE.md`.
    - `uv run pytest tests/test_phase7_benchmark.py -x` exits 0.
    - `uv run pytest tests/test_phase6_asset_regression.py -x` exits 0.
    - `.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` contains the heading `## Verification parity restored`.
    - `.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` contains the string `true regression remains` or the string `zero remaining true regressions`.
    - `.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` contains the string `algorithm-only fixture set`.
    - `.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` does not contain the string `Option B (modified) — Post-Phase-6 committed snapshots`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the dedicated Phase 7 benchmark, then the Phase 6 asset-backed regression
file, and confirm the verification report clearly separates parity artifacts
from genuine regressions without touching the canonical ground-truth fixtures.
</verification>

<success_criteria>
- Phase 7 benchmark inputs are owned by Phase 7 and provenance-locked to
  algorithm-only standalone reruns at `c5c19f0`.
- Phase 6 historical debug-report assets remain unchanged and their asset-backed
  regression tests still pass.
- `07-VERIFICATION.md` is parity-corrected and no longer treats mixed-mode
  baseline differences as clean algorithm regressions.
</success_criteria>
