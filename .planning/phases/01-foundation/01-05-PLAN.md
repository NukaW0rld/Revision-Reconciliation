---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: [01-03, 01-04]
files_modified:
  - shop/routers/setup.py
  - shop/templates/setup/step1_shop_name.html
  - shop/templates/setup/step2_password.html
  - shop/templates/setup/step3_engineer.html
  - shop/templates/setup/wizard_layout.html
autonomous: true
requirements: [SETUP-01, AUTH-01]

must_haves:
  truths:
    - "Any request to / or /dashboard when setup_complete=False redirects to /setup/step1"
    - "Admin cannot skip from step 1 to step 3 by direct URL navigation"
    - "Step 1 saves shop name to shop_config; step 2 changes admin password and blocks reuse of default; step 3 creates first engineer account"
    - "Mid-wizard resume: closing browser and revisiting /setup redirects to the first incomplete step"
    - "Step 4 is not accessible until steps 1-3 are complete (wizard_step < 3 redirects back)"
    - "Setup middleware exempts /setup/*, /static/*, /login, /logout from the redirect"
  artifacts:
    - path: "shop/routers/setup.py"
      provides: "GET+POST /setup/step1, step2, step3; wizard step ordering logic"
      exports: [router]
    - path: "shop/templates/setup/wizard_layout.html"
      provides: "Shared wizard layout with step progress indicator"
    - path: "shop/templates/setup/step1_shop_name.html"
      provides: "Step 1: shop name form"
    - path: "shop/templates/setup/step2_password.html"
      provides: "Step 2: admin password change form"
    - path: "shop/templates/setup/step3_engineer.html"
      provides: "Step 3: first engineer account form"
  key_links:
    - from: "shop/middleware/setup_guard.py"
      to: "shop/models.py:ShopConfig.setup_complete"
      via: "db.query(ShopConfig).filter(ShopConfig.id==1).first().setup_complete"
      pattern: "setup_complete"
    - from: "shop/routers/setup.py:GET /setup/step{N}"
      to: "shop/models.py:ShopConfig.wizard_step"
      via: "if wizard_step < N-1: redirect to first incomplete step"
      pattern: "wizard_step"
    - from: "shop/app.py:create_app"
      to: "shop/routers/setup.py:router"
      via: "app.include_router(setup.router, prefix='/setup')"
      pattern: "include_router.*setup"
---

<objective>
Build the setup wizard middleware guard and steps 1-3: shop name, admin password change, and first engineer account creation. After this plan, the undismissable setup guard is in effect and the first three wizard steps are completable.

Purpose: SETUP-01 (undismissable wizard) is one of the most critical Phase 1 requirements — the system must not allow any authenticated operation until configuration is complete.
Output: Working wizard middleware + steps 1-3 with step-ordering enforcement and mid-wizard resume.
</objective>

<execution_context>
@/home/khoa2/.claude/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 -->

From shop/models.py:
```python
class ShopConfig(Base):
    __tablename__ = "shop_config"
    id: Mapped[int]           # always 1
    shop_name: Mapped[str]    # "" until step 1
    setup_complete: Mapped[bool]  # False until all 4 steps done
    column_mapping: Mapped[dict]  # {} until step 4
    wizard_step: Mapped[int]  # 0=not started, 1=step1 done, 2=step2 done, 3=step3 done, 4=complete

class User(Base):
    id: Mapped[int]; email: Mapped[str]; hashed_password: Mapped[str]
    role: Mapped[str]; is_active: Mapped[bool]
```

From shop/services/auth.py:
```python
def hash_password(plain: str) -> str: ...
def verify_password(plain: str, hashed: str) -> bool: ...
```

From shop/middleware/setup_guard.py (Plan 02):
# Middleware already registered; exempts /setup, /static, /login, /logout
# Uses HX-Redirect for HTMX requests; 302 for plain browser
```

<!-- Wizard step logic -->
Step ordering rule (in each route GET handler):
  - wizard_step = config.wizard_step (from ShopConfig singleton)
  - If accessing step N and wizard_step < N-1: redirect to /setup/step{wizard_step+1}
  - Can navigate BACK to completed steps (wizard_step >= N-1 means step N is accessible)
  - Can NOT skip forward

Mid-wizard resume:
  - GET /setup/ or GET /setup with no step: redirect to /setup/step{wizard_step+1}
  - Each completed step sets wizard_step to its step number immediately
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Setup wizard router — steps 1 through 3</name>
  <files>shop/routers/setup.py</files>
  <behavior>
    - GET /setup/step1: returns 200 with step 1 form (shop name); no auth required
    - POST /setup/step1 with shop_name: saves to ShopConfig; sets wizard_step=1; redirects to /setup/step2
    - GET /setup/step2: if wizard_step < 1, redirects to /setup/step1
    - POST /setup/step2 with new_password, confirm_password: validates they match; validates new_password != default "changeme"; updates admin hashed_password; sets wizard_step=2; redirects to /setup/step3
    - POST /setup/step2 where new_password == "changeme": returns step2 form with error "Cannot reuse the default password"
    - GET /setup/step3: if wizard_step < 2, redirects to /setup/step2
    - POST /setup/step3 with engineer email+password: creates engineer user (role="engineer"); sets wizard_step=3; redirects to /setup/step4
    - POST /setup/step3 with duplicate email: returns step3 form with error, wizard_step NOT advanced
    - GET /setup/ (no step): redirect to /setup/step{wizard_step+1}
    - GET /setup/step1 when wizard_step >= 1: show form pre-filled with current shop_name (allow editing completed steps)
  </behavior>
  <action>
Create `shop/routers/setup.py`:

```python
import logging
from fastapi import APIRouter, Depends, Form, Request
from fastapi.responses import HTMLResponse, RedirectResponse
from sqlalchemy.exc import IntegrityError
from sqlalchemy.orm import Session
from shop.app import templates
from shop.dependencies import get_db
from shop.models import ShopConfig, User
from shop.services.auth import hash_password, verify_password

router = APIRouter()
logger = logging.getLogger(__name__)

ADMIN_DEFAULT_PASSWORD = "changeme"  # Matches ADMIN_DEFAULT_PASSWORD env var default

def _get_or_create_config(db: Session) -> ShopConfig:
    """Get singleton ShopConfig row, creating it if missing."""
    config = db.query(ShopConfig).filter(ShopConfig.id == 1).first()
    if not config:
        config = ShopConfig(id=1)
        db.add(config)
        db.commit()
        db.refresh(config)
    return config

def _get_admin(db: Session) -> User | None:
    return db.query(User).filter(User.role == "admin").first()

def _step_redirect(step: int) -> RedirectResponse:
    return RedirectResponse(f"/setup/step{step}", status_code=302)

@router.get("/", response_class=HTMLResponse)
def setup_root(db: Session = Depends(get_db)):
    config = _get_or_create_config(db)
    next_step = min(config.wizard_step + 1, 4)
    return RedirectResponse(f"/setup/step{next_step}", status_code=302)

@router.get("/step1", response_class=HTMLResponse)
def step1_get(request: Request, db: Session = Depends(get_db)):
    config = _get_or_create_config(db)
    return templates.TemplateResponse(
        "setup/step1_shop_name.html",
        {"request": request, "config": config, "current_step": 1},
    )

@router.post("/step1")
def step1_post(
    shop_name: str = Form(...),
    db: Session = Depends(get_db),
):
    config = _get_or_create_config(db)
    config.shop_name = shop_name.strip()
    if config.wizard_step < 1:
        config.wizard_step = 1
    db.commit()
    return _step_redirect(2)

@router.get("/step2", response_class=HTMLResponse)
def step2_get(request: Request, db: Session = Depends(get_db)):
    config = _get_or_create_config(db)
    if config.wizard_step < 1:
        return _step_redirect(1)
    return templates.TemplateResponse(
        "setup/step2_password.html",
        {"request": request, "config": config, "current_step": 2},
    )

@router.post("/step2", response_class=HTMLResponse)
def step2_post(
    request: Request,
    new_password: str = Form(...),
    confirm_password: str = Form(...),
    db: Session = Depends(get_db),
):
    config = _get_or_create_config(db)
    if config.wizard_step < 1:
        return _step_redirect(1)
    if new_password != confirm_password:
        return templates.TemplateResponse(
            "setup/step2_password.html",
            {"request": request, "config": config, "current_step": 2,
             "error": "Passwords do not match"},
        )
    if new_password == ADMIN_DEFAULT_PASSWORD:
        return templates.TemplateResponse(
            "setup/step2_password.html",
            {"request": request, "config": config, "current_step": 2,
             "error": "Cannot reuse the default password"},
        )
    admin = _get_admin(db)
    if admin:
        admin.hashed_password = hash_password(new_password)
        db.commit()
    if config.wizard_step < 2:
        config.wizard_step = 2
        db.commit()
    return _step_redirect(3)

@router.get("/step3", response_class=HTMLResponse)
def step3_get(request: Request, db: Session = Depends(get_db)):
    config = _get_or_create_config(db)
    if config.wizard_step < 2:
        return _step_redirect(config.wizard_step + 1)
    return templates.TemplateResponse(
        "setup/step3_engineer.html",
        {"request": request, "config": config, "current_step": 3},
    )

@router.post("/step3", response_class=HTMLResponse)
def step3_post(
    request: Request,
    engineer_email: str = Form(...),
    engineer_password: str = Form(...),
    db: Session = Depends(get_db),
):
    config = _get_or_create_config(db)
    if config.wizard_step < 2:
        return _step_redirect(config.wizard_step + 1)
    new_engineer = User(
        email=engineer_email,
        hashed_password=hash_password(engineer_password),
        role="engineer",
        is_active=True,
    )
    db.add(new_engineer)
    try:
        db.commit()
    except IntegrityError:
        db.rollback()
        return templates.TemplateResponse(
            "setup/step3_engineer.html",
            {"request": request, "config": config, "current_step": 3,
             "error": f"Email '{engineer_email}' already exists"},
        )
    if config.wizard_step < 3:
        config.wizard_step = 3
        db.commit()
    return _step_redirect(4)
```

Register in app.py `create_app()`:
```python
from shop.routers import auth, admin, setup
app.include_router(setup.router, prefix="/setup")
```
(Add alongside existing router registrations; keep auth and admin registrations.)
  </action>
  <verify>
    <automated>cd /home/khoa2/delta-preservation && uv run pytest tests/test_setup.py::test_wizard_intercepts_all_routes -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>GET /setup/step2 with wizard_step=0 redirects to /setup/step1; POST /setup/step2 with "changeme" returns error; steps 1-3 complete and advance wizard_step correctly</done>
</task>

<task type="auto">
  <name>Task 2: Wizard layout and step 1-3 templates</name>
  <files>shop/templates/setup/wizard_layout.html, shop/templates/setup/step1_shop_name.html, shop/templates/setup/step2_password.html, shop/templates/setup/step3_engineer.html</files>
  <action>
Create `shop/templates/setup/wizard_layout.html` — a shared layout block for all wizard steps, extending base.html:
```html
{% extends "base.html" %}
{% block title %}Setup Wizard — Step {{ current_step }} of 4{% endblock %}
{% block content %}
<div class="min-h-screen flex flex-col items-center justify-center p-4">
    <div class="card w-full max-w-lg bg-base-100 shadow-xl">
        <div class="card-body">
            <!-- Step progress -->
            <ul class="steps w-full mb-6">
                {% for step_num, step_label in [(1,'Shop'), (2,'Password'), (3,'Engineer'), (4,'Form 3')] %}
                <li class="step {% if current_step > step_num %}step-primary{% elif current_step == step_num %}step-primary{% endif %}">
                    {{ step_label }}
                </li>
                {% endfor %}
            </ul>
            {% if error %}
            <div class="alert alert-error mb-4">
                <span>{{ error }}</span>
            </div>
            {% endif %}
            {% block step_content %}{% endblock %}
        </div>
    </div>
</div>
{% endblock %}
```

Create `shop/templates/setup/step1_shop_name.html`:
```html
{% extends "setup/wizard_layout.html" %}
{% block step_content %}
<h2 class="text-xl font-bold mb-4">Step 1: Shop Name</h2>
<form method="post" action="/setup/step1">
    <div class="form-control mb-4">
        <label class="label"><span class="label-text">Shop Name</span></label>
        <input type="text" name="shop_name" class="input input-bordered"
               value="{{ config.shop_name or '' }}" required placeholder="e.g. Acme Quality Lab">
    </div>
    <button type="submit" class="btn btn-primary w-full">Continue</button>
</form>
{% endblock %}
```

Create `shop/templates/setup/step2_password.html`:
```html
{% extends "setup/wizard_layout.html" %}
{% block step_content %}
<h2 class="text-xl font-bold mb-4">Step 2: Set Admin Password</h2>
<p class="text-sm text-base-content/70 mb-4">The default password must be changed before proceeding.</p>
<form method="post" action="/setup/step2">
    <div class="form-control mb-3">
        <label class="label"><span class="label-text">New Password</span></label>
        <input type="password" name="new_password" class="input input-bordered" required minlength="8">
    </div>
    <div class="form-control mb-4">
        <label class="label"><span class="label-text">Confirm Password</span></label>
        <input type="password" name="confirm_password" class="input input-bordered" required minlength="8">
    </div>
    <button type="submit" class="btn btn-primary w-full">Continue</button>
</form>
{% endblock %}
```

Create `shop/templates/setup/step3_engineer.html`:
```html
{% extends "setup/wizard_layout.html" %}
{% block step_content %}
<h2 class="text-xl font-bold mb-4">Step 3: Create First Engineer Account</h2>
<form method="post" action="/setup/step3">
    <div class="form-control mb-3">
        <label class="label"><span class="label-text">Engineer Email</span></label>
        <input type="email" name="engineer_email" class="input input-bordered" required>
    </div>
    <div class="form-control mb-4">
        <label class="label"><span class="label-text">Temporary Password</span></label>
        <input type="password" name="engineer_password" class="input input-bordered" required minlength="8">
    </div>
    <button type="submit" class="btn btn-primary w-full">Continue</button>
</form>
{% endblock %}
```

Note: All wizard forms use standard HTML POST (not HTMX). The wizard is a full-page flow; HTMX partial swaps would conflict with browser back/forward and step validation redirects.
  </action>
  <verify>
    <automated>cd /home/khoa2/delta-preservation && uv run python -c "from jinja2 import Environment, FileSystemLoader; env = Environment(loader=FileSystemLoader('shop/templates')); [env.get_template(f'setup/{t}') for t in ['wizard_layout.html','step1_shop_name.html','step2_password.html','step3_engineer.html']]; print('wizard templates OK')"</automated>
  </verify>
  <done>All 4 wizard templates parse; step 1-3 extend wizard_layout.html; progress indicator renders current_step correctly</done>
</task>

</tasks>

<verification>
After Wave 3 (Plan 05) complete:
- `uv run pytest tests/test_setup.py::test_wizard_intercepts_all_routes -x -q` passes
- Direct URL GET /setup/step3 with wizard_step=0 redirects to /setup/step1
- POST /setup/step2 with "changeme" returns 200 with error message
- POST /setup/step1 with shop name advances wizard_step to 1 in DB
</verification>

<success_criteria>
- Setup router registered at /setup prefix
- Steps 1-3 complete and advance wizard_step correctly
- Forward-skip prevention enforced in each GET handler
- Mid-wizard resume works via /setup/ root redirect
- Wizard layout shared across all step templates
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` with:
- Routes created for steps 1-3
- Wizard step ordering logic description
- Test results for test_setup.py
- Note which steps are fully functional vs stub
</output>
