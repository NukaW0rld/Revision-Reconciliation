---
phase: 03-review-and-sign-off
plan: 06
type: execute
wave: 5
depends_on: [03-04, 03-05]
files_modified:
  - tests/test_review.py
autonomous: false
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, REVIEW-04, REVIEW-05, REVIEW-06, REVIEW-07, SIGNOFF-01, SIGNOFF-02, SIGNOFF-03]

must_haves:
  truths:
    - "All 10 requirement tests in test_review.py pass (none xfail, none skip)"
    - "Full pytest suite is green"
    - "Review queue accessible from completed run status page"
    - "Approve and override HTMX saves update card state without page reload"
    - "Sign-off button enables only when all items are resolved"
    - "Sign-off and rollback behave atomically per SIGNOFF-02"
  artifacts:
    - path: "tests/test_review.py"
      provides: "10 passing integration tests"
      min_lines: 150
  key_links:
    - from: "tests/test_review.py"
      to: "all 10 REVIEW-*/SIGNOFF-* requirements"
      via: "pytest collection + execution"
      pattern: "def test_"
---

<objective>
Phase 3 gate: complete all xfail test stubs to passing, run the full suite to confirm nothing regressed, and perform human verification of the review workflow in Docker.

Purpose: Plans 02-05 may have left some test stubs as xfail. This plan finishes implementing any remaining stubs, then the engineer verifies the complete workflow manually in the running Docker container.
Output: All 10 tests in test_review.py passing; Docker human verification approved.
</objective>

<execution_context>
@/home/khoa2/.claude/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-and-sign-off/03-CONTEXT.md
@.planning/phases/03-review-and-sign-off/03-RESEARCH.md
@.planning/phases/03-review-and-sign-off/03-VALIDATION.md
@.planning/phases/03-review-and-sign-off/03-04-SUMMARY.md
@.planning/phases/03-review-and-sign-off/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement any remaining xfail stubs to passing</name>
  <files>tests/test_review.py</files>
  <action>
Run `pytest tests/test_review.py -v` to see which tests are still xfail. Implement each remaining xfail stub by:
1. Removing `@pytest.mark.xfail(strict=False, ...)` decorator
2. Implementing the test body against the actual routes/services built in Plans 02-05

Required behaviors per test (follow the established conftest.py fixture patterns):

**test_review_queue_loads (REVIEW-01):** If not already passing from Plan 02:
- Create a Run with status="completed" and a real tmp delta_packet.json (3 DeltaItems, use tmp_path)
- Seed Run in db; log in as engineer (POST /auth/login)
- GET /review/{run_id}; assert status_code == 200
- Assert response text contains each char_no

**test_review_item_card_html (REVIEW-02):** If not already passing from Plan 03:
- Create ReviewItem in db; log in as engineer
- GET response from /review/{run_id}; assert HTML contains char_no, pipeline_classification
- POST approve; assert response contains "Approved" badge

**test_approve_item (REVIEW-03):** If not already passing from Plan 03:
- POST /review/{run_id}/items/{char_no}/approve as engineer
- Query ReviewItem from db; assert reviewer_decision == "approved"

**test_override_requires_note (REVIEW-04):** If not already passing from Plan 03:
- POST override with override_note="" → assert 422
- POST override with override_note="valid reason" → assert 200; ReviewItem.reviewer_decision == "overridden"

**test_review_state_persisted (REVIEW-05):** If not already passing from Plan 02:
- Open review queue twice; assert ReviewItem count equals DeltaItem count (not doubled)
- Approve one item; reload queue; assert that item still shows approved

**test_admin_can_reassign (REVIEW-06):** If not already passing from Plan 04:
- Admin POST /review/{id}/reassign with new reviewer_id → assert 302
- Engineer POST /review/{id}/reassign → assert 403

**test_review_counts (REVIEW-07):** If not already passing from Plan 02:
- Open review queue with 3 items: approve 1, leave 2 pending
- GET /review/{id}; assert response text contains "2" (pending) and "1" (approved)

**test_sign_off_gate (SIGNOFF-01):** If not already passing from Plan 04:
- Queue with 1 pending item → GET /review/{id} → assert sign-off button has "disabled" attribute
- Approve all items → GET /review/{id} → assert sign-off button does NOT have "disabled"

**test_sign_off_rollback (SIGNOFF-02):** If not already passing from Plan 05:
- Patch db.commit to raise on second call → attempt_sign_off returns False → Run.status == "reviewing"

**test_signed_off_immutable (SIGNOFF-03):** If not already passing from Plan 05:
- First attempt_sign_off succeeds → signed_at set
- Second attempt_sign_off returns False → signed_at unchanged

After implementing all stubs, run:
```bash
pytest tests/test_review.py -v
pytest -q
```

All 10 tests in test_review.py must pass (not xfail — actually pass). Full suite must be green.
  </action>
  <verify>
    <automated>cd /home/khoa2/delta-preservation && pytest tests/test_review.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 tests in test_review.py PASSED (no xfail, no skip, no error); `pytest -q` full suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 review and sign-off workflow:
- Review queue at /review/{run_id} with per-item approve/override HTMX saves
- Progress counter OOB update after each save
- Resolved cards show green border + status badge
- Sign-off gate (button disabled until all items resolved)
- Confirmation modal before sign-off POST
- Sign-off generating status page with SSE
- Admin reviewer reassignment on run status page
- Snippet serving at /review/{run_id}/snippets/{filename}
- Click-to-enlarge modal overlay for snippet images
  </what-built>
  <how-to-verify>
1. Start Docker: `docker compose up --build` (from project root)
2. Log in as admin at http://localhost:8000
3. Submit a real pipeline run (Rev A PDF + Rev B PDF + Form 3 Excel)
4. Wait for pipeline to complete (SSE progress page)
5. Click "Review" on the completed run (or navigate to /review/{run_id})
6. Verify: all characteristics are listed ordered by char_no ascending
7. Verify: each card shows Rev A snippet (top) and Rev B snippet (bottom) with labels
8. Verify: pending/approved/overridden counts in header are accurate
9. Click "Approve" on 2-3 items — verify card turns green with badge, no page reload, counts update
10. Click "Override" on 1 item — enter a reason, select a classification — verify card shows warning badge + override note
11. Try Override with empty note — verify error shown in card
12. Try clicking Sign Off when items are still pending — verify button is disabled or shows pending count
13. Approve/override all remaining items
14. Verify sign-off button is now enabled
15. Click Sign Off — verify confirmation modal appears with correct metadata
16. Click Confirm — verify redirect to generating page with spinner
17. Verify redirect to /runs/{run_id} with "signed off" success banner
18. Navigate back to /review/{run_id} — verify it redirects to /runs/{run_id} (no re-review of signed run)
19. As admin: verify reassign form appears on /runs/{run_id} status page
20. Click any snippet image — verify full-size modal opens; press Escape or Close to dismiss
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/test_review.py -v` shows 10 PASSED
- `pytest -q` full suite green
- Docker e2e human verification approved
</verification>

<success_criteria>
- All 10 REVIEW-* and SIGNOFF-* tests passing (not xfail)
- Full pytest suite green
- Engineer can complete a full review workflow end-to-end in Docker: queue → approve/override all → sign off → signed confirmation
- Admin can reassign a reviewer
- Snippet images display correctly; click-to-enlarge works
- Sign-off gate blocks action when items are pending
- Rollback behavior confirmed by automated test (no manual rollback verification needed in Docker)
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-and-sign-off/03-06-SUMMARY.md`
</output>
