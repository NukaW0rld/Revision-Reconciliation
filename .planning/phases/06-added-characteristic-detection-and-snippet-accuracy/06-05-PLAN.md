---
phase: 06-added-characteristic-detection-and-snippet-accuracy
plan: 05
type: execute
wave: 5
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - delta_preservation/reconcile/classify.py
  - delta_preservation/reconcile/match.py
  - tests/test_added_detection_phase6.py
  - tests/test_phase6_asset_regression.py
autonomous: true
requirements: [ADD-01, ADD-02]
test_ids:
  - tests/test_added_detection_phase6.py::TestGroupedAddedEvidence
  - tests/test_phase6_asset_regression.py::TestPhase6Part8Exemplars
  - tests/test_phase6_asset_regression.py::TestPhase6Part9DuplicateAddedRows
must_haves:
  truths:
    - "Fresh part8 pipeline runs no longer merge `⌰ .015 B` with `Ø10.000±.001`, and `⌰ .002 A` remains available to the added detector"
    - "Fresh part9 pipeline runs emit both physical instances of each duplicate added callout instead of collapsing them by text"
    - "Removed+added reconciliation does not consume legitimate duplicate added rows in Part 9"
    - "The fixes improve real end-to-end coverage without reintroducing fragment false positives"
  artifacts:
    - path: ".planning/debug/phase06-added-e2e-regressions.md"
      provides: "Verified root causes from fresh part8/part9 and 9-part reruns"
      contains: "Part 8 over-grouping and Part 9 text-only dedupe evidence"
---

<objective>
Close the real Phase 06 verification gaps found by automated UAT. The goal is not another synthetic helper tweak; it is to make the end-to-end pipeline stop collapsing true added rows and stop stealing them via weak matching/reconciliation paths, while preserving the fragment-suppression gains already achieved.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/phase06-added-e2e-regressions.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-UAT.md
@delta_preservation/reconcile/classify.py
@delta_preservation/reconcile/match.py
@tests/test_added_detection_phase6.py
@tests/test_phase6_asset_regression.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Part 8 row grouping so distinct same-row annotations stay distinct</name>
  <files>delta_preservation/reconcile/classify.py, tests/test_added_detection_phase6.py, tests/test_phase6_asset_regression.py</files>
  <read_first>.planning/debug/phase06-added-e2e-regressions.md, delta_preservation/reconcile/classify.py, assets/part8/revB.pdf</read_first>
  <action>
Add a regression that proves the fresh Part 8 added-detector path does NOT merge `⌰ .015 B` with `Ø10.000±.001`, and that the grouped-text output preserves those as separate rows.

Then narrow the Pass 0 GD&T companion collection logic so same-row companions are collected from the actual FCF neighborhood, not from any span that happens to fall within the current 300 pt sweep. The fix must remain generic and geometry-driven; do not hardcode Part 8 strings or coordinates.

Re-run the fresh Part 8 pipeline and confirm the merged text `⌰ .015  B Ø10.000±.001` no longer appears as a single added row.
</action>
  <acceptance_criteria>
    - A new regression fails on current code and passes after the fix.
    - `uv run python run.py part8 --out_dir <tmp>` no longer emits a merged `⌰ .015  B Ø10.000±.001` added row.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve distinct duplicate added rows through detection and packet assembly</name>
  <files>delta_preservation/reconcile/classify.py, tests/test_added_detection_phase6.py, tests/test_phase6_asset_regression.py</files>
  <read_first>.planning/debug/phase06-added-e2e-regressions.md, delta_preservation/reconcile/classify.py, assets/part9/revB.pdf</read_first>
  <action>
Add a regression that proves two physically distinct duplicates with identical grouped text survive the standard added-detection pass as two candidates.

Replace the text-only dedupe key with a grouped-evidence identity that includes geometry, so duplicates at different locations are preserved while true companion-span rediscovery is still deduped.

Re-run the fresh Part 9 detection path and confirm it emits two instances each of `Ø.250 ±.008`, `⌖∅ .015 D H`, and `↧ .50 ±.05` before packet evaluation.
</action>
  <acceptance_criteria>
    - The dedupe regression fails on current code and passes after the fix.
    - Fresh Part 9 detection emits 6 duplicate-instance rows across the 3 known duplicate texts, not 3.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stop weak existing-match capture and over-permissive removed+added reconciliation from hiding real added rows</name>
  <files>delta_preservation/reconcile/match.py, delta_preservation/reconcile/classify.py, tests/test_phase6_asset_regression.py</files>
  <read_first>.planning/debug/phase06-added-e2e-regressions.md, delta_preservation/reconcile/match.py, delta_preservation/reconcile/classify.py</read_first>
  <action>
Add focused regressions for the two verified end-to-end leaks:

- Part 8: existing Form 3 `char 5` must not claim the real added `⌰ .002 A` row through a low-confidence semantic mismatch.
- Part 9: removed+added reconciliation must not consume a legitimate duplicate added row using only page + distance + broad type compatibility.

Implement the smallest generic gates needed to block those false captures. Prefer semantic/type compatibility and stronger ownership evidence over raising arbitrary score thresholds.

After the code changes, rerun fresh Part 8 and Part 9 pipelines and confirm the final packets claim the intended added truth indexes rather than hiding them behind changed/removed rows.
</action>
  <acceptance_criteria>
    - A regression proves Part 8 `⌰ .002 A` remains available to added detection.
    - A regression proves Part 9 duplicate added rows survive final packet assembly instead of being reconciled away.
    - Fresh Part 8 and Part 9 packet summaries improve versus the current automated UAT results.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
- `uv run pytest tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x`
- `uv run python run.py part8 --out_dir /tmp/phase6-plan05-part8`
- `uv run python run.py part9 --out_dir /tmp/phase6-plan05-part9`
- Fresh 9-part rerun plus aggregate scoring using the same packet-vs-truth script used in `.planning/debug/phase06-added-e2e-regressions.md`

Success targets:
- Part 8: `missing_added_truth_indexes` improves from `[8, 9, 10]`
- Part 9: duplicate added rows appear as distinct packet rows with distinct claimed truth indexes
- Aggregate: emitted added rows increases materially from `21/35` and missing truth rows decreases from `28`
</verification>

<success_criteria>
- Phase 06 no longer fails its real pipeline verification on Parts 8 and 9.
- Duplicate added rows are preserved by location-aware evidence identity.
- Legitimate added rows are not hidden by weak matching or coarse removed+added reconciliation.
- The fixes generalize across the 9-part corpus and are verified by a fresh rerun, not by fixture edits.
</success_criteria>

<output>
After completion, create `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-05-SUMMARY.md`
</output>
