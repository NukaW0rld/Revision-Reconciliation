---
phase: 06-added-characteristic-detection-and-snippet-accuracy
plan: 04
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - tests/test_phase6_asset_regression.py
autonomous: true
requirements: [ADD-01, ADD-02, SNP-01]
test_ids:
  - tests/test_phase6_asset_regression.py::TestPhase6AssetInvariants
  - tests/test_phase6_asset_regression.py::TestPhase6Part8Exemplars
  - tests/test_phase6_asset_regression.py::TestPhase6Part9DuplicateAddedRows
must_haves:
  truths:
    - "Phase-6 regressions are pinned to exact Part 8 / Part 9 exemplar strings and canonical centers, not vague count caps"
    - "The harness remains read-only with respect to `assets/` and `ground_truth.json`"
    - "Phase-6 helper behavior is exercised through stable public or integration surfaces, not by editing the fixtures"
  artifacts:
    - path: "tests/test_phase6_asset_regression.py"
      provides: "Read-only Part 8 / Part 9 regression harness keyed to checked-in debug artifacts and ground truth"
      contains: "class TestPhase6Part8Exemplars"
---

<objective>
Lock Phase 6 against the real debug corpus without taking ownership of the full 9-part rerun. This plan creates a read-only asset-backed harness tied to the exact Part 8 / Part 9 failure exemplars so later changes cannot quietly reintroduce fragment added rows, duplicate-truth ambiguity, or dropped canonical added rows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-RESEARCH.md
@delta_preservation/reconcile/classify.py
@delta_preservation/evaluation/conformance.py
@tests/test_added_detection_phase6.py
@tests/test_added_truth_selection.py
@assets/debug_report_part8.json
@assets/debug_report_part9.json
@assets/part8/ground_truth.json
@assets/part9/ground_truth.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a read-only asset regression harness keyed to exact Phase 6 exemplars</name>
  <files>tests/test_phase6_asset_regression.py</files>
  <read_first>assets/debug_report_part8.json, assets/debug_report_part9.json, assets/part8/ground_truth.json, assets/part9/ground_truth.json, .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-RESEARCH.md</read_first>
  <action>
Create `tests/test_phase6_asset_regression.py` with three test classes.

`TestPhase6AssetInvariants` must:
- assert the required Part 8 / Part 9 snapshot files exist
- load the exact canonical added rows for Part 8 and Part 9
- assert the known duplicate groups in Part 9 are still present in ground truth by exact text (`Ø.250 ±.008`, `⌖ ∅.015 D H`, `↧.50 ±.05`)

`TestPhase6Part8Exemplars` must:
- pin the exact Part 8 fragment false-positive strings from the checked-in debug report (`.045 A`, `⚪ .005`, and `⌰ .002 A`)
- assert the Phase-6 added-detection path treats the full-callout text as authoritative and does not allow the fragment-only forms to survive as standalone added rows when matched ownership exists
- use the exact `⌰ .002 A` canonical added row as the positive missing-row target

`TestPhase6Part9DuplicateAddedRows` must:
- load the exact duplicate truth centers from `assets/part9/ground_truth.json`
- build packet items from those centers and assert the Phase-6 evaluator claims distinct added-pool tokens for the duplicated rows
- pin the truncated debug-report exemplar `⌖∅` against the expected full canonical text `⌖ ∅.015 D H`

Prefer stable public or integration-level functions (`detect_added_characteristics`, packet assembly helpers, `select_truth_row_for_item`, `evaluate_packet_against_truth`) over direct assertions on private helper implementations.
</action>
  <acceptance_criteria>
    - `tests/test_phase6_asset_regression.py` exists and contains `class TestPhase6AssetInvariants`, `class TestPhase6Part8Exemplars`, and `class TestPhase6Part9DuplicateAddedRows`.
    - `rg -n "\\.045 A|⚪ \\.005|⌰ \\.002 A|⌖∅|⌖ ∅\\.015 D H|Ø\\.250 ±\\.008|↧\\.50 ±\\.05" tests/test_phase6_asset_regression.py` returns the pinned exemplar strings.
    - `uv run pytest tests/test_phase6_asset_regression.py -x` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in debug reports / ground truth -> regression harness | Frozen corpus artifacts become the authoritative exemplar source for future Phase-6 regressions. |
| Harness -> production helpers | The tests must remain stable even if helper internals change, so they should prefer public or integration-level surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | T | `tests/test_phase6_asset_regression.py` | mitigate | Use exact exemplar strings and canonical centers from checked-in assets instead of vague count assertions. |
| T-06-10 | I | `tests/test_phase6_asset_regression.py` | mitigate | Keep the harness read-only; never rewrite `assets/` or `ground_truth.json`, and never depend on regenerated pipeline output. |
</threat_model>

<verification>
Run:
- `uv run pytest tests/test_phase6_asset_regression.py -x`
- `python3 - <<'PY'\nfrom pathlib import Path\nfor path in [\n    'assets/debug_report_part8.json',\n    'assets/debug_report_part9.json',\n    'assets/part8/ground_truth.json',\n    'assets/part9/ground_truth.json',\n]:\n    assert Path(path).exists(), path\nprint('ok')\nPY`
</verification>

<success_criteria>
- Phase 6 is guarded by exact Part 8 / Part 9 exemplar regressions, not by abstract counts.
- The harness stays fast and read-only.
- Later work can refactor internals without losing coverage on the actual failure corpus.
</success_criteria>

<output>
After completion, create `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-04-SUMMARY.md`
</output>
