---
phase: 02-focused-debug-workflow
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - shop/services/review.py
  - shop/routers/review.py
  - shop/routers/runs.py
  - shop/templates/review/queue.html
  - shop/templates/review/_item_card_debug.html
  - shop/templates/review/_signoff_footer.html
  - shop/templates/runs/status.html
  - tests/test_focused_debug_queue.py
  - tests/test_run_status_debug_summary.py
autonomous: true
requirements: [DREV-01, DREV-02, DREV-03, DREV-04]
user_setup: []
must_haves:
  truths:
    - "Admins see a mismatch-first exception queue: only exception rows are visible, ordered mismatch codes/messages lead each card, and heavy diagnostics stay collapsed by default."
    - "All-conforming runs stay on the run-details page with an explicit auto-pass summary and debug-report access instead of opening an empty queue."
    - "The debug UI exposes only the Phase 2 exception outcomes `algorithm_error` and `acceptable_alternate`, and the required fields change with the selected outcome."
  artifacts:
    - path: "shop/templates/runs/status.html"
      provides: "Admin debug evaluation summary and all-conforming run-details CTA"
      contains: "All characteristics auto-passed"
    - path: "shop/templates/review/_item_card_debug.html"
      provides: "Mismatch-first exception card with Phase 2 outcome vocabulary"
      contains: "acceptable_alternate"
    - path: "shop/templates/review/_signoff_footer.html"
      provides: "Exception-only debug export readiness footer"
      contains: "Debug report ready"
    - path: "tests/test_run_status_debug_summary.py"
      provides: "Regression coverage for run-details debug summary and CTA behavior"
      contains: "def test_all_conforming_admin_run_stays_on_status_page"
  key_links:
    - from: "shop/routers/runs.py"
      to: "shop/services/review.py"
      via: "build_run_debug_summary"
      pattern: "build_run_debug_summary"
    - from: "shop/routers/review.py"
      to: "shop/services/review.py"
      via: "item-id keyed debug helpers"
      pattern: "debug_internals_by_item_id"
    - from: "shop/templates/review/_signoff_footer.html"
      to: "shop/routers/review.py"
      via: "debug_submitted and debug_total counts"
      pattern: "debug_submitted"
---

<objective>
Finish the Phase 2 admin experience: run details should summarize auto-passes, and the exception queue should prioritize mismatch triage plus reviewer rationale.

Purpose: The internal debug loop only gets faster if the run-details surface explains auto-pass outcomes and the exception cards surface the minimum high-signal context needed to classify an issue quickly.
Output: run-details debug summary, mismatch-first exception cards, collapsed heavy diagnostics, and exception-only export readiness UX.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-focused-debug-workflow/02-CONTEXT.md
@.planning/phases/02-focused-debug-workflow/02-RESEARCH.md
@.planning/phases/02-focused-debug-workflow/02-VALIDATION.md
@shop/services/review.py
@shop/routers/review.py
@shop/routers/runs.py
@shop/templates/review/queue.html
@shop/templates/review/_item_card_debug.html
@shop/templates/review/_signoff_footer.html
@shop/templates/runs/status.html
@tests/test_runs.py
@tests/test_focused_debug_queue.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration coverage for run-details debug summary and focused exception rendering</name>
  <files>tests/test_focused_debug_queue.py, tests/test_run_status_debug_summary.py</files>
  <read_first>.planning/phases/02-focused-debug-workflow/02-CONTEXT.md, shop/routers/review.py, shop/routers/runs.py, shop/templates/review/queue.html, shop/templates/review/_item_card_debug.html, shop/templates/runs/status.html, tests/test_runs.py</read_first>
  <action>Create `tests/test_run_status_debug_summary.py` to cover admin run-details behavior for three concrete cases: a zero-exception completed run stays on `/runs/{id}` and shows `All characteristics auto-passed`; a mixed run shows explicit conforming vs exception counts plus an `Open Exception Queue` CTA; a fully resolved exception run shows a `Download debug_report.json` link on the status page. Extend `tests/test_focused_debug_queue.py` so the rendered debug queue asserts that mismatch codes/messages appear before diagnostic panels, the debug form exposes only `algorithm_error` and `acceptable_alternate`, and heavy details remain behind closed `<details>` disclosures by default.</action>
  <verify>
    <automated>uv run pytest -q tests/test_focused_debug_queue.py tests/test_run_status_debug_summary.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_run_status_debug_summary.py` contains `def test_all_conforming_admin_run_stays_on_status_page`
    - `tests/test_run_status_debug_summary.py` contains `Open Exception Queue`
    - `tests/test_focused_debug_queue.py` contains `algorithm_error`
    - `tests/test_focused_debug_queue.py` contains `acceptable_alternate`
    - `tests/test_focused_debug_queue.py` contains `<details`
  </acceptance_criteria>
  <done>Regression coverage exists for the run-details debug summary, the all-conforming status-page flow, and the mismatch-first exception card rendering.</done>
</task>

<task type="auto">
  <name>Task 2: Implement run-details debug summary and auto-pass inspection</name>
  <files>shop/services/review.py, shop/routers/runs.py, shop/templates/runs/status.html</files>
  <read_first>shop/services/review.py, shop/routers/runs.py, shop/templates/runs/status.html, .planning/phases/02-focused-debug-workflow/02-CONTEXT.md, tests/test_run_status_debug_summary.py</read_first>
  <action>In `shop/services/review.py`, add `build_run_debug_summary(db: Session, run: Run)` that reuses Phase 2 queue/report helpers to return stable-order `conforming_rows`, `exception_rows`, `resolved_exception_count`, `unresolved_exception_count`, and `debug_report_ready`. In `shop/routers/runs.py`, load this summary for admin users on `completed`, `reviewing`, `warning`, and `signed_off` runs. In `shop/templates/runs/status.html`, add a dedicated admin-only `Debug evaluation summary` card with this exact behavior: when `exception_rows` is empty, show the heading `All characteristics auto-passed`, show the conforming count, keep the user on the status page, and surface `Download debug_report.json` when `debug_report_ready` is true; when exceptions exist, show both conforming and exception counts, keep a visible `Open Exception Queue` button, and add a collapsed `Canonical matches` list that makes auto-passed rows inspectable on the run-details surface by `queue_index`, `char_no`, `pipeline_classification`, and `requirement_revB`.</action>
  <verify>
    <automated>uv run pytest -q tests/test_run_status_debug_summary.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `shop/services/review.py` contains `def build_run_debug_summary(`
    - `shop/templates/runs/status.html` contains `Debug evaluation summary`
    - `shop/templates/runs/status.html` contains `All characteristics auto-passed`
    - `shop/templates/runs/status.html` contains `Open Exception Queue`
    - `shop/templates/runs/status.html` contains `Download debug_report.json`
  </acceptance_criteria>
  <done>Auto-passed rows are inspectable from run details, and zero-exception runs stay on the status page with a clean summary plus debug-report access.</done>
</task>

<task type="auto">
  <name>Task 3: Implement mismatch-first exception cards and exception-only export footer</name>
  <files>shop/services/review.py, shop/routers/review.py, shop/templates/review/queue.html, shop/templates/review/_item_card_debug.html, shop/templates/review/_signoff_footer.html</files>
  <read_first>shop/services/review.py, shop/routers/review.py, shop/templates/review/queue.html, shop/templates/review/_item_card_debug.html, shop/templates/review/_signoff_footer.html, .planning/phases/02-focused-debug-workflow/02-CONTEXT.md, tests/test_focused_debug_queue.py</read_first>
  <action>In `shop/services/review.py`, add item-id keyed helpers such as `semantic_contracts_by_item_id(...)` and `debug_internals_by_item_id(...)` so duplicate or `None` `char_no` rows do not collide in the debug UI. In `shop/routers/review.py`, pass those item-id keyed maps plus the exception-only counts from `build_debug_queue_state()` to the debug template branch. In `shop/templates/review/queue.html`, switch debug copy from the generic queue framing to exception-focused framing while keeping one card at a time in the existing scroll flow. In `shop/templates/review/_item_card_debug.html`, render the ordered `evaluation.mismatches` block first, put the resolution form immediately after it, label the stored `explanation` field as `Rationale`, expose only the select options `algorithm_error` and `acceptable_alternate`, and show the corrected-classification fields only when `algorithm_error` is selected. Move scores, semantic internals, bbox centers, and raw reasons under closed `<details>` blocks so mismatch summary plus review action is the default expanded state. In `shop/templates/review/_signoff_footer.html`, change the debug copy to `Debug report ready` vs `Resolve all exception rows to export debug_report.json`, and make the button state depend only on exception rows, not on conforming-row counts.</action>
  <verify>
    <automated>uv run pytest -q tests/test_focused_debug_queue.py tests/test_run_status_debug_summary.py tests/test_debug_verdicts.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `shop/services/review.py` contains `def debug_internals_by_item_id(`
    - `shop/services/review.py` contains `def semantic_contracts_by_item_id(`
    - `shop/templates/review/_item_card_debug.html` contains `algorithm_error`
    - `shop/templates/review/_item_card_debug.html` contains `acceptable_alternate`
    - `shop/templates/review/_item_card_debug.html` contains `Rationale`
    - `shop/templates/review/_item_card_debug.html` contains `<details`
    - `shop/templates/review/_signoff_footer.html` contains `Debug report ready`
  </acceptance_criteria>
  <done>The admin debug queue is mismatch-first, uses stable row identity in the UI, and exposes exception-only export readiness without cluttering the manual-review surface with auto-passed rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run/debug summary service -> Jinja templates | Reviewer rationale and packet mismatch data cross from validated backend state into rendered HTML. |
| admin debug POST/GET routes -> exception UI | The admin-only debug branch exposes extra data and must stay role-scoped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | `shop/templates/review/_item_card_debug.html` | mitigate | Keep Jinja auto-escaping for rationale and mismatch text; do not render any debug notes or rationale as trusted HTML. |
| T-02-08 | E | `shop/routers/review.py` / `shop/routers/runs.py` | mitigate | Preserve admin-only gating for debug mode and only surface debug-report links or exception summaries to admins. |
| T-02-09 | T/R | `shop/services/review.py` item-id helpers | mitigate | Key debug UI context by `ReviewItem.id` so duplicate or null `char_no` rows cannot display or resolve against the wrong packet row. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_focused_debug_queue.py tests/test_run_status_debug_summary.py tests/test_debug_verdicts.py -x`
- [ ] `rg -n "All characteristics auto-passed|Open Exception Queue|algorithm_error|acceptable_alternate|Rationale|Debug report ready" shop/templates/runs/status.html shop/templates/review/_item_card_debug.html shop/templates/review/_signoff_footer.html`
- [ ] Manual visual check: open one all-conforming admin run and one exception-bearing admin run to confirm the status-page summary and collapsed diagnostics match the expected copy/layout.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Auto-passed rows are inspectable from run details and no longer appear in the manual exception queue
- Exception cards lead with ordered mismatch information and reviewer action
- Debug export readiness and CTA copy are driven only by unresolved exception rows
</success_criteria>

<output>
After completion, create `.planning/phases/02-focused-debug-workflow/02-03-SUMMARY.md`
</output>
