---
phase: 02-focused-debug-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shop/services/review.py
  - shop/routers/review.py
  - tests/test_focused_debug_queue.py
  - tests/test_debug_row_identity.py
autonomous: true
requirements: [DREV-01]
user_setup: []
must_haves:
  truths:
    - "Admin debug review shows only rows whose `delta_packet.json` evaluation status is `review_needed`."
    - "Duplicate or `None` `char_no` packet rows stay distinct in debug state because queue order is anchored to `ReviewItem.id`, not `char_no` sorting."
    - "The admin debug route never renders an empty queue for an all-conforming run."
  artifacts:
    - path: "shop/services/review.py"
      provides: "Evaluation-driven debug queue state with packet-order item pairing"
      contains: "def build_debug_queue_state("
    - path: "shop/routers/review.py"
      provides: "Admin debug route that filters to exception rows and redirects zero-exception runs"
      contains: "debug_queue_state = build_debug_queue_state"
    - path: "tests/test_focused_debug_queue.py"
      provides: "Regression coverage for exception-only queue membership and zero-exception redirect"
      contains: "def test_admin_debug_queue_only_shows_review_needed_rows"
    - path: "tests/test_debug_row_identity.py"
      provides: "Regression coverage for duplicate and null characteristic numbers"
      contains: "def test_duplicate_and_none_char_rows_keep_distinct_review_item_ids"
  key_links:
    - from: "shop/routers/review.py"
      to: "shop/services/review.py"
      via: "build_debug_queue_state"
      pattern: "build_debug_queue_state"
    - from: "shop/services/review.py"
      to: "delta_packet.json"
      via: "DeltaItem.model_validate on packet rows"
      pattern: "DeltaItem.model_validate"
    - from: "shop/services/review.py"
      to: "shop.models.ReviewItem"
      via: "queue ordering by ReviewItem.id"
      pattern: "order_by\\(ReviewItem.id\\)"
---

<objective>
Move admin debug queue state to the Phase 1 evaluation contract instead of the legacy "every review item needs manual debug attention" flow.

Purpose: Phase 2 cannot narrow manual review until the debug branch trusts `DeltaItem.evaluation.status`, preserves stable row identity, and refuses to open a blank queue for all-conforming runs.
Output: packet-order debug queue helpers, exception-only admin queue routing, and regression tests for duplicate/`None` characteristic numbers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-focused-debug-workflow/02-CONTEXT.md
@.planning/phases/02-focused-debug-workflow/02-RESEARCH.md
@.planning/phases/02-focused-debug-workflow/02-VALIDATION.md
@.planning/phases/01-evaluation-foundation/01-03-SUMMARY.md
@delta_preservation/types.py
@shop/services/review.py
@shop/routers/review.py
@tests/test_review.py
@tests/test_debug_internals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add queue-state and row-identity regression coverage</name>
  <files>tests/test_focused_debug_queue.py, tests/test_debug_row_identity.py</files>
  <read_first>.planning/phases/02-focused-debug-workflow/02-CONTEXT.md, .planning/phases/02-focused-debug-workflow/02-RESEARCH.md, shop/services/review.py, shop/routers/review.py, tests/test_review.py, tests/test_debug_internals.py</read_first>
  <action>Create `tests/test_focused_debug_queue.py` to prove the Phase 2 debug branch uses `delta_packet.json -> item.evaluation.status` as the source of truth: `GET /review/{run_id}?debug=1` must render only rows whose evaluation status is `review_needed`, must leave non-debug review mode untouched, and must return a 302 back to `/runs/{run_id}` when every packet row is `conforming`. Create `tests/test_debug_row_identity.py` to cover duplicate `char_no` values and `char_no = null` rows so queue pairing, debug helpers, and packet ordering stay aligned by stable row identity instead of `char_no`-only joins.</action>
  <verify>
    <automated>uv run pytest -q tests/test_focused_debug_queue.py tests/test_debug_row_identity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_focused_debug_queue.py` contains `def test_admin_debug_queue_only_shows_review_needed_rows`
    - `tests/test_focused_debug_queue.py` contains `def test_admin_debug_queue_redirects_all_conforming_run_to_status_page`
    - `tests/test_debug_row_identity.py` contains `def test_duplicate_and_none_char_rows_keep_distinct_review_item_ids`
    - `tests/test_debug_row_identity.py` contains `ReviewItem.id`
  </acceptance_criteria>
  <done>Regression coverage exists for exception-only queue membership, zero-exception redirect behavior, and stable row identity across duplicate or null characteristic numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement evaluation-driven debug queue state and zero-exception redirect</name>
  <files>shop/services/review.py, shop/routers/review.py</files>
  <read_first>shop/services/review.py, shop/routers/review.py, delta_preservation/types.py, .planning/phases/01-evaluation-foundation/01-03-SUMMARY.md, tests/test_focused_debug_queue.py, tests/test_debug_row_identity.py</read_first>
  <action>Per D-01 and D-02, add `build_debug_queue_state(db: Session, run: Run)` to `shop/services/review.py`. This helper must validate packet rows with `DeltaItem.model_validate`, preserve original packet order, seed or reload `ReviewItem` rows in `ReviewItem.id` order, and derive `exception_items` strictly from `item.evaluation.status == "review_needed"`. Stop returning existing items via `order_by(ReviewItem.char_no)` inside `open_review_queue`; use `ReviewItem.id` ordering so duplicate or `None` `char_no` rows remain distinct and keep their packet pairing. In `shop/routers/review.py`, use `build_debug_queue_state(...)` only for `debug=True`: keep non-debug review queues unchanged, render only the exception subset for admins, and redirect to `/runs/{run_id}` when the exception count is zero instead of rendering an empty queue.</action>
  <verify>
    <automated>uv run pytest -q tests/test_focused_debug_queue.py tests/test_debug_row_identity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `shop/services/review.py` contains `def build_debug_queue_state(`
    - `shop/services/review.py` contains `order_by(ReviewItem.id)`
    - `shop/services/review.py` contains `evaluation.status == "review_needed"` or `evaluation_status == "review_needed"`
    - `shop/routers/review.py` contains `debug_queue_state = build_debug_queue_state`
    - `shop/routers/review.py` contains `return RedirectResponse(f"/runs/{run_id}"`
  </acceptance_criteria>
  <done>Admin debug queue membership is driven only by packet evaluation state, existing review rows preserve stable identity, and all-conforming runs no longer open an empty debug queue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `delta_packet.json` -> `shop/services/review.py` | Packet evaluation data is file-backed input and must be validated before it drives queue membership. |
| `?debug=1` request -> `shop/routers/review.py` | Client input selects the admin-only debug branch and must not bypass role checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `shop/services/review.py::build_debug_queue_state` | mitigate | Validate packet rows with `DeltaItem.model_validate` and derive exception membership only from `evaluation.status` values `review_needed` / `conforming`. |
| T-02-02 | E | `shop/routers/review.py::review_queue` | mitigate | Preserve the existing admin-role check before loading or redirecting the debug branch. |
| T-02-03 | T | `shop/services/review.py::open_review_queue` | mitigate | Order seeded and reloaded rows by `ReviewItem.id` so duplicate or null `char_no` values cannot collide into the wrong packet row. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_focused_debug_queue.py tests/test_debug_row_identity.py -x`
- [ ] `rg -n "build_debug_queue_state|order_by\\(ReviewItem.id\\)|RedirectResponse\\(f\"/runs/\\{run_id\\}\"" shop/services/review.py shop/routers/review.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Debug queue membership is derived from `DeltaItem.evaluation.status`
- Duplicate and `None` `char_no` rows remain distinct in packet-to-review-item pairing
- Zero-exception admin debug requests stop at run details instead of rendering an empty queue
</success_criteria>

<output>
After completion, create `.planning/phases/02-focused-debug-workflow/02-01-SUMMARY.md`
</output>
