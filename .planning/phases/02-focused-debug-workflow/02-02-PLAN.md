---
phase: 02-focused-debug-workflow
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - shop/services/review.py
  - tests/test_debug_verdicts.py
  - tests/test_debug_internals.py
autonomous: true
requirements: [DREV-03, DREV-04, RPT-01, RPT-02, RPT-03]
user_setup: []
must_haves:
  truths:
    - "Auto-passed packet rows remain full `debug_report.json` entries with `row_state == \"canonical_match\"` and do not require a manual resolution."
    - "Exception rows can only resolve to `algorithm_error` or `acceptable_alternate`, with validation rules that match the selected outcome, treat legacy verdict files as stale on strict paths, and allow `corrected_classification` to match the pipeline label when it represents the reviewer-accepted outcome."
    - "`debug_report.json` emits explicit row states `canonical_match`, `acceptable_alternate`, `unresolved_review_needed`, and `algorithm_error` while preserving ordered mismatch details."
  artifacts:
    - path: "shop/services/review.py"
      provides: "Phase 2 debug verdict validation and row-state derivation for report export"
      contains: "row_state"
    - path: "tests/test_debug_verdicts.py"
      provides: "Regression coverage for exception outcome vocabulary and export readiness"
      contains: "algorithm_error"
    - path: "tests/test_debug_internals.py"
      provides: "Regression coverage for ordered mismatches and placeholder history references"
      contains: "history_reference"
  key_links:
    - from: "shop/services/review.py"
      to: "debug_verdicts.json"
      via: "validated payloads keyed by ReviewItem.id"
      pattern: "load_debug_verdicts"
    - from: "shop/services/review.py"
      to: "delta_packet.json"
      via: "evaluation.status and mismatches in assemble_debug_report_payload"
      pattern: "assemble_debug_report_payload"
    - from: "shop/services/review.py"
      to: "shop.models.ReviewItem"
      via: "exception-only debug_total/debug_submitted counts"
      pattern: "debug_total"
---

<objective>
Rewrite the debug verdict and export contract around Phase 2 exception outcomes instead of the legacy `correct / incorrect / partially_correct` workflow.

Purpose: The report/export layer must represent auto-pass rows and manual exception resolutions without requiring manual entries for canonical matches or leaking Phase 3 history persistence early.
Output: validated exception outcome payloads, explicit report row states, and export readiness driven only by unresolved exceptions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-focused-debug-workflow/02-CONTEXT.md
@.planning/phases/02-focused-debug-workflow/02-RESEARCH.md
@.planning/phases/02-focused-debug-workflow/02-VALIDATION.md
@.planning/phases/01-evaluation-foundation/01-03-SUMMARY.md
@shop/services/review.py
@tests/test_debug_verdicts.py
@tests/test_debug_internals.py
@delta_preservation/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression coverage for exception outcomes and report row states</name>
  <files>tests/test_debug_verdicts.py, tests/test_debug_internals.py</files>
  <read_first>.planning/phases/02-focused-debug-workflow/02-CONTEXT.md, .planning/phases/02-focused-debug-workflow/02-RESEARCH.md, shop/services/review.py, tests/test_debug_verdicts.py, tests/test_debug_internals.py, delta_preservation/types.py</read_first>
  <action>Extend the existing debug verdict/report tests so they prove the Phase 2 contract. Replace assertions for `correct / incorrect / partially_correct` with the exact exception outcomes `algorithm_error` and `acceptable_alternate`. Add coverage that `algorithm_error` requires `corrected_classification` plus reviewer rationale (`explanation` stays the stored field key), that `corrected_classification` is the reviewer-accepted classification and may equal the pipeline classification, that `acceptable_alternate` requires reviewer rationale only, and that corrected requirement fields remain optional. Add explicit stale-compatibility coverage: render helpers may ignore unsupported legacy verdict entries, but strict write/export paths must fail with a clear Phase 2 re-entry message instead of auto-migrating `correct / incorrect / partially_correct`. Add report assertions that `assemble_debug_report_payload()` keeps `conforming` packet rows as full entries with `row_state == "canonical_match"`, leaves unresolved exception rows at `row_state == "unresolved_review_needed"`, emits resolved exception rows as `row_state == "algorithm_error"` or `row_state == "acceptable_alternate"`, preserves ordered `evaluation.mismatches`, and includes `history_reference: null` on Phase 2 rows.</action>
  <verify>
    <automated>uv run pytest -q tests/test_debug_verdicts.py::test_algorithm_error_accepts_reviewer_accepted_classification_when_label_matches_pipeline tests/test_debug_verdicts.py::test_load_debug_verdicts_for_render_ignores_legacy_verdict_entries tests/test_debug_verdicts.py::test_legacy_debug_verdicts_require_phase2_reentry_on_strict_paths tests/test_debug_internals.py::test_debug_report_rows_keep_ordered_mismatches_and_history_placeholder -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_debug_verdicts.py` contains `algorithm_error`
    - `tests/test_debug_verdicts.py` contains `acceptable_alternate`
    - `tests/test_debug_verdicts.py` contains `def test_algorithm_error_accepts_reviewer_accepted_classification_when_label_matches_pipeline`
    - `tests/test_debug_verdicts.py` contains `def test_load_debug_verdicts_for_render_ignores_legacy_verdict_entries`
    - `tests/test_debug_verdicts.py` contains `def test_legacy_debug_verdicts_require_phase2_reentry_on_strict_paths`
    - `tests/test_debug_internals.py` contains `history_reference`
    - `tests/test_debug_internals.py` contains `def test_debug_report_rows_keep_ordered_mismatches_and_history_placeholder`
  </acceptance_criteria>
  <done>Regression coverage exists for the new exception outcome vocabulary, exception-only export readiness, and the four explicit report row states.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Phase 2 debug verdict validation and report row-state derivation</name>
  <files>shop/services/review.py</files>
  <read_first>shop/services/review.py, .planning/phases/02-focused-debug-workflow/02-CONTEXT.md, .planning/phases/02-focused-debug-workflow/02-RESEARCH.md, tests/test_debug_verdicts.py, tests/test_debug_internals.py, delta_preservation/types.py</read_first>
  <action>Replace `VALID_DEBUG_VERDICTS` with the exact set `{"algorithm_error", "acceptable_alternate"}`. Update `validate_debug_verdict_payload()`, `load_debug_verdicts_for_render()`, and related helpers so saved debug resolution payloads use `verdict`, `corrected_classification`, `corrected_requirement_revA`, `corrected_requirement_revB`, and `explanation` as the reviewer rationale field. Enforce these exact rules: `algorithm_error` requires `corrected_classification` and non-empty `explanation`, but `corrected_classification` is the reviewer-accepted classification and may equal the pipeline classification; `acceptable_alternate` requires non-empty `explanation` only; corrected requirement fields stay optional for both outcomes. Do **not** auto-migrate legacy verdict payloads using `correct / incorrect / partially_correct`: `load_debug_verdicts_for_render()` may ignore unsupported legacy entries on a best-effort basis, but strict write/export paths must fail with a clear Phase 2 re-entry message so reviewers re-save those rows under the new vocabulary. In `assemble_debug_report_payload()`, stop blocking export on missing verdicts for `conforming` packet rows, reuse `build_debug_queue_state()` from Plan 02-01 for stable row pairing, and derive `row_state` with these exact mappings: `canonical_match` for rows whose evaluation status is `conforming`; `unresolved_review_needed` for exception rows with no saved debug verdict; `acceptable_alternate` for exception rows saved with verdict `acceptable_alternate`; `algorithm_error` for exception rows saved with verdict `algorithm_error`. Keep every packet row in `items[]`, keep ordered `mismatches`, add `history_reference: null` instead of Phase 3 persistence, and compute `debug_total` / `debug_submitted` from exception rows only so auto-passed rows never block `debug_report.json` readiness.</action>
  <verify>
    <automated>uv run pytest -q tests/test_debug_verdicts.py::test_algorithm_error_accepts_reviewer_accepted_classification_when_label_matches_pipeline tests/test_debug_verdicts.py::test_load_debug_verdicts_for_render_ignores_legacy_verdict_entries tests/test_debug_verdicts.py::test_legacy_debug_verdicts_require_phase2_reentry_on_strict_paths tests/test_debug_internals.py::test_debug_report_rows_keep_ordered_mismatches_and_history_placeholder -x</automated>
  </verify>
  <acceptance_criteria>
    - `shop/services/review.py` contains `VALID_DEBUG_VERDICTS = {"algorithm_error", "acceptable_alternate"}`
    - `shop/services/review.py` contains `canonical_match`
    - `shop/services/review.py` contains `unresolved_review_needed`
    - `shop/services/review.py` contains `acceptable_alternate`
    - `shop/services/review.py` contains `algorithm_error`
    - `shop/services/review.py` contains `history_reference`
    - `shop/services/review.py` contains `Phase 2 re-entry`
  </acceptance_criteria>
  <done>Debug verdict persistence uses the new exception vocabulary, treats legacy verdict payloads as stale on strict paths, allows reviewer-accepted classifications that match the pipeline label, and `debug_report.json` explicitly distinguishes canonical matches, unresolved exceptions, acceptable alternates, and algorithm errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin debug form payload -> `shop/services/review.py` | Reviewer-entered resolution data must be validated before it is persisted or exported. |
| `debug_verdicts.json` + `delta_packet.json` -> export assembler | File-backed data from two sources is merged into `debug_report.json` and must stay deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `shop/services/review.py::validate_debug_verdict_payload` | mitigate | Accept only `algorithm_error` / `acceptable_alternate` and enforce the exact corrected-classification / rationale requirements before writing `debug_verdicts.json`. |
| T-02-05 | T/I | `shop/services/review.py::assemble_debug_report_payload` | mitigate | Derive `row_state` from validated packet evaluation plus `ReviewItem.id`-keyed debug resolutions and emit `history_reference: null` so Phase 3 history data cannot be spoofed or leaked early. |
| T-02-06 | R | `debug_report.json` export rows | mitigate | Keep `review_item_id`, `queue_index`, and `reviewed_at` in each exported row so later audits can trace every exception resolution back to the exact run row. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_debug_verdicts.py::test_algorithm_error_accepts_reviewer_accepted_classification_when_label_matches_pipeline tests/test_debug_verdicts.py::test_load_debug_verdicts_for_render_ignores_legacy_verdict_entries tests/test_debug_verdicts.py::test_legacy_debug_verdicts_require_phase2_reentry_on_strict_paths tests/test_debug_internals.py::test_debug_report_rows_keep_ordered_mismatches_and_history_placeholder -x`
- [ ] `rg -n "VALID_DEBUG_VERDICTS|canonical_match|unresolved_review_needed|acceptable_alternate|algorithm_error|history_reference" shop/services/review.py tests/test_debug_verdicts.py tests/test_debug_internals.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Debug verdict persistence uses only `algorithm_error` and `acceptable_alternate`
- Legacy `correct / incorrect / partially_correct` verdict payloads are treated as stale and require a clear Phase 2 re-entry on strict paths
- `debug_report.json` remains exportable when conforming rows have no manual debug resolution
- Every exported row has one of the four explicit row states and preserves ordered mismatch details
</success_criteria>

<output>
After completion, create `.planning/phases/02-focused-debug-workflow/02-02-SUMMARY.md`
</output>
