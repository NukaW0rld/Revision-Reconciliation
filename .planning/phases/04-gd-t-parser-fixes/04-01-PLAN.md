---
phase: 04-gd-t-parser-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - delta_preservation/reconcile/normalize.py
  - tests/test_semantic_extraction.py
  - tests/test_semantic_comparison.py
autonomous: true
requirements: [GDT-01, GDT-02]
user_setup: []
must_haves:
  truths:
    - "Word-form Form 3 GD&T names normalize to the same control symbols used by drawing annotations before semantic dispatch runs."
    - "Compact tokens like `⌖∅0.35ABC`, `⌓0.5A`, and `⏥0.2` produce structured GD&T payloads instead of `gdt_malformed_frame`, while true malformed inputs like `⌖ A` still error."
  artifacts:
    - path: "delta_preservation/reconcile/normalize.py"
      provides: "Word-form normalization and compact-token parsing inside the existing GD&T parser path"
      contains: "_normalize_gdt_word_controls"
    - path: "delta_preservation/reconcile/normalize.py"
      provides: "Compact remainder splitting for control-symbol-leading single-token FCFs"
      contains: "_split_compact_gdt_remainder"
    - path: "tests/test_semantic_extraction.py"
      provides: "Regression coverage for word-form and compact GD&T parsing cases"
      contains: "test_extract_semantic_callout_gdt_parsed_compact_token_variants"
    - path: "tests/test_semantic_comparison.py"
      provides: "Regression coverage proving word-form and symbol-form GD&T extractions compare equal"
      contains: "test_compare_semantic_callouts_gdt_word_form_and_symbol_form_report_semantic_match"
  key_links:
    - from: "delta_preservation/reconcile/normalize.py"
      to: "tests/test_semantic_extraction.py"
      via: "extract_semantic_callout"
      pattern: "extract_semantic_callout("
    - from: "tests/test_semantic_comparison.py"
      to: "delta_preservation/reconcile/semantic_compare.py"
      via: "compare_semantic_callouts"
      pattern: "compare_semantic_callouts"
---

<objective>
Make the Phase 4 parser handle the compact and word-form GD&T cases that currently fall back to malformed or non-GD&T parsing.

Purpose: Phase 4 cannot improve cross-part accuracy until the shared parser recognizes whitespace-free FCF tokens and Form 3 word-form controls through the same bounded `normalize.py` entry path used across the corpus.
Output: `normalize.py` gains explicit word-control normalization and compact-token splitting, `tests/test_semantic_extraction.py` covers the new parser cases, and `tests/test_semantic_comparison.py` proves word-form and symbol-form extractions compare equal.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gd-t-parser-fixes/04-CONTEXT.md
@.planning/phases/04-gd-t-parser-fixes/04-RESEARCH.md
@.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md
@delta_preservation/reconcile/normalize.py
@delta_preservation/reconcile/semantic_compare.py
@tests/test_semantic_extraction.py
@tests/test_semantic_comparison.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize supported word-form GD&T controls before parser dispatch</name>
  <files>delta_preservation/reconcile/normalize.py, tests/test_semantic_extraction.py, tests/test_semantic_comparison.py</files>
  <read_first>.planning/phases/04-gd-t-parser-fixes/04-CONTEXT.md, .planning/phases/04-gd-t-parser-fixes/04-RESEARCH.md, delta_preservation/reconcile/normalize.py, delta_preservation/reconcile/semantic_compare.py, tests/test_semantic_extraction.py, tests/test_semantic_comparison.py</read_first>
  <action>In `delta_preservation/reconcile/normalize.py`, add `_GDT_WORD_CONTROL_MAP` and `_normalize_gdt_word_controls(text: str) -> str` so the parser recognizes these exact word forms case-insensitively and longest-first before `_parse_gdt_frame` tokenizes them: `total runout -> ⟃`, `runout -> ⌿`, `circularity -> ○`, `position -> ⌖`, `flatness -> ⏥`, and `perpendicularity -> ⟂`. Before finalizing those symbol entries, inspect one representative drawing-side semantic sample for the circularity/runout/total-runout families and use the exact emitted codepoints in `_GDT_CONTROL_MAP` and the regression fixtures; if the corpus emits a different symbol than `○`, `⌿`, or `⟃`, update the map and tests to that exact codepoint in the same task. Call `_normalize_gdt_word_controls(...)` inside the GD&T semantic extraction path in `normalize.py`; do not move normalization into `xlsx.py` or any I/O layer. In `tests/test_semantic_extraction.py`, add parser coverage for `circularity`, `runout`, `total runout`, and `perpendicularity` inputs. In `tests/test_semantic_comparison.py`, add `test_compare_semantic_callouts_gdt_word_form_and_symbol_form_report_semantic_match` that extracts a word-form and symbol-form pair and asserts `compare_semantic_callouts(...)` returns a semantic match rather than fallback.</action>
  <verify>
    <automated>uv run pytest -q tests/test_semantic_extraction.py tests/test_semantic_comparison.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/reconcile/normalize.py` contains `_GDT_WORD_CONTROL_MAP`
    - `delta_preservation/reconcile/normalize.py` contains `def _normalize_gdt_word_controls(`
    - `delta_preservation/reconcile/normalize.py` contains `total runout`
    - `delta_preservation/reconcile/normalize.py` contains `circularity`
    - `tests/test_semantic_extraction.py` contains `test_extract_semantic_callout_gdt_parsed_word_name_controls`
    - `tests/test_semantic_comparison.py` contains `test_compare_semantic_callouts_gdt_word_form_and_symbol_form_report_semantic_match`
  </acceptance_criteria>
  <done>Word-form GD&T controls now enter the existing parser path as recognized control symbols and compare semantically to symbol-form annotations.</done>
</task>

<task type="auto">
  <name>Task 2: Split compact single-token GD&T frames without regressing malformed-frame detection</name>
  <files>delta_preservation/reconcile/normalize.py, tests/test_semantic_extraction.py</files>
  <read_first>delta_preservation/reconcile/normalize.py, .planning/phases/04-gd-t-parser-fixes/04-CONTEXT.md, .planning/phases/04-gd-t-parser-fixes/04-RESEARCH.md, tests/test_semantic_extraction.py</read_first>
  <action>In `delta_preservation/reconcile/normalize.py`, add `_GDT_COMPACT_REMAINDER_RE` and `_split_compact_gdt_remainder(remainder: str)` that use the exact compact-form regex `^([⌀∅])?((?:\\d+(?:\\.\\d+)?|\\.\\d+))([A-Z](?:-[A-Z])?(?:[A-Z](?:-[A-Z])?)*)?$` to split a control-symbol-leading single token into diameter prefix, numeric tolerance, and datum suffix. Invoke the helper only inside `_parse_gdt_frame` when the token already begins with a recognized GD&T control symbol and the remainder must be parsed without whitespace neighbors. Preserve the existing whitespace-tokenized path and keep malformed inputs like `⌖ A` on the `gdt_malformed_frame` error path. In `tests/test_semantic_extraction.py`, add `test_extract_semantic_callout_gdt_parsed_compact_token_variants` with compact cases for `⌖∅0.35ABC`, `⌓0.5A`, and `⏥0.2`, and keep regression assertions for the existing whitespace-tokenized position example plus the malformed `⌖ A` case.</action>
  <verify>
    <automated>uv run pytest -q tests/test_semantic_extraction.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/reconcile/normalize.py` contains `_GDT_COMPACT_REMAINDER_RE`
    - `delta_preservation/reconcile/normalize.py` contains `def _split_compact_gdt_remainder(`
    - `delta_preservation/reconcile/normalize.py` contains `([⌀∅])?((?:\\d+(?:\\.\\d+)?|\\.\\d+))`
    - `tests/test_semantic_extraction.py` contains `test_extract_semantic_callout_gdt_parsed_compact_token_variants`
    - `tests/test_semantic_extraction.py` contains `⌖∅0.35ABC`
    - `tests/test_semantic_extraction.py` contains `⌓0.5A`
    - `tests/test_semantic_extraction.py` contains `⏥0.2`
  </acceptance_criteria>
  <done>Compact control-symbol-leading tokens are parsed into structured GD&T payloads without weakening malformed-frame detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form 3 / PDF semantic text -> `normalize.py` parser | Free-form text crosses into bounded regex parsing and must remain inside the GD&T-only path. |
| Parser helper output -> semantic comparison/tests | Normalized symbols and split tolerances become durable semantic payloads used by downstream equality logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `delta_preservation/reconcile/normalize.py` | mitigate | Only invoke word-control and compact-token helpers from the GD&T parser branch that already starts with a recognized control symbol. |
| T-04-02 | I | `delta_preservation/reconcile/normalize.py` | mitigate | Preserve the existing malformed-frame behavior for inputs that still lack a tolerance token after compact parsing. |
| T-04-03 | D | `delta_preservation/reconcile/normalize.py` | mitigate | Use anchored regexes with bounded character classes and keep the compact split helper single-pass to avoid slow pathological matches. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_semantic_extraction.py tests/test_semantic_comparison.py -x`
- [ ] `rg -n "_GDT_WORD_CONTROL_MAP|_normalize_gdt_word_controls|_GDT_COMPACT_REMAINDER_RE|_split_compact_gdt_remainder|test_extract_semantic_callout_gdt_parsed_word_name_controls|test_extract_semantic_callout_gdt_parsed_compact_token_variants|test_compare_semantic_callouts_gdt_word_form_and_symbol_form_report_semantic_match" delta_preservation/reconcile/normalize.py tests/test_semantic_extraction.py tests/test_semantic_comparison.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Supported word-form GD&T controls normalize into the GD&T parser path before tokenization
- Compact tokens like `⌖∅0.35ABC`, `⌓0.5A`, and `⏥0.2` parse into structured payloads
- Existing whitespace-tokenized GD&T parsing and malformed-frame fallback continue to behave correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-gd-t-parser-fixes/04-01-SUMMARY.md`
</output>
