---
phase: 04-gd-t-parser-fixes
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - delta_preservation/reconcile/normalize.py
  - delta_preservation/types.py
  - delta_preservation/reconcile/semantic_compare.py
  - tests/test_semantic_extraction.py
  - tests/test_semantic_comparison.py
  - tests/test_semantic_types.py
autonomous: true
requirements: [GDT-03]
user_setup: []
must_haves:
  truths:
    - "Composite `/`-separated GD&T frames preserve every compartment in structured parser output instead of being rejected as unsupported."
    - "The new `compartments` payload shape is consumed by semantic comparison without regressing single-compartment GD&T equality."
  artifacts:
    - path: "delta_preservation/types.py"
      provides: "Structured GD&T compartment payload model and top-level `compartments` field"
      contains: "class GdtCompartment"
    - path: "delta_preservation/reconcile/normalize.py"
      provides: "Composite GD&T frame capture that emits all parsed compartments in order"
      contains: "compartments"
    - path: "delta_preservation/reconcile/semantic_compare.py"
      provides: "Compartment-aware GD&T comparison logic"
      contains: "compartments"
    - path: "tests/test_semantic_comparison.py"
      provides: "Regression coverage for compartment equality and mismatch handling"
      contains: "test_compare_semantic_callouts_gdt_compartment_count_mismatch_reports_change"
  key_links:
    - from: "delta_preservation/reconcile/normalize.py"
      to: "delta_preservation/types.py"
      via: "GdtCompartment"
      pattern: "GdtCompartment"
    - from: "delta_preservation/reconcile/semantic_compare.py"
      to: "delta_preservation/types.py"
      via: "GdtSemanticPayload.compartments"
      pattern: "compartments"
    - from: "tests/test_semantic_comparison.py"
      to: "delta_preservation/reconcile/semantic_compare.py"
      via: "_compare_gdt"
      pattern: "_compare_gdt"
---

<objective>
Capture composite GD&T compartments end-to-end and make semantic comparison aware of the new payload shape.

Purpose: Phase 4 is incomplete until `/`-separated multi-compartment frames survive parsing and comparison as structured data instead of being silently reduced to one segment or rejected outright.
Output: `types.py` defines a stable GD&T compartment model, `normalize.py` preserves every parsed compartment, `semantic_compare.py` compares compartments safely, and regression coverage guards weld/fit slash inputs plus single-compartment equality.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gd-t-parser-fixes/04-CONTEXT.md
@.planning/phases/04-gd-t-parser-fixes/04-RESEARCH.md
@.planning/phases/04-gd-t-parser-fixes/04-VALIDATION.md
@.planning/phases/04-gd-t-parser-fixes/04-01-PLAN.md
@delta_preservation/reconcile/normalize.py
@delta_preservation/types.py
@delta_preservation/reconcile/semantic_compare.py
@tests/test_semantic_extraction.py
@tests/test_semantic_comparison.py
@tests/test_semantic_types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Represent and capture every composite GD&T compartment</name>
  <files>delta_preservation/types.py, delta_preservation/reconcile/normalize.py, tests/test_semantic_extraction.py, tests/test_semantic_types.py</files>
  <read_first>.planning/phases/04-gd-t-parser-fixes/04-CONTEXT.md, .planning/phases/04-gd-t-parser-fixes/04-RESEARCH.md, delta_preservation/types.py, delta_preservation/reconcile/normalize.py, tests/test_semantic_extraction.py, tests/test_semantic_types.py</read_first>
  <action>In `delta_preservation/types.py`, add `class GdtCompartment(BaseModel)` with these exact fields: `control_type`, `tolerance_text`, `datum_refs`, and `modifiers`. Add `compartments: List[GdtCompartment] = Field(default_factory=list, description=\"Ordered GD&T compartments captured from a composite frame\")` to `GdtSemanticPayload`. In `delta_preservation/reconcile/normalize.py`, update `_parse_gdt_frame` so it splits on `/` only when every non-empty segment starts with a recognized GD&T control symbol after word normalization, parses each segment through the GD&T path, keeps the first segment as the top-level `frame_text/control_type/tolerance_text/datum_refs/modifiers`, and stores every parsed segment in `compartments` in order. Do not let this slash handling capture weld text like `1/8 FILLET` or fit text like `H7/p6`. In `tests/test_semantic_extraction.py`, add composite-frame coverage for `⌓ .05 D B C / ⌓ .01 D` and at least one word-normalized or mixed-notation composite variant. In `tests/test_semantic_types.py`, add a serialization/default-factory assertion that a `GdtSemanticPayload` with no compartments still serializes with `compartments=[]`.</action>
  <verify>
    <automated>uv run pytest -q tests/test_semantic_extraction.py tests/test_semantic_types.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/types.py` contains `class GdtCompartment`
    - `delta_preservation/types.py` contains `compartments: List[GdtCompartment]`
    - `delta_preservation/reconcile/normalize.py` contains `compartments`
    - `tests/test_semantic_extraction.py` contains `⌓ .05 D B C / ⌓ .01 D`
    - `tests/test_semantic_types.py` contains `compartments=[]`
  </acceptance_criteria>
  <done>Composite GD&T frames now preserve every parsed compartment in a structured payload without contaminating weld or fit parsing.</done>
</task>

<task type="auto">
  <name>Task 2: Compare compartment-aware GD&T payloads and guard slash-family regressions</name>
  <files>delta_preservation/reconcile/semantic_compare.py, tests/test_semantic_comparison.py, tests/test_semantic_extraction.py</files>
  <read_first>delta_preservation/reconcile/semantic_compare.py, delta_preservation/types.py, delta_preservation/reconcile/normalize.py, tests/test_semantic_comparison.py, tests/test_semantic_extraction.py, .planning/phases/04-gd-t-parser-fixes/04-RESEARCH.md</read_first>
  <action>In `delta_preservation/reconcile/semantic_compare.py`, update `_compare_gdt` so `compartments` participate in equality before the generic changed fallback: identical ordered compartments should return semantic match, a compartment-count mismatch should return `semantic GD&T changed: compartments ...`, and field-level compartment differences should return a specific changed fragment instead of silently ignoring the secondary compartment. Treat `[]` as the neutral default so the existing single-compartment position/profile cases still compare equal without needing explicit compartments. In `tests/test_semantic_comparison.py`, add `test_compare_semantic_callouts_gdt_compartment_count_mismatch_reports_change`, a positive match case for identical composite payloads, and a regression case that proves the existing single-compartment position example still reports semantic match. In `tests/test_semantic_extraction.py`, add explicit guards that `1/8 FILLET` continues to parse as `weld` and `H7/p6` continues to parse as `fit` after the composite slash branch lands.</action>
  <verify>
    <automated>uv run pytest -q tests/test_semantic_comparison.py tests/test_semantic_extraction.py tests/test_semantic_types.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/reconcile/semantic_compare.py` contains `compartments`
    - `tests/test_semantic_comparison.py` contains `test_compare_semantic_callouts_gdt_compartment_count_mismatch_reports_change`
    - `tests/test_semantic_comparison.py` contains `semantic GD&T changed: compartments`
    - `tests/test_semantic_extraction.py` contains `1/8 FILLET`
    - `tests/test_semantic_extraction.py` contains `H7/p6`
  </acceptance_criteria>
  <done>Semantic comparison understands composite GD&T payloads while slash-containing weld and fit inputs remain on their existing parser families.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composite parser output -> `GdtSemanticPayload` schema | Multi-compartment parse state crosses from parser internals into the durable semantic model used by downstream comparison and packet generation. |
| `GdtSemanticPayload` -> `semantic_compare.py` | New structured fields influence equality and change classification across revisions. |
| Slash-containing semantic text -> parser family dispatch | Composite GD&T handling must not consume weld or fit syntaxes that also contain `/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `delta_preservation/reconcile/normalize.py` | mitigate | Only split composite compartments when every slash-separated segment starts with a recognized GD&T control symbol after normalization. |
| T-04-02 | I | `delta_preservation/types.py`, `delta_preservation/reconcile/semantic_compare.py` | mitigate | Use `compartments=[]` as the backward-compatible default and add explicit single-compartment equality regression tests. |
| T-04-03 | D | `delta_preservation/reconcile/normalize.py` | mitigate | Keep the composite split linear and bounded to slash-separated segments instead of recursive or ambiguous parsing. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_semantic_extraction.py tests/test_semantic_comparison.py tests/test_semantic_types.py -x`
- [ ] `uv run pytest -q tests/test_reconcile_semantic_integration.py tests/test_pipeline_semantic_packet.py -x`
- [ ] `rg -n "class GdtCompartment|compartments|test_compare_semantic_callouts_gdt_compartment_count_mismatch_reports_change|1/8 FILLET|H7/p6" delta_preservation/types.py delta_preservation/reconcile/normalize.py delta_preservation/reconcile/semantic_compare.py tests/test_semantic_extraction.py tests/test_semantic_comparison.py tests/test_semantic_types.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Composite GD&T frames preserve every compartment in structured parser output
- `GdtSemanticPayload` exposes a stable `compartments` field with a backward-compatible default
- `_compare_gdt` treats compartment mismatches as semantic changes without regressing single-compartment equality
- Slash-containing weld and fit inputs still parse on their original semantic families
</success_criteria>

<output>
After completion, create `.planning/phases/04-gd-t-parser-fixes/04-02-SUMMARY.md`
</output>
