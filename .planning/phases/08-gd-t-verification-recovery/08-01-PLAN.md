---
phase: 08-gd-t-verification-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - delta_preservation/reconcile/normalize.py
  - tests/test_semantic_extraction.py
autonomous: true
requirements: [GDT-01, GDT-02, GDT-03]
test_ids:
  - tests/test_semantic_extraction.py::test_classify_requirement_type_recognizes_phase4_gdt_symbols_and_word_forms
  - tests/test_semantic_extraction.py::test_classify_requirement_type_keeps_non_gdt_families_stable
must_haves:
  truths:
    - "Downstream requirement-type classification recognizes the Phase 04 parser-produced GD&T symbols `○`, `⌿`, and `⟃` as `gdt` instead of `other`."
    - "Word-form Phase 04 controls route through the same GDT type gate as their symbol-form counterparts."
    - "Non-GD&T families such as `H7/p6`, `1/8 FILLET BOTH SIDES`, and `positioning hole` remain on their original non-GDT routes."
  artifacts:
    - path: "delta_preservation/reconcile/normalize.py"
      provides: "Requirement-type gate aligned with the current Phase 04 parser-produced GD&T symbol family"
      contains: "def classify_requirement_type("
    - path: "tests/test_semantic_extraction.py"
      provides: "Regression coverage for Phase 08 type-gate recovery"
      contains: "test_classify_requirement_type_recognizes_phase4_gdt_symbols_and_word_forms"
  key_links:
    - from: "delta_preservation/reconcile/classify.py"
      to: "delta_preservation/reconcile/normalize.py"
      via: "classify_requirement_type"
      pattern: "classify_requirement_type("
    - from: "delta_preservation/reconcile/match.py"
      to: "delta_preservation/reconcile/normalize.py"
      via: "classify_requirement_type"
      pattern: "classify_requirement_type("
---

<objective>
Close the residual Phase 04 GD&T type-gate debt before Phase 04 is re-verified.

Purpose: the parser currently accepts `○`, `⌿`, and `⟃` GD&T callouts through
`extract_semantic_callout()`, but `classify_requirement_type()` still labels
those same parser-produced families as `other`. Phase 08 cannot write a
truthful `04-VERIFICATION.md` while that downstream mismatch remains open.
This plan makes the type gate consistent with the shipped parser contract and
adds direct regression coverage for the supported symbol and word-form inputs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/08-gd-t-verification-recovery/08-RESEARCH.md
@.planning/phases/08-gd-t-verification-recovery/08-VALIDATION.md
@.planning/phases/04-gd-t-parser-fixes/04-REVIEW-FIX.md
@delta_preservation/reconcile/normalize.py
@delta_preservation/reconcile/classify.py
@delta_preservation/reconcile/match.py
@tests/test_semantic_extraction.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the GD&T requirement-type gate with the current parser-produced symbol family</name>
  <files>delta_preservation/reconcile/normalize.py</files>
  <read_first>
.planning/phases/08-gd-t-verification-recovery/08-RESEARCH.md,
.planning/v1.1-MILESTONE-AUDIT.md,
delta_preservation/reconcile/normalize.py,
delta_preservation/reconcile/classify.py,
delta_preservation/reconcile/match.py
  </read_first>
  <action>
In `delta_preservation/reconcile/normalize.py`, update
`classify_requirement_type(text: str)` so it recognizes the full current
Phase 04 parser-produced GD&T symbol family before the non-GD&T branches run.

Use this exact supported leading-symbol set for the GDT gate:
`⌖⌒⟂⊙⌓⏥∥∠○⌿⟃`.

Implementation rules:
- Keep `classify_requirement_type()` lightweight; do not call
  `extract_semantic_callout()` from inside it.
- Normalize a stripped local copy through `_normalize_gdt_word_controls(...)`
  before testing the leading GD&T symbol so supported word forms like
  `Circularity 0.002`, `Runout .025 A-B`, and `Total Runout .015 B`
  route to `"gdt"`.
- Preserve the current dispatch order for thread, weld, fit, surface-finish,
  chamfer, and angle detection.
- Compact strings such as `⌖∅0.35ABC` and composite strings such as
  `⌓ .05 D B C / ⌓ .01 D` must still return `"gdt"`.
- Do **not** add alias support for corpus-only `↗` or `⌰` in this plan. That
  scope decision is handled by Plan 08-02 when verification is written.
  This task is limited to the Phase 04 parser-produced symbol family already
  encoded in `_GDT_CONTROL_MAP`.
  </action>
  <verify>
    <automated>uv run python - <<'PY'
from delta_preservation.reconcile.normalize import classify_requirement_type
cases = {
    "○ 0.002": "gdt",
    "⌿ .025 A-B": "gdt",
    "⟃ .015 B": "gdt",
    "Circularity 0.002": "gdt",
    "Runout .025 A-B": "gdt",
    "Total Runout .015 B": "gdt",
    "⌖∅0.35ABC": "gdt",
    "⌓ .05 D B C / ⌓ .01 D": "gdt",
}
for text, expected in cases.items():
    actual = classify_requirement_type(text)
    assert actual == expected, (text, actual, expected)
print("ok")
PY</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/reconcile/normalize.py` contains the literal symbol set `○⌿⟃` in the GD&T type gate.
    - `delta_preservation/reconcile/normalize.py` uses `_normalize_gdt_word_controls(` inside `classify_requirement_type(` or an equivalent local normalization step in that function.
    - `rg -n "def classify_requirement_type\\(" delta_preservation/reconcile/normalize.py` returns exactly one match.
    - `uv run python - <<'PY'\nfrom delta_preservation.reconcile.normalize import classify_requirement_type\ncases = {\n    '○ 0.002': 'gdt',\n    '⌿ .025 A-B': 'gdt',\n    '⟃ .015 B': 'gdt',\n    'Circularity 0.002': 'gdt',\n    'Runout .025 A-B': 'gdt',\n    'Total Runout .015 B': 'gdt',\n    '⌖∅0.35ABC': 'gdt',\n    '⌓ .05 D B C / ⌓ .01 D': 'gdt',\n}\nfor text, expected in cases.items():\n    actual = classify_requirement_type(text)\n    assert actual == expected, (text, actual, expected)\nprint('ok')\nPY` exits 0.
  </acceptance_criteria>
  <done>The lightweight requirement-type gate now recognizes the shipped Phase 04 GD&T symbol and word-form families without broadening into unrelated requirement families.</done>
</task>

<task type="auto">
  <name>Task 2: Add direct regression coverage for the recovered GD&T type gate</name>
  <files>tests/test_semantic_extraction.py</files>
  <read_first>
tests/test_semantic_extraction.py,
delta_preservation/reconcile/normalize.py,
.planning/phases/08-gd-t-verification-recovery/08-RESEARCH.md,
.planning/phases/04-gd-t-parser-fixes/04-UAT.md
  </read_first>
  <action>
Extend `tests/test_semantic_extraction.py` with two new direct
`classify_requirement_type()` regression tests.

Add exactly these test names:
1. `test_classify_requirement_type_recognizes_phase4_gdt_symbols_and_word_forms`
2. `test_classify_requirement_type_keeps_non_gdt_families_stable`

Test content:
- The positive test must use `pytest.mark.parametrize` with these exact
  inputs and expected `"gdt"` outputs:
  - `"○ 0.002"`
  - `"⌿ .025 A-B"`
  - `"⟃ .015 B"`
  - `"Circularity 0.002"`
  - `"Runout .025 A-B"`
  - `"Total Runout .015 B"`
  - `"⌖∅0.35ABC"`
  - `"⌓ .05 D B C / ⌓ .01 D"`
- The negative/stability test must use `pytest.mark.parametrize` with these
  exact inputs and outputs:
  - `"H7/p6"` -> `"fit"`
  - `"1/8 FILLET BOTH SIDES"` -> `"weld"`
  - `"positioning hole"` -> `"other"`

Keep these tests in `tests/test_semantic_extraction.py` so the Phase 04 parser
and Phase 08 gate-recovery checks remain co-located. Do not create a new test
module for this task.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_semantic_extraction.py -k "classify_requirement_type" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_classify_requirement_type_recognizes_phase4_gdt_symbols_and_word_forms|test_classify_requirement_type_keeps_non_gdt_families_stable" tests/test_semantic_extraction.py` returns two matches.
    - `tests/test_semantic_extraction.py` contains the literals `○ 0.002`, `⌿ .025 A-B`, `⟃ .015 B`, `Circularity 0.002`, `Runout .025 A-B`, `Total Runout .015 B`, `H7/p6`, and `1/8 FILLET BOTH SIDES`.
    - `uv run pytest -q tests/test_semantic_extraction.py -k "classify_requirement_type" -x` exits 0.
    - `uv run pytest -q tests/test_semantic_extraction.py tests/test_semantic_comparison.py tests/test_semantic_types.py tests/test_pipeline_semantic_packet.py -x` exits 0.
  </acceptance_criteria>
  <done>Direct regression coverage now pins the recovered GD&T type gate and confirms fit, weld, and plain-text non-GD&T families remain stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GD&T parser output -> lightweight type classification | Parser-produced symbol families cross into `classify_requirement_type()` and must still be treated as GD&T by downstream matching/classification. |
| Type classification -> non-GD&T family routing | Broader GD&T matching must not steal fit, weld, or substring-based non-GD&T cases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I | `delta_preservation/reconcile/normalize.py` | mitigate | Keep the GD&T gate tied to the exact parser-produced symbol family and normalized word forms rather than broad regex guesses. |
| T-08-02 | I | `delta_preservation/reconcile/normalize.py` | mitigate | Preserve the existing detection order so fit, weld, surface-finish, chamfer, and substring false positives remain stable. |
| T-08-03 | T | `tests/test_semantic_extraction.py` | mitigate | Pin both positive GD&T cases and negative non-GD&T families with exact parametrize inputs. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_semantic_extraction.py -k "classify_requirement_type" -x`
- [ ] `uv run pytest -q tests/test_semantic_extraction.py tests/test_semantic_comparison.py tests/test_semantic_types.py tests/test_pipeline_semantic_packet.py -x`
- [ ] `uv run python - <<'PY'\nfrom delta_preservation.reconcile.normalize import classify_requirement_type\nchecks = {\n    '○ 0.002': 'gdt',\n    '⌿ .025 A-B': 'gdt',\n    '⟃ .015 B': 'gdt',\n    'H7/p6': 'fit',\n    '1/8 FILLET BOTH SIDES': 'weld',\n    'positioning hole': 'other',\n}\nfor text, expected in checks.items():\n    actual = classify_requirement_type(text)\n    assert actual == expected, (text, actual, expected)\nprint('ok')\nPY`
</verification>

<success_criteria>

- All tasks completed
- The current Phase 04 parser-produced symbol family is recognized as `gdt` by downstream requirement-type classification
- Supported word-form inputs now share the same lightweight GDT type gate as their symbol-form counterparts
- Non-GD&T slash families and substring false positives remain stable
</success_criteria>

<output>
After completion, create `.planning/phases/08-gd-t-verification-recovery/08-01-SUMMARY.md`
</output>
