---
phase: 10-debug-exception-gating-and-advisory-surfacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shop/services/review.py
  - shop/routers/review.py
  - shop/templates/review/_item_card.html
  - shop/templates/review/_item_card_debug.html
  - shop/templates/runs/status.html
  - tests/test_review.py
  - tests/test_run_status_debug_summary.py
  - tests/test_debug_verdicts.py
autonomous: true
requirements: [CLS-01, ADD-01, ADD-02, SNP-01, VER-01]
test_ids:
  - tests/test_review.py::test_review_queue_surfaces_packet_confidence_flags_on_standard_item_cards
  - tests/test_run_status_debug_summary.py::test_status_page_surfaces_confidence_flags_for_exception_rows
  - tests/test_debug_verdicts.py::test_debug_queue_surfaces_packet_confidence_flags_without_rederiving_them
must_haves:
  truths:
    - "Normal review, debug review, and run-status surfaces render packet-native `confidence_flags` next to the affected rows instead of re-deriving warnings from `reasons` or mismatch text."
    - "Packet advisory joins are keyed by `ReviewItem.id` / packet row order so duplicate or `None` `char_no` rows keep the correct advisory state."
    - "`build_run_debug_summary()` exposes row-level advisory data for both exception and conforming rows so the status page can show the same flag text the debug packet carries."
  artifacts:
    - path: "shop/services/review.py"
      provides: "Packet-derived advisory mapping and summary rows with `confidence_flags`"
      contains: "def advisory_flags_by_item_id("
    - path: "shop/templates/review/_item_card.html"
      provides: "Normal review card advisory rendering"
      contains: "Packet Advisories"
    - path: "shop/templates/runs/status.html"
      provides: "Run-status advisory surfacing in debug summary rows"
      contains: "confidence_flags"
  key_links:
    - from: "shop/services/review.py::build_debug_queue_state"
      to: "shop/templates/review/_item_card.html"
      via: "ReviewItem.id -> packet DeltaItem.confidence_flags"
      pattern: "confidence_flags"
    - from: "shop/services/review.py::build_run_debug_summary"
      to: "shop/templates/runs/status.html"
      via: "summary row advisory state"
      pattern: "confidence_flags"
---

<objective>
Surface classifier advisory flags on the existing maintainer-facing review
surfaces before sign-off/export gating work lands.

Purpose: Phase 5 already persists `DeltaItem.confidence_flags`, but the normal
review queue, admin debug cards, and run status page do not show them. Phase 10
must make those packet-native advisories visible on the same rows the
maintainer reviews, without changing classifier semantics or collapsing duplicate
row identities.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-CONTEXT.md
@.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-RESEARCH.md
@.planning/quick/260413-hfc-add-missing-added-characteristics-check-/260413-hfc-SUMMARY.md
@shop/services/review.py
@shop/routers/review.py
@shop/templates/review/_item_card.html
@shop/templates/review/_item_card_debug.html
@shop/templates/runs/status.html
@tests/test_review.py
@tests/test_run_status_debug_summary.py
@tests/test_debug_verdicts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread packet-native advisory flags through review service state keyed by review row</name>
  <files>
shop/services/review.py,
shop/routers/review.py,
tests/test_review.py,
tests/test_debug_verdicts.py
  </files>
  <read_first>
shop/services/review.py,
shop/routers/review.py,
tests/test_review.py,
tests/test_debug_verdicts.py,
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-RESEARCH.md
  </read_first>
  <action>
Use the existing packet/review join contract in `build_debug_queue_state(...,
activate_review=False)` instead of inventing a second advisory lookup path.

1. In `shop/services/review.py`, add a helper named
   `advisory_flags_by_item_id(db: Session, run: Run) -> dict[int, list[str]]`
   that reads packet rows from `build_debug_queue_state` and returns each
   persisted `DeltaItem.confidence_flags` list keyed by `ReviewItem.id`.
   - Legacy packet rows that omit `confidence_flags` must yield `[]`.
   - Do not key by `char_no`; duplicate and `None` characteristic numbers are
     already valid in the debug export contract.
2. Extend `build_run_debug_summary()` so every row dict includes
   `confidence_flags`.
   - Packet-backed conforming and exception rows use the packet-native list.
   - Synthetic missing-added rows use `[]`.
3. In `shop/routers/review.py`, load the advisory map for both normal and debug
   review mode and pass it into the templates without changing queue ordering.

Add these exact tests:
- `tests/test_review.py::test_review_queue_surfaces_packet_confidence_flags_on_standard_item_cards`
- `tests/test_debug_verdicts.py::test_debug_queue_surfaces_packet_confidence_flags_without_rederiving_them`

The tests should seed packet rows whose `reasons` text is intentionally
different from the advisory text, then assert the rendered queue uses the
packet-native `confidence_flags` string rather than a re-derived warning phrase.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_review.py tests/test_debug_verdicts.py -k "confidence_flags_on_standard_item_cards or debug_queue_surfaces_packet_confidence_flags_without_rederiving_them" -x</automated>
  </verify>
  <acceptance_criteria>
    - `shop/services/review.py` contains `def advisory_flags_by_item_id(`.
    - `shop/services/review.py` contains `confidence_flags` inside `build_run_debug_summary(` row payloads.
    - `rg -n "confidence_flags" shop/services/review.py shop/routers/review.py` returns matches in both files.
    - `uv run pytest -q tests/test_review.py tests/test_debug_verdicts.py -k "confidence_flags_on_standard_item_cards or debug_queue_surfaces_packet_confidence_flags_without_rederiving_them" -x` exits 0.
  </acceptance_criteria>
  <done>Review-service state now exposes packet-native advisory flags per review row without collapsing duplicate identities or re-deriving warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Render advisory flags on normal review, debug review, and status surfaces</name>
  <files>
shop/templates/review/_item_card.html,
shop/templates/review/_item_card_debug.html,
shop/templates/runs/status.html,
tests/test_run_status_debug_summary.py,
tests/test_review.py,
tests/test_debug_verdicts.py
  </files>
  <read_first>
shop/templates/review/_item_card.html,
shop/templates/review/_item_card_debug.html,
shop/templates/runs/status.html,
tests/test_run_status_debug_summary.py,
tests/test_review.py,
tests/test_debug_verdicts.py,
.planning/phases/10-debug-exception-gating-and-advisory-surfacing/10-CONTEXT.md
  </read_first>
  <action>
Add advisory rendering to the existing maintainer surfaces with compact,
maintainer-first presentation. Required behavior:

1. Normal review queue item cards (`_item_card.html`) show a visible advisory
   block when the current row has non-empty `confidence_flags`.
2. Debug item cards (`_item_card_debug.html`) show the same packet-native flag
   list near the mismatch/debug verdict area so the maintainer sees the warning
   before entering a debug verdict.
3. The run status page (`runs/status.html`) shows flag text alongside the
   affected summary rows.
   - Exception rows with advisories must show those advisories in the debug
     summary area.
   - Conforming rows with advisories may remain in the "Canonical matches"
     section, but the advisory text must still be visible on the row.

Use the exact packet text. Do not paraphrase
`"Rev B text may contain adjacent balloon content"` into a template-only alias.

Add this exact test:
- `tests/test_run_status_debug_summary.py::test_status_page_surfaces_confidence_flags_for_exception_rows`

That test should assert the status page shows the advisory string and still
retains the existing unresolved-exception counts/CTA behavior.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_review.py tests/test_run_status_debug_summary.py tests/test_debug_verdicts.py -k "confidence_flags or advisory" -x</automated>
  </verify>
  <acceptance_criteria>
    - `shop/templates/review/_item_card.html` contains `Packet Advisories`.
    - `shop/templates/review/_item_card_debug.html` contains `Packet Advisories`.
    - `shop/templates/runs/status.html` contains `confidence_flags`.
    - `uv run pytest -q tests/test_review.py tests/test_run_status_debug_summary.py tests/test_debug_verdicts.py -k "confidence_flags or advisory" -x` exits 0.
  </acceptance_criteria>
  <done>Maintainer-facing review/status surfaces now render packet-native advisory flags next to the rows they annotate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Packet row -> review row rendering | Advisory flags must attach to the exact persisted review row, not just a matching `char_no`. |
| Packet advisory text -> maintainer UI | The UI must surface packet-native flags, not warnings reconstructed from weaker proxy fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Integrity | review/advisory mapping | mitigate | Join advisory state by `ReviewItem.id` using existing packet row order from `build_debug_queue_state()`. |
| T-10-02 | Integrity | review/status templates | mitigate | Render `confidence_flags` directly from packet-derived context and default legacy packets to `[]`. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_review.py tests/test_debug_verdicts.py -k "confidence_flags_on_standard_item_cards or debug_queue_surfaces_packet_confidence_flags_without_rederiving_them" -x`
- [ ] `uv run pytest -q tests/test_review.py tests/test_run_status_debug_summary.py tests/test_debug_verdicts.py -k "confidence_flags or advisory" -x`
</verification>

<success_criteria>

- All tasks completed
- Packet-native advisory flags are visible on normal review, debug review, and run-status surfaces
- Duplicate/`None` characteristic rows retain the correct advisory state
- The surfaced advisory text exactly matches the packet’s `confidence_flags`

</success_criteria>
