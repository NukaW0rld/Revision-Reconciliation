---
phase: 09-full-corpus-added-characteristic-closure
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - delta_preservation/reconcile/classify.py
  - tests/test_added_detection_phase6.py
  - tests/test_phase6_asset_regression.py
autonomous: true
requirements: [ADD-01, ADD-02, SNP-01]
test_ids:
  - tests/test_added_detection_phase6.py::TestExplainedByMatchSuppression
  - tests/test_phase6_asset_regression.py
must_haves:
  truths:
    - "The Part 9 added `⏥ .01` row survives detection because matched-owner signatures no longer absorb unrelated same-row annotations."
    - "Known Part 3-5 exemplar misses are closed through shared detector/grouping behavior, not part-specific conditionals."
    - "Phase 6 false-positive suppressor guardrails remain green after the detector-side fixes."
  artifacts:
    - path: "delta_preservation/reconcile/classify.py"
      provides: "Narrowed matched-owner signature construction and shared added-detection behavior"
      contains: "matched_annotation_signatures"
    - path: "tests/test_added_detection_phase6.py"
      provides: "Positive and negative suppressor coverage for Phase 9 detector-side fixes"
      contains: "test_part9_flatness_survives_when_unrelated_same_row_match_exists"
    - path: "tests/test_phase6_asset_regression.py"
      provides: "Read-only regression guard against Phase 6 asset contract regressions"
      contains: "test_live_part8_pipeline_keeps_added_rows_distinct"
  key_links:
    - from: "delta_preservation/reconcile/classify.py"
      to: "tests/test_added_detection_phase6.py"
      via: "detect_added_characteristics / explained-by-match suppressor"
      pattern: "def detect_added_characteristics("
---

<objective>
Close the remaining detector-side Phase 9 misses without regressing the Phase 6
added-detection safety rails.

Purpose: after Plan 09-01, the remaining miss bucket should contain only true
detector/grouping losses. The confirmed Part 9 loss is caused by over-broad
matched-owner signatures in `detect_added_characteristics()`, while Parts 3-5
still contain a heterogeneous set of missed added callouts (`FINISH TURN 1000 Ra`,
`⌵ Ø.531 X 82.0°`, `⌖ ∅.005Ⓜ A B C`, `1.250`, `800`, `3X Ø18 ↧30`,
`M20x2.5 − 6H ↧6`). This plan repairs the shared detector path so those
families surface as canonical added rows without part-specific logic.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-CONTEXT.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-PATTERNS.md
@.planning/debug/phase06-aggregate-added-gaps.md
@.planning/debug/phase06-part9-flatness-miss.md
@delta_preservation/reconcile/classify.py
@tests/test_added_detection_phase6.py
@tests/test_phase6_asset_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow explained-by-match owner signatures so the Part 9 flatness row survives</name>
  <files>
delta_preservation/reconcile/classify.py,
tests/test_added_detection_phase6.py
  </files>
  <read_first>
.planning/debug/phase06-part9-flatness-miss.md,
delta_preservation/reconcile/classify.py,
tests/test_added_detection_phase6.py,
.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
  </read_first>
  <action>
In `delta_preservation/reconcile/classify.py`, replace the current matched-owner
signature expansion that sweeps unmatched same-row spans within `±200 pt` into a
matched annotation.

Target state:

- A matched-owner signature may include:
  - spans already present in `source_spans`
  - unmatched companion spans that satisfy `_spans_are_annotation_companions(...)`
    when walked from the matched span / source spans to a fixed point
- A matched-owner signature may **not** include an unmatched span only because it
  shares the same row and falls within an arbitrary ±200 pt horizontal window.
- Keep the two-part suppressor gate unchanged:
  - content subset must still pass
  - bbox containment/overlap must still pass

Add these exact regression tests to `tests/test_added_detection_phase6.py`:

1. `test_part9_flatness_survives_when_unrelated_same_row_match_exists`
   - reproduces the missing `⏥ .01` row near truth index 42
   - proves the candidate survives when an unrelated matched owner shares the row
2. `test_part8_fragment_still_suppressed_after_owner_signature_narrowing`
   - reuses the existing Part 8-style fragment pattern
   - proves the narrower signature still suppresses a true explained fragment

Do not delete the explained-by-match suppressor. Do not change the bbox-overlap
threshold from the existing Phase 6 contract unless a new direct test proves the
current threshold is the blocker.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_added_detection_phase6.py -k "part9_flatness_survives_when_unrelated_same_row_match_exists or part8_fragment_still_suppressed_after_owner_signature_narrowing or suppression" -x</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/reconcile/classify.py` no longer contains the literal same-row sweep condition `ox0 > mx1 + 200.0 or ox1 < mx0 - 200.0` inside matched-owner signature assembly.
    - `tests/test_added_detection_phase6.py` contains `test_part9_flatness_survives_when_unrelated_same_row_match_exists`.
    - `tests/test_added_detection_phase6.py` contains `test_part8_fragment_still_suppressed_after_owner_signature_narrowing`.
    - `uv run pytest -q tests/test_added_detection_phase6.py -k "part9_flatness_survives_when_unrelated_same_row_match_exists or part8_fragment_still_suppressed_after_owner_signature_narrowing or suppression" -x` exits 0.
  </acceptance_criteria>
  <done>The Part 9 flatness row is no longer falsely absorbed by an unrelated matched owner, and the Phase 6 suppressor guardrails remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Close the remaining Parts 3-5 miss families through shared detector/grouping behavior</name>
  <files>
delta_preservation/reconcile/classify.py,
tests/test_added_detection_phase6.py,
tests/test_phase6_asset_regression.py
  </files>
  <read_first>
.planning/debug/phase06-aggregate-added-gaps.md,
delta_preservation/reconcile/classify.py,
tests/test_added_detection_phase6.py,
tests/test_phase6_asset_regression.py,
.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
  </read_first>
  <action>
Start by reproducing the current Parts 3-5 miss families from the Phase 9
research note and current packets/fixtures, then close them with the smallest
shared-path changes inside `detect_added_characteristics()` and its helper
grouping logic.

The known exemplar rows that must be representable as canonical added rows are:

- Part 3: `FINISH TURN 1000 Ra`, `⌵ Ø.531 X 82.0°`
- Part 4: `⌖ ∅.005Ⓜ A B C`, both `1.250` rows
- Part 5: `800`, `3X Ø18 ↧30`, `M20x2.5 − 6H ↧6`

Required implementation rules:

- Keep all fixes in shared detector/grouping code. Do not branch on part number,
  truth index, or asset path.
- If a missed row is a surface-finish or multi-token callout, group enough
  companion text to emit the full canonical requirement, not a fragment.
- If a missed row is a plain-decimal or plain-integer dimension, keep the
  existing Phase 6 title-block / boilerplate exclusions intact.
- If the only remaining reproducer after these detector changes is a glyph alias
  mismatch (`↗` / `⌰` form only), stop and implement the minimal shared alias
  normalization needed to make the packet row canonical. Do not widen alias work
  beyond the reproducer.

Add targeted regression tests to `tests/test_added_detection_phase6.py` for the
detector families you close. Use exact exemplar names in the test strings so the
result is grep-verifiable. Keep `tests/test_phase6_asset_regression.py` read-only
relative to the frozen historical assets: use it only to prove the detector-side
changes did not regress the established Phase 6 contract.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_added_detection_phase6.py` contains the literal exemplar strings `FINISH TURN 1000 Ra`, `⌵ Ø.531 X 82.0°`, `⌖ ∅.005Ⓜ A B C`, `3X Ø18 ↧30`, and `M20x2.5 − 6H ↧6`.
    - `delta_preservation/reconcile/classify.py` still contains `span_is_excluded_for_annotation_search(` after the detector changes.
    - `uv run pytest -q tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x` exits 0.
    - `git diff --name-only -- assets/debug_report_part1.json assets/debug_report_part2.json assets/debug_report_part3.json assets/debug_report_part4.json assets/debug_report_part5.json assets/debug_report_part6.json assets/debug_report_part7.json assets/debug_report_part8.json assets/debug_report_part9.json` produces no output.
  </acceptance_criteria>
  <done>The remaining Parts 3-5 detector-side misses are closed through shared grouping/detection logic, and the historical Phase 6 asset contract stays untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matched-owner signatures -> explained-by-match suppressor | Owner signatures must represent only the real matched annotation, not unrelated same-row neighbors. |
| Detector grouping -> historical Phase 6 safeguards | Closing new miss families must not reintroduce fragment false positives or title-block regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Integrity | `delta_preservation/reconcile/classify.py` | mitigate | Replace the ±200 pt same-row sweep with companion-only grouping for owner signatures. |
| T-09-04 | Integrity | detector/grouping heuristics | mitigate | Close Part 3-5 exemplar families with shared-path logic and direct regression tests, never part-number branches. |
| T-09-05 | Evidence | frozen Phase 6 assets | mitigate | Keep `tests/test_phase6_asset_regression.py` read-only and verify that no `assets/debug_report_part*.json` files are modified. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_added_detection_phase6.py -k "part9_flatness_survives_when_unrelated_same_row_match_exists or part8_fragment_still_suppressed_after_owner_signature_narrowing or suppression" -x`
- [ ] `uv run pytest -q tests/test_added_detection_phase6.py tests/test_phase6_asset_regression.py -x`
- [ ] `git diff --name-only -- assets/debug_report_part1.json assets/debug_report_part2.json assets/debug_report_part3.json assets/debug_report_part4.json assets/debug_report_part5.json assets/debug_report_part6.json assets/debug_report_part7.json assets/debug_report_part8.json assets/debug_report_part9.json`
</verification>

<success_criteria>

- All tasks completed
- The Part 9 `⏥ .01` row survives added detection for the right reason
- Parts 3-5 known exemplar misses are closed through shared detector behavior
- Phase 6 false-positive and exclusion guardrails remain green
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-corpus-added-characteristic-closure/09-02-SUMMARY.md`
</output>
