---
phase: 09-full-corpus-added-characteristic-closure
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - tests/fixtures/phase7_algorithm_only/part1-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part2-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part3-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part4-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part5-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part6-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part7-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part8-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part9-debug-report.json
  - tests/test_phase7_benchmark.py
  - .planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md
  - .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
  - .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md
  - .planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md
  - .planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md
autonomous: true
requirements: [ADD-01, ADD-02, SNP-01]
test_ids:
  - tests/test_phase7_benchmark.py::TestCrossPartBenchmark
  - tests/test_phase6_asset_regression.py
must_haves:
  truths:
    - "Fresh standalone reruns for parts 1-9 produce empty `missing_added_truth_indexes` across the whole corpus, including Part 5 after Plans 04 and 05 land."
    - "`BASELINE_COUNTS` in `tests/test_phase7_benchmark.py` encodes `max_missing_added=0` for every part, and the corresponding `min_conforming` values match the refreshed fixtures."
    - "Phase 06/07/09 verification artifacts reflect the final full-corpus closure, and the 09-02-SUMMARY `contains` mismatch flagged in `09-VERIFICATION.md` is corrected against the real Phase 6 test name."
  artifacts:
    - path: "tests/fixtures/phase7_algorithm_only/part5-debug-report.json"
      provides: "Refreshed Part 5 algorithm-only fixture proving indexes 16 and 17 are closed"
      contains: "\"missing_added_truth_indexes\": []"
    - path: "tests/test_phase7_benchmark.py"
      provides: "Benchmark baseline pinned at zero missing-added for all 9 parts"
      contains: "max_missing_added"
    - path: ".planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md"
      provides: "Post-closure algorithm-only baseline provenance"
      contains: "Phase 9 full-corpus closure"
    - path: ".planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md"
      provides: "Re-verification note confirming Part 5 closure and contains-mismatch correction"
      contains: "Phase 9 re-verification"
  key_links:
    - from: "tests/test_phase7_benchmark.py"
      to: "tests/fixtures/phase7_algorithm_only/"
      via: "FIXTURES_DIR / BASELINE_COUNTS"
      pattern: "phase7_algorithm_only"
---

<objective>
After Plans 04 (near-boilerplate filter) and 05 (matching dimensional-
incompatibility guard) land, rerun the authoritative standalone pipeline for
every part, refresh the Phase 7 algorithm-only fixture set and benchmark
baseline to reflect `missing_added_truth_indexes=[]` for all 9 parts (including
Part 5), correct the minor plan-02 `contains` mismatch noted in
`09-VERIFICATION.md`, and close out the Phase 06/07/09 verification docs.

Purpose: Plan 03 had to set `max_missing_added=2` for Part 5 because indexes 16
and 17 were deferred architectural gaps. With the fixes from Plans 04 and 05
in place, Part 5's algorithm-only output should now match the rest of the
corpus at zero. This plan is the evidence-refresh wave that converts the code
fixes into a stable benchmark contract and the final milestone closure.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-CONTEXT.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-03-SUMMARY.md
@.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md
@.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md
@run.py
@tests/test_phase7_benchmark.py
@tests/test_phase6_asset_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the 9-part algorithm-only fixture set and pin BASELINE_COUNTS at zero</name>
  <files>
tests/fixtures/phase7_algorithm_only/part1-debug-report.json,
tests/fixtures/phase7_algorithm_only/part2-debug-report.json,
tests/fixtures/phase7_algorithm_only/part3-debug-report.json,
tests/fixtures/phase7_algorithm_only/part4-debug-report.json,
tests/fixtures/phase7_algorithm_only/part5-debug-report.json,
tests/fixtures/phase7_algorithm_only/part6-debug-report.json,
tests/fixtures/phase7_algorithm_only/part7-debug-report.json,
tests/fixtures/phase7_algorithm_only/part8-debug-report.json,
tests/fixtures/phase7_algorithm_only/part9-debug-report.json,
tests/test_phase7_benchmark.py
  </files>
  <read_first>
run.py,
tests/test_phase7_benchmark.py,
tests/fixtures/phase7_algorithm_only/README.md,
.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-03-SUMMARY.md
  </read_first>
  <action>
Run the standalone pipeline for each part and refresh fixtures. Concrete steps:

1. Execute `uv run python run.py part1` through `uv run python run.py part9`
   (one invocation per part) using the current HEAD with Plans 01–05 applied.
2. For each part, regenerate
   `tests/fixtures/phase7_algorithm_only/partN-debug-report.json`. Use the same
   top-level shape as the existing fixtures (see `09-03-SUMMARY.md` for the
   exact fields): `baseline_commit`, `packet_run_id`, `part_name`, `items`,
   `missing_added_truth_indexes`, `conforming_count`, `review_needed_count`.
   Set `baseline_commit` to the short SHA of the current HEAD at the time of
   regeneration.
3. After all 9 fixtures are written, confirm via a short Python check (see
   automated verify below) that every regenerated fixture has
   `missing_added_truth_indexes == []`. If any part still has a non-empty list,
   STOP and hand back to the author; do not proceed to the benchmark update
   with a partial closure.
4. Update `tests/test_phase7_benchmark.py`:
   - Set `BASELINE_COUNTS[partN]["max_missing_added"] = 0` for every part
     (including part5 which previously was `2`).
   - Set `BASELINE_COUNTS[partN]["min_conforming"]` to the refreshed
     `conforming_count` from the corresponding fixture (use the regenerated
     value exactly; do not hand-edit).
   - Remove any inline comment that documents part5 indexes 16/17 as
     architectural deferrals; replace with a short note that Phase 9 closure
     proved zero missing-added for all 9 parts.

Do NOT modify `assets/debug_report_part*.json`. Do NOT advance to later tasks
until all 9 regenerated fixtures have empty `missing_added_truth_indexes` and
the benchmark tests are green.
  </action>
  <verify>
    <automated>uv run python - <<'PY'
import json
from pathlib import Path
fixture_dir = Path("tests/fixtures/phase7_algorithm_only")
for idx in range(1, 10):
    payload = json.loads((fixture_dir / f"part{idx}-debug-report.json").read_text())
    assert payload["missing_added_truth_indexes"] == [], (idx, payload["missing_added_truth_indexes"])
print("ok")
PY
uv run python - <<'PY'
from tests.test_phase7_benchmark import BASELINE_COUNTS
assert sorted(BASELINE_COUNTS) == [f"part{i}" for i in range(1, 10)]
assert all(bounds["max_missing_added"] == 0 for bounds in BASELINE_COUNTS.values()), BASELINE_COUNTS
print("ok")
PY
uv run pytest -q tests/test_phase7_benchmark.py tests/test_phase6_asset_regression.py -x
git diff --name-only -- assets/debug_report_part1.json assets/debug_report_part2.json assets/debug_report_part3.json assets/debug_report_part4.json assets/debug_report_part5.json assets/debug_report_part6.json assets/debug_report_part7.json assets/debug_report_part8.json assets/debug_report_part9.json</automated>
  </verify>
  <acceptance_criteria>
    - The first `uv run python` heredoc above exits 0 (every regenerated fixture has `missing_added_truth_indexes == []`).
    - The second `uv run python` heredoc exits 0 (`BASELINE_COUNTS` covers parts 1–9 and every `max_missing_added` is `0`).
    - `uv run pytest -q tests/test_phase7_benchmark.py tests/test_phase6_asset_regression.py -x` exits 0.
    - `git diff --name-only -- assets/debug_report_part*.json` returns no output (historical Phase 6 assets untouched).
    - `rg -n "max_missing_added.{0,3}:\s*2" tests/test_phase7_benchmark.py` returns no matches (no remaining part5 deferral).
  </acceptance_criteria>
  <done>The algorithm-only fixture set and benchmark baseline now encode zero missing-added rows for every part without touching frozen historical Phase 6 assets.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the plan-02 contains mismatch for test_phase6_asset_regression.py</name>
  <files>
.planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md
  </files>
  <read_first>
.planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md,
tests/test_phase6_asset_regression.py
  </read_first>
  <action>
`09-VERIFICATION.md` flags a minor artifact mismatch: `09-02-PLAN.md` declares
a `contains` value of
`test_live_part8_packet_preserves_separate_runout_and_diameter_rows` for
`tests/test_phase6_asset_regression.py`, but the actual function in that file
is `test_live_part8_pipeline_keeps_added_rows_distinct` (see line ~365 of
`tests/test_phase6_asset_regression.py`). Both names describe the same guard —
"Part 8 runout and diameter rows stay distinct after added detection".

Open `.planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md`
and update the single `contains` field inside the `artifacts:` block for
`tests/test_phase6_asset_regression.py`. Replace the incorrect test name with
the real one:

- Old: `contains: "test_live_part8_packet_preserves_separate_runout_and_diameter_rows"`
- New: `contains: "test_live_part8_pipeline_keeps_added_rows_distinct"`

Do NOT alter any other field in `09-02-PLAN.md`. Do NOT rename the underlying
test function in `tests/test_phase6_asset_regression.py` — the plan should
match the code, not the other way round.
  </action>
  <verify>
    <automated>rg -n "test_live_part8_packet_preserves_separate_runout_and_diameter_rows" .planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md
rg -n "test_live_part8_pipeline_keeps_added_rows_distinct" .planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md
rg -n "^def test_live_part8_pipeline_keeps_added_rows_distinct\(" tests/test_phase6_asset_regression.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_live_part8_packet_preserves_separate_runout_and_diameter_rows" .planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md` returns no matches (the stale test name is gone).
    - `rg -n "test_live_part8_pipeline_keeps_added_rows_distinct" .planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md` returns at least one match (the corrected test name is present in the `contains` field).
    - `rg -n "^def test_live_part8_pipeline_keeps_added_rows_distinct\(" tests/test_phase6_asset_regression.py` returns exactly one match (the underlying test was not renamed).
  </acceptance_criteria>
  <done>The plan-02 `contains` field now matches the actual Phase 6 test name, closing the minor artifact mismatch flagged by `09-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh 06/07/09 verification documents and re-verify</name>
  <files>
.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md
  </files>
  <read_first>
.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-03-SUMMARY.md
  </read_first>
  <action>
After Tasks 1 and 2 are green, update the four verification/baseline documents
below to reflect the final Phase 9 full-corpus closure. All edits must be
additive or a narrow replacement of the affected row/sentence; do not rewrite
entire documents.

1. `.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md`
   - Replace the Part 5 row in the per-part counts table so its
     `Missing-Added (indexes)` column reads `[]` (matching the refreshed
     fixture) and its conforming/review counts match the new fixture totals.
   - Append a short "Phase 9 full-corpus closure" note at the end of the
     document stating that Part 5 indexes 16 and 17 are now closed by the
     near-boilerplate filter refinement (Plan 04) and the matching-layer
     dimensional-incompatibility guard (Plan 05), and that
     `max_missing_added=0` now holds for every part.

2. `.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md`
   - Extend the existing "Phase 9 Closure Note" section with a follow-up
     paragraph explaining that after Plans 04 and 05 the Part 5 deferral is
     resolved and the algorithm-only fixture set now shows zero missing-added
     rows for all 9 parts.

3. `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md`
   - Update the ADD-01 Requirements Coverage row from
     "VERIFIED (Phase 9 closure with Part 5 deferrals)" (or equivalent) to
     "VERIFIED (Phase 9 full-corpus closure, parts 1–9 at zero missing-added)".
     Do not invent statuses beyond VERIFIED.

4. `.planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md`
   - Add a new top-level section titled `## Phase 9 Re-verification` below the
     existing body. It must:
     * State the re-verification date (today's date in ISO-8601).
     * Show the updated Observable Truth #1 as VERIFIED with evidence citing
       Plans 04, 05, and 06.
     * Note that the Part 5 `missing_added_truth_indexes` is now `[]` and
       `BASELINE_COUNTS["part5"]["max_missing_added"]` is now `0`.
     * Close the "Gaps Summary" section by marking both gaps (indexes 16 and 17)
       as CLOSED with one-sentence pointers to Plans 04 and 05.
     * Note that the plan-02 `contains` mismatch was corrected in Plan 06 Task 2.
   - Update the YAML front-matter `status:` from `gaps_found` to `verified` and
     the `score:` field to `3/3 must-haves verified`.

Do NOT edit `.planning/REQUIREMENTS.md` in this task — ADD-01 is already
checked (Plan 03) and the traceability row correctly points at Plans 01–03
plus 04–06 via the Phase 9 milestone; the traceability line does not need to
list every sub-plan.

After the doc edits, run the full test suite one more time to prove nothing
regressed while the docs were being updated.
  </action>
  <verify>
    <automated>rg -n "Phase 9 full-corpus closure" .planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md
rg -n "Phase 9 Closure Note" .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
rg -n "VERIFIED \(Phase 9 full-corpus closure" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md
rg -n "## Phase 9 Re-verification" .planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md
rg -n "^status: verified$" .planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md
uv run pytest -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase 9 full-corpus closure" .planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md` returns at least one match.
    - `rg -n "Phase 9 Closure Note" .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` returns at least one match (the section still exists after the follow-up paragraph).
    - `rg -n "VERIFIED \(Phase 9 full-corpus closure" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md` returns at least one match.
    - `rg -n "## Phase 9 Re-verification" .planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md` returns exactly one match.
    - `rg -n "^status: verified$" .planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md` returns exactly one match.
    - `uv run pytest -q -x` exits 0 for the full suite.
  </acceptance_criteria>
  <done>Phase 06/07/09 verification documents reflect the final full-corpus closure, the plan-02 `contains` mismatch is corrected, and the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh standalone rerun output -> committed benchmark fixtures | Refreshed fixtures must represent current algorithm behaviour with Plans 04–05 applied; they must not be captured from an intermediate state. |
| Refreshed algorithm-only fixtures -> `BASELINE_COUNTS` | The benchmark must be updated only after all 9 regenerated fixtures have `missing_added_truth_indexes == []`. |
| Phase 9 closure evidence -> downstream milestone artifacts | Verification documents must be refreshed atomically so no downstream reader sees a mix of "deferred" and "closed" statements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Integrity | refreshed algorithm-only fixtures | mitigate | Require all 9 refreshed fixtures to have empty `missing_added_truth_indexes` before updating `BASELINE_COUNTS`. |
| T-09-16 | Traceability | 06/07/09 verification docs | mitigate | Gate the doc refresh on green benchmark and full-suite results so the narrative matches runtime behaviour. |
| T-09-17 | Integrity | historical Phase 6 assets | mitigate | Verify via `git diff --name-only -- assets/debug_report_part*.json` that no historical snapshot was mutated. |
</threat_model>

<verification>
- [ ] `uv run python` fixture heredoc (parts 1–9 all have `missing_added_truth_indexes == []`)
- [ ] `uv run python` BASELINE_COUNTS heredoc (every `max_missing_added == 0`)
- [ ] `uv run pytest -q tests/test_phase7_benchmark.py tests/test_phase6_asset_regression.py -x`
- [ ] `rg -n "test_live_part8_pipeline_keeps_added_rows_distinct" .planning/phases/09-full-corpus-added-characteristic-closure/09-02-PLAN.md`
- [ ] `rg -n "## Phase 9 Re-verification" .planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md`
- [ ] `rg -n "^status: verified$" .planning/phases/09-full-corpus-added-characteristic-closure/09-VERIFICATION.md`
- [ ] `uv run pytest -q -x`
- [ ] `git diff --name-only -- assets/debug_report_part*.json` -> no output
</verification>

<success_criteria>

- All tasks completed
- Fresh standalone evidence proves zero missing-added rows across parts 1–9, including Part 5
- `BASELINE_COUNTS` pins `max_missing_added=0` for every part
- Plan-02 `contains` mismatch corrected
- Phase 06/07/09 verification artifacts reflect the final full-corpus closure
- Historical Phase 6 assets untouched
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-corpus-added-characteristic-closure/09-06-SUMMARY.md`
</output>
