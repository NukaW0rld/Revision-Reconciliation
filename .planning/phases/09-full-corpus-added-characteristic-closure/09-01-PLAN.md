---
phase: 09-full-corpus-added-characteristic-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - delta_preservation/evaluation/conformance.py
  - shop/services/review.py
  - tests/test_added_truth_selection.py
  - tests/test_debug_row_identity.py
autonomous: true
requirements: [ADD-01]
test_ids:
  - tests/test_added_truth_selection.py::TestDuplicateAddedTruthSelection
  - tests/test_debug_row_identity.py::test_no_missing_added_when_all_truth_added_rows_are_claimed
must_haves:
  truths:
    - "Canonical added rows matched by the evaluator are serialized with one stable token contract so debug/missing-added accounting does not lose them when the truth row still has a `char_no`."
    - "Leading-zero formatting variants such as `0.635 / 0.615` and `.635 / .615` normalize to the same deterministic added-text form without introducing fuzzy matching."
    - "The Phase 9 non-detector misses on Part 1 and Part 2 are closed in shared evaluator/accounting code, not by UI masking or ground-truth edits."
  artifacts:
    - path: "delta_preservation/evaluation/conformance.py"
      provides: "Canonical added-row truth-token and normalization behavior"
      contains: "def _truth_match_token("
    - path: "shop/services/review.py"
      provides: "Backward-compatible debug missing-added accounting"
      contains: "def _load_missing_added_truth_items("
    - path: "tests/test_added_truth_selection.py"
      provides: "Focused evaluator coverage for Phase 9 token and normalization fixes"
      contains: "test_added_truth_match_token_uses_added_pool_prefix_even_when_truth_row_has_char_no"
    - path: "tests/test_debug_row_identity.py"
      provides: "Debug-queue regression coverage for canonical and legacy added-token shapes"
      contains: "test_missing_added_accepts_legacy_int_token_for_added_truth_row_char_no"
  key_links:
    - from: "delta_preservation/evaluation/conformance.py"
      to: "shop/services/review.py"
      via: "matched_truth_char_no / added truth token contract"
      pattern: "ADDED_POOL_TOKEN_PREFIX"
---

<objective>
Close the non-detector Phase 9 failures first so the remaining miss bucket
contains only true detector-side gaps.

Purpose: Part 1 is currently a false missing-added row caused by token/accounting
drift between the evaluator and `shop/services/review.py`, while Part 2 remains
blocked on deterministic normalization of `0.635 / 0.615` versus `.635 / .615`.
This plan fixes both issues in the shared packet/evaluation contract so later
benchmark refresh work can trust `missing_added_truth_indexes` again.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-CONTEXT.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-PATTERNS.md
@.planning/debug/phase06-aggregate-added-gaps.md
@delta_preservation/evaluation/conformance.py
@shop/services/review.py
@tests/test_added_truth_selection.py
@tests/test_debug_row_identity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify canonical added-row token semantics between the evaluator and debug accounting</name>
  <files>
delta_preservation/evaluation/conformance.py,
shop/services/review.py,
tests/test_added_truth_selection.py,
tests/test_debug_row_identity.py
  </files>
  <read_first>
.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md,
.planning/debug/phase06-aggregate-added-gaps.md,
delta_preservation/evaluation/conformance.py,
shop/services/review.py,
tests/test_added_truth_selection.py,
tests/test_debug_row_identity.py
  </read_first>
  <action>
In `delta_preservation/evaluation/conformance.py`, change `_truth_match_token(...)`
to use one canonical rule:

- If `truth_row.classification == "added"`, always return
  `f"{ADDED_POOL_TOKEN_PREFIX}:{truth_index}"`, even when the canonical truth row
  still carries a legacy `char_no`.
- Keep integer `char_no` tokens only for non-added truth rows.

Then make `shop/services/review.py::_load_missing_added_truth_items(...)`
backward-compatible while preferring the canonical token:

- Continue counting canonical `added:<index>` tokens exactly as today.
- Also accept a legacy integer token **only when** that integer equals the
  `char_no` of a truth row whose `classification == "added"`.
- Do not treat arbitrary integer tokens as added-row claims; only consume the
  integer when it resolves to an added truth row in the loaded fixture.

Add these exact regression tests:

1. In `tests/test_added_truth_selection.py` add
   `test_added_truth_match_token_uses_added_pool_prefix_even_when_truth_row_has_char_no`
   asserting that an added truth row with `char_no=39` still yields
   `matched_truth_char_no == "added:0"` (or the appropriate pool index), not `39`.
2. In `tests/test_debug_row_identity.py` add
   `test_missing_added_accepts_legacy_int_token_for_added_truth_row_char_no`
   proving the review-layer collector treats a legacy integer token as claimed
   only when that integer belongs to an added truth row.

Do not change ground-truth fixtures. Do not patch the debug queue by hiding the
missing row after the fact; fix the shared truth-token contract instead.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_added_truth_selection.py tests/test_debug_row_identity.py -k "added_truth_match_token_uses_added_pool_prefix_even_when_truth_row_has_char_no or missing_added_accepts_legacy_int_token_for_added_truth_row_char_no" -x</automated>
  </verify>
  <acceptance_criteria>
    - `delta_preservation/evaluation/conformance.py` contains `truth_row.classification == "added"` or equivalent logic inside `_truth_match_token(`.
    - `delta_preservation/evaluation/conformance.py` contains `f"{ADDED_POOL_TOKEN_PREFIX}:{truth_index}"`.
    - `rg -n "test_added_truth_match_token_uses_added_pool_prefix_even_when_truth_row_has_char_no|test_missing_added_accepts_legacy_int_token_for_added_truth_row_char_no" tests/test_added_truth_selection.py tests/test_debug_row_identity.py` returns two matches.
    - `uv run pytest -q tests/test_added_truth_selection.py tests/test_debug_row_identity.py -k "added_truth_match_token_uses_added_pool_prefix_even_when_truth_row_has_char_no or missing_added_accepts_legacy_int_token_for_added_truth_row_char_no" -x` exits 0.
  </acceptance_criteria>
  <done>Canonical added rows now flow through one shared token contract, and debug accounting remains correct for both new and legacy packet shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize leading-zero added-text variants deterministically for Part 2</name>
  <files>
delta_preservation/evaluation/conformance.py,
tests/test_added_truth_selection.py
  </files>
  <read_first>
delta_preservation/evaluation/conformance.py,
tests/test_added_truth_selection.py,
.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-CONTEXT.md
  </read_first>
  <action>
Extend `_normalize_requirement_text(...)` in
`delta_preservation/evaluation/conformance.py` so Phase 9 treats harmless
leading-zero variants in added dimensions as equal.

Required target behavior:

- `0.635 / 0.615` and `.635 / .615` normalize to the same final string.
- `0.50 ± 0.05` and `.50 ± .05` normalize to the same final string.
- Keep full integer-bearing values such as `10.000±.001`, `800`, and `1.250`
  unchanged apart from the function's existing whitespace/symbol normalization.

Implement this as a deterministic token-level rewrite after the current
`parse_requirement(...)` / semantic normalization path:

- Only strip the leading zero from tokens matching `0.<digits>` or signed
  variants like `+0.<digits>` / `-0.<digits>`.
- Do not change tokens where the integer part is greater than zero
  (`10.000`, `1000`, `18`, etc.).
- Keep the current control-symbol spacing normalization intact.

Add this exact regression test to `tests/test_added_truth_selection.py`:

- `test_unique_exact_text_accepts_leading_zero_variant_for_stacked_limits`

The test must assert that a packet item using `0.635 / 0.615` selects the same
unique added truth row as a canonical truth value `.635 / .615`.
  </action>
  <verify>
    <automated>uv run python - <<'PY'
from delta_preservation.evaluation.conformance import _normalize_requirement_text
assert _normalize_requirement_text("0.635 / 0.615") == _normalize_requirement_text(".635 / .615")
assert _normalize_requirement_text("+0.50 ± 0.05") == _normalize_requirement_text("+.50 ± .05")
assert _normalize_requirement_text("10.000±.001") == _normalize_requirement_text("10.000±.001")
print("ok")
PY
uv run pytest -q tests/test_added_truth_selection.py -k "leading_zero_variant" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "leading zero|0\\.635|\\.635 / \\.615" delta_preservation/evaluation/conformance.py tests/test_added_truth_selection.py` returns at least one match in the test file and one match in the implementation or accompanying comment.
    - `tests/test_added_truth_selection.py` contains `test_unique_exact_text_accepts_leading_zero_variant_for_stacked_limits`.
    - `uv run python - <<'PY'\nfrom delta_preservation.evaluation.conformance import _normalize_requirement_text\nassert _normalize_requirement_text(\"0.635 / 0.615\") == _normalize_requirement_text(\".635 / .615\")\nassert _normalize_requirement_text(\"+0.50 ± 0.05\") == _normalize_requirement_text(\"+.50 ± .05\")\nassert _normalize_requirement_text(\"10.000±.001\") == _normalize_requirement_text(\"10.000±.001\")\nprint(\"ok\")\nPY` exits 0.
    - `uv run pytest -q tests/test_added_truth_selection.py -k "leading_zero_variant" -x` exits 0.
  </acceptance_criteria>
  <done>Phase 9 now treats harmless leading-zero formatting differences as one deterministic added-text form without introducing fuzzy matching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evaluator truth selection -> debug queue accounting | `matched_truth_char_no` must mean the same thing everywhere so missing-added reporting does not invent or hide rows. |
| Added text normalization -> truth claiming | Part 2 formatting differences must normalize deterministically without collapsing distinct real dimensions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Integrity | `delta_preservation/evaluation/conformance.py` | mitigate | Emit one canonical token contract for added rows regardless of legacy `char_no` presence. |
| T-09-02 | Integrity | `shop/services/review.py` | mitigate | Keep review accounting backward-compatible for legacy integer tokens, but only when they resolve to added truth rows. |
| T-09-03 | Integrity | `_normalize_requirement_text()` | mitigate | Restrict normalization to `0.<digits>` token forms so whole-number dimensions remain unchanged. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_added_truth_selection.py tests/test_debug_row_identity.py -k "added_truth_match_token_uses_added_pool_prefix_even_when_truth_row_has_char_no or missing_added_accepts_legacy_int_token_for_added_truth_row_char_no" -x`
- [ ] `uv run python - <<'PY'\nfrom delta_preservation.evaluation.conformance import _normalize_requirement_text\nassert _normalize_requirement_text(\"0.635 / 0.615\") == _normalize_requirement_text(\".635 / .615\")\nassert _normalize_requirement_text(\"+0.50 ± 0.05\") == _normalize_requirement_text(\"+.50 ± .05\")\nassert _normalize_requirement_text(\"10.000±.001\") == _normalize_requirement_text(\"10.000±.001\")\nprint(\"ok\")\nPY`
- [ ] `uv run pytest -q tests/test_added_truth_selection.py -k "leading_zero_variant" -x`
</verification>

<success_criteria>

- All tasks completed
- Added-row truth claims use one canonical token contract across evaluator and debug accounting
- Part 1 false missing-added accounting is eliminated in shared code
- Part 2 leading-zero variants select the canonical added truth row deterministically
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-corpus-added-characteristic-closure/09-01-SUMMARY.md`
</output>
