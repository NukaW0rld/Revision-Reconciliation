---
phase: 09-full-corpus-added-characteristic-closure
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - tests/fixtures/phase7_algorithm_only/part1-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part2-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part3-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part4-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part5-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part6-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part7-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part8-debug-report.json
  - tests/fixtures/phase7_algorithm_only/part9-debug-report.json
  - tests/test_phase7_benchmark.py
  - .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md
  - .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md
  - .planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md
  - .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ADD-01, ADD-02, SNP-01]
test_ids:
  - tests/test_phase7_benchmark.py::TestCrossPartBenchmark
must_haves:
  truths:
    - "Fresh standalone reruns for parts 1-9 produce empty `missing_added_truth_indexes` across the full corpus."
    - "The refreshed Phase 7 algorithm-only fixture set and benchmark baseline encode zero missing-added rows without overwriting frozen historical Phase 6 assets."
    - "Phase 06/07 verification artifacts and ADD-01 traceability are refreshed to reflect the final Phase 9 closure."
  artifacts:
    - path: "tests/test_phase7_benchmark.py"
      provides: "Updated benchmark baseline requiring zero missing-added rows"
      contains: "BASELINE_COUNTS"
    - path: ".planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md"
      provides: "Post-Phase-9 algorithm-only baseline provenance"
      contains: "Missing-Added (indexes)"
    - path: ".planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md"
      provides: "Parity-corrected verification report updated with final Phase 9 closure"
      contains: "Phase 9"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ADD-01 completion and Phase 9 traceability"
      contains: "| ADD-01 | Phase 9 |"
  key_links:
    - from: "tests/test_phase7_benchmark.py"
      to: "tests/fixtures/phase7_algorithm_only/"
      via: "FIXTURES_DIR"
      pattern: "phase7_algorithm_only"
---

<objective>
Refresh the accepted corpus evidence and traceability only after the packet and
detector paths are fixed.

Purpose: the roadmap contract for Phase 9 is not complete until the standalone
9-part rerun, algorithm-only benchmark fixtures, and Phase 06/07 verification
artifacts all reflect the final zero-miss state. This plan captures fresh
standalone evidence, updates the benchmark contract, records the new baseline in
planning docs, and closes ADD-01 traceability without rewriting frozen historical
Phase 6 assets.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-CONTEXT.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
@.planning/phases/09-full-corpus-added-characteristic-closure/09-PATTERNS.md
@run.py
@tests/test_phase7_benchmark.py
@tests/fixtures/phase7_algorithm_only/README.md
@.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md
@.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md
@.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the refreshed zero-miss algorithm-only fixture set and benchmark baseline</name>
  <files>
tests/fixtures/phase7_algorithm_only/part1-debug-report.json,
tests/fixtures/phase7_algorithm_only/part2-debug-report.json,
tests/fixtures/phase7_algorithm_only/part3-debug-report.json,
tests/fixtures/phase7_algorithm_only/part4-debug-report.json,
tests/fixtures/phase7_algorithm_only/part5-debug-report.json,
tests/fixtures/phase7_algorithm_only/part6-debug-report.json,
tests/fixtures/phase7_algorithm_only/part7-debug-report.json,
tests/fixtures/phase7_algorithm_only/part8-debug-report.json,
tests/fixtures/phase7_algorithm_only/part9-debug-report.json,
tests/test_phase7_benchmark.py,
.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md
  </files>
  <read_first>
run.py,
tests/test_phase7_benchmark.py,
tests/fixtures/phase7_algorithm_only/README.md,
.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
  </read_first>
  <action>
Run the standalone pipeline for each part `part1` through `part9` using the
current fixed code (`uv run python run.py partN`). For each resulting run:

1. Regenerate `tests/fixtures/phase7_algorithm_only/partN-debug-report.json`
   using the existing Phase 7 fixture shape:
   - `baseline_commit`
   - `packet_run_id`
   - `part_name`
   - `items`
   - `missing_added_truth_indexes`
   - `conforming_count`
   - `review_needed_count`
2. Confirm the regenerated fixture has an empty `missing_added_truth_indexes`
   list for every part.
3. Update `tests/test_phase7_benchmark.py::BASELINE_COUNTS` so every
   `max_missing_added` value is `0` and every `min_conforming` value matches the
   refreshed fixture counts.
4. Rewrite
   `.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md`
   with the refreshed per-part counts and an explicit statement that
   `assets/debug_report_part*.json` remain frozen historical Phase 6 evidence.

Do not modify `assets/debug_report_part*.json`. Do not update the benchmark
baseline until all nine refreshed fixtures exist and all have empty
`missing_added_truth_indexes`.
  </action>
  <verify>
    <automated>uv run python - <<'PY'
import json
from pathlib import Path
fixture_dir = Path("tests/fixtures/phase7_algorithm_only")
for idx in range(1, 10):
    payload = json.loads((fixture_dir / f"part{idx}-debug-report.json").read_text())
    assert payload["missing_added_truth_indexes"] == [], (idx, payload["missing_added_truth_indexes"])
print("ok")
PY
uv run python - <<'PY'
from tests.test_phase7_benchmark import BASELINE_COUNTS
assert sorted(BASELINE_COUNTS) == [f"part{i}" for i in range(1, 10)]
assert all(bounds["max_missing_added"] == 0 for bounds in BASELINE_COUNTS.values()), BASELINE_COUNTS
print("ok")
PY
uv run pytest -q tests/test_phase7_benchmark.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python - <<'PY'\nimport json\nfrom pathlib import Path\nfixture_dir = Path(\"tests/fixtures/phase7_algorithm_only\")\nfor idx in range(1, 10):\n    payload = json.loads((fixture_dir / f\"part{idx}-debug-report.json\").read_text())\n    assert payload[\"missing_added_truth_indexes\"] == [], (idx, payload[\"missing_added_truth_indexes\"])\nprint(\"ok\")\nPY` exits 0.
    - `uv run python - <<'PY'\nfrom tests.test_phase7_benchmark import BASELINE_COUNTS\nassert sorted(BASELINE_COUNTS) == [f\"part{i}\" for i in range(1, 10)]\nassert all(bounds[\"max_missing_added\"] == 0 for bounds in BASELINE_COUNTS.values()), BASELINE_COUNTS\nprint(\"ok\")\nPY` exits 0.
    - `uv run pytest -q tests/test_phase7_benchmark.py -x` exits 0.
    - `rg -n "historical Phase 6|Missing-Added \\(indexes\\)|part9" .planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md` returns matches.
    - `git diff --name-only -- assets/debug_report_part1.json assets/debug_report_part2.json assets/debug_report_part3.json assets/debug_report_part4.json assets/debug_report_part5.json assets/debug_report_part6.json assets/debug_report_part7.json assets/debug_report_part8.json assets/debug_report_part9.json` produces no output.
  </acceptance_criteria>
  <done>The refreshed algorithm-only fixture set and benchmark baseline now encode a zero-miss corpus without touching frozen historical Phase 6 assets.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Phase 06/07 verification evidence and close ADD-01 traceability</name>
  <files>
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md,
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
.planning/REQUIREMENTS.md
  </files>
  <read_first>
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md,
.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md,
.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md,
.planning/REQUIREMENTS.md,
.planning/phases/07-regression-tests-and-verification/07-04-ALGORITHM-ONLY-BASELINE.md,
.planning/phases/09-full-corpus-added-characteristic-closure/09-RESEARCH.md
  </read_first>
  <action>
After Task 1's refreshed fixtures and benchmark are green, update the
verification/traceability documents to reflect the final closure.

Required document updates:

1. In `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md`
   append a Phase 9 follow-up note stating that the final full-corpus closure is
   now proven by refreshed standalone/algorithm-only evidence and that the
   historical Phase 6 asset snapshots remain frozen.
2. In `.planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md`
   update the ADD-01 coverage section so it no longer says end-to-end closure is
   deferred. It must cite the refreshed Phase 9 corpus result and the updated
   Phase 7 algorithm-only baseline.
3. In `.planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md`
   add an explicit Phase 9 closure note stating that the authoritative
   algorithm-only fixture set now has zero missing-added rows across all 9 parts.
4. In `.planning/REQUIREMENTS.md`
   - change `ADD-01` from unchecked to checked
   - update the traceability row to:
     `| ADD-01 | Phase 9 | 09-01-PLAN.md, 09-02-PLAN.md, 09-03-PLAN.md |`

Do not update `.planning/REQUIREMENTS.md` until the refreshed benchmark and
verification docs exist. Leave ADD-02 and SNP-01 traceability alone unless a
doc link must be tightened to cite the refreshed evidence.
  </action>
  <verify>
    <automated>rg -n "Phase 9|historical Phase 6|zero missing-added|algorithm-only" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md
rg -n "^- \\[x\\] \\*\\*ADD-01\\*\\*|^\\| ADD-01 \\| Phase 9 \\| 09-01-PLAN.md, 09-02-PLAN.md, 09-03-PLAN.md \\|$" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase 9|historical Phase 6|zero missing-added|algorithm-only" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md` returns matches in all three files.
    - `rg -n "^- \\[x\\] \\*\\*ADD-01\\*\\*" .planning/REQUIREMENTS.md` returns one match.
    - `rg -n "^\\| ADD-01 \\| Phase 9 \\| 09-01-PLAN.md, 09-02-PLAN.md, 09-03-PLAN.md \\|$" .planning/REQUIREMENTS.md` returns one match.
    - `uv run pytest -q tests/test_phase7_benchmark.py tests/test_phase6_asset_regression.py -x` exits 0 after the document refresh.
  </acceptance_criteria>
  <done>The refreshed Phase 06/07 evidence and requirements traceability now reflect the final zero-miss closure instead of the older mixed baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh standalone rerun output -> committed benchmark fixtures | The refreshed fixture set must represent current algorithm behavior without mutating historical assets. |
| Refreshed evidence -> milestone traceability | ADD-01 should only be marked complete after the refreshed benchmark and verification artifacts exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Integrity | refreshed algorithm-only fixtures | mitigate | Require all 9 refreshed fixtures to have empty `missing_added_truth_indexes` before updating the benchmark baseline. |
| T-09-07 | Traceability | `.planning/REQUIREMENTS.md` | mitigate | Gate ADD-01 completion on the existence of refreshed Phase 06/07 verification artifacts and green benchmark tests. |
| T-09-08 | Integrity | historical Phase 6 assets | mitigate | Explicitly verify that `assets/debug_report_part*.json` remain untouched during the refresh. |
</threat_model>

<verification>
- [ ] `uv run python - <<'PY'\nimport json\nfrom pathlib import Path\nfixture_dir = Path(\"tests/fixtures/phase7_algorithm_only\")\nfor idx in range(1, 10):\n    payload = json.loads((fixture_dir / f\"part{idx}-debug-report.json\").read_text())\n    assert payload[\"missing_added_truth_indexes\"] == [], (idx, payload[\"missing_added_truth_indexes\"])\nprint(\"ok\")\nPY`
- [ ] `uv run python - <<'PY'\nfrom tests.test_phase7_benchmark import BASELINE_COUNTS\nassert sorted(BASELINE_COUNTS) == [f\"part{i}\" for i in range(1, 10)]\nassert all(bounds[\"max_missing_added\"] == 0 for bounds in BASELINE_COUNTS.values()), BASELINE_COUNTS\nprint(\"ok\")\nPY`
- [ ] `uv run pytest -q tests/test_phase7_benchmark.py tests/test_phase6_asset_regression.py -x`
- [ ] `rg -n "Phase 9|historical Phase 6|zero missing-added|algorithm-only" .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VALIDATION.md .planning/phases/06-added-characteristic-detection-and-snippet-accuracy/06-VERIFICATION.md .planning/phases/07-regression-tests-and-verification/07-VERIFICATION.md`
- [ ] `rg -n "^- \\[x\\] \\*\\*ADD-01\\*\\*|^\\| ADD-01 \\| Phase 9 \\| 09-01-PLAN.md, 09-02-PLAN.md, 09-03-PLAN.md \\|$" .planning/REQUIREMENTS.md`
</verification>

<success_criteria>

- All tasks completed
- Fresh standalone evidence proves zero missing-added rows across parts 1-9
- The Phase 7 benchmark baseline now requires zero missing-added rows
- Phase 06/07 verification artifacts and ADD-01 traceability reflect the final closure
</success_criteria>

<output>
After completion, create `.planning/phases/09-full-corpus-added-characteristic-closure/09-03-SUMMARY.md`
</output>
