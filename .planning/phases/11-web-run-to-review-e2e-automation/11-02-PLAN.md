---
phase: 11-web-run-to-review-e2e-automation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tests/test_web_run_review_e2e.py
autonomous: true
requirements: [TST-02, VER-01]
test_ids:
  - tests/test_web_run_review_e2e.py::test_live_run_can_be_cleared_signed_and_exported_after_debug_resolution
  - tests/test_web_run_review_e2e.py::test_phase11_companion_seeded_snapshot_surfaces_advisories
must_haves:
  truths:
    - "The dedicated Phase 11 module proves the same live run can move from blocked review state to signed/exportable state once review items and debug exceptions are resolved."
    - "Phase 11 keeps advisory coverage inside the dedicated module via a narrow seeded companion case because current live corpus fixtures do not emit stable non-empty confidence_flags."
    - "Phase 11 proves reachability of the existing Phase 10 sign-off/export contract; it does not redefine that contract or replace slice tests."
  artifacts:
    - path: "tests/test_web_run_review_e2e.py"
      provides: "Live clearance-to-export proof for the dedicated Phase 11 module"
      contains: "test_live_run_can_be_cleared_signed_and_exported_after_debug_resolution"
    - path: "tests/test_web_run_review_e2e.py"
      provides: "Companion advisory coverage kept inside the Phase 11 artifact"
      contains: "test_phase11_companion_seeded_snapshot_surfaces_advisories"
  key_links:
    - from: "POST /review/{run_id}/sign-off/confirm"
      to: "shop/services/review.py"
      via: "attempt_sign_off signed snapshot creation"
      pattern: "attempt_sign_off("
    - from: "Run.packet_versions[].debug_snapshot_path"
      to: "GET /exports/{run_id}/*"
      via: "signed export route eligibility"
      pattern: "debug_snapshot_path"
    - from: "seeded packet confidence_flags"
      to: "Phase 11 advisory companion assertion"
      via: "same dedicated integration module"
      pattern: "confidence_flags"
---

<objective>
Extend the dedicated Phase 11 artifact so it proves the live run can be
cleared, signed off, and exported, then add one companion seeded advisory case
inside that same module to close the approved advisory-coverage fallback.

Purpose: Phase 11 is incomplete if it only proves the live run can fail the
gate. The milestone needs proof that the maintained web path can also reach the
signed export endpoints after debug resolution, while advisory coverage remains
traceable inside the same artifact even though the live corpus does not provide
stable packet flags.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-CONTEXT.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-RESEARCH.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-VALIDATION.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-01-PLAN.md
@tests/test_web_run_review_e2e.py
@tests/test_review.py
@tests/test_exports.py
@tests/test_debug_verdicts.py
@shop/routers/review.py
@shop/services/review.py
@shop/routers/exports.py
@shop/services/exports.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear the live run through review/debug and prove signed export reachability</name>
  <files>
tests/test_web_run_review_e2e.py
  </files>
  <read_first>
tests/test_web_run_review_e2e.py,
tests/test_review.py,
tests/test_exports.py,
tests/test_debug_verdicts.py,
shop/services/review.py,
shop/routers/exports.py
  </read_first>
  <action>
Extend the dedicated module with
`test_live_run_can_be_cleared_signed_and_exported_after_debug_resolution`:

1. Reuse the live `assets/part6` run helper from Plan 01.
2. After proving the blocked sign-off path, clear the live run into an
   exportable state using the lightest mechanism that preserves the phase goal:
   - normal review rows may be approved with a helper or direct DB update after
     the route boundary has already been exercised,
   - unresolved debug exceptions should be cleared using the existing debug
     verdict contract (route submission preferred when practical; service-level
     helper acceptable if the route path is already covered elsewhere in the
     same module).
3. Assert `POST /review/{run_id}/sign-off/confirm` now redirects to the
   generating/success path.
4. Assert the persisted `Run` becomes `signed_off`, carries a
   `packet_versions` entry with `debug_snapshot_path`, and that signed export
   routes such as `/exports/{run_id}/audit-packet.csv` and
   `/exports/{run_id}/work-order.csv` return 200.

Keep the assertions focused on live-path reachability and signed snapshot
presence. Do not duplicate all export CSV column assertions already owned by
Phase 10 slice tests.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_web_run_review_e2e.py -k "can_be_cleared_signed_and_exported" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_web_run_review_e2e.py` contains `test_live_run_can_be_cleared_signed_and_exported_after_debug_resolution`.
    - The test asserts `run.status == "signed_off"` and verifies `debug_snapshot_path` exists in `packet_versions`.
    - The test asserts signed audit/work-order export routes return 200 for the cleared live run.
    - `uv run pytest -q tests/test_web_run_review_e2e.py -k "can_be_cleared_signed_and_exported" -x` exits 0.
  </acceptance_criteria>
  <done>The dedicated Phase 11 artifact now proves the real live run can traverse the full review -> sign-off -> signed export path after debug resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Add the companion seeded advisory case inside the Phase 11 artifact</name>
  <files>
tests/test_web_run_review_e2e.py
  </files>
  <read_first>
tests/test_web_run_review_e2e.py,
tests/test_review.py,
tests/test_exports.py,
shop/services/exports.py,
shop/services/review.py
  </read_first>
  <action>
Add `test_phase11_companion_seeded_snapshot_surfaces_advisories` inside the
same dedicated module.

This companion case should:

1. Seed the smallest possible signed-off run whose packet/snapshot data carries
   a non-empty `confidence_flags` value.
2. Exercise one existing advisory surface inside the Phase 11 artifact:
   - preferred: signed audit packet CSV or work-order CSV, because the live run
     already covers standard review/debug route entry,
   - acceptable: normal review queue HTML, if that keeps the helper smaller.
3. Assert the packet-native advisory text is surfaced from the seeded snapshot
   contract and not reconstructed from unrelated reasons text.

Keep the helper local to `tests/test_web_run_review_e2e.py` unless extracting it
to `tests/conftest.py` is clearly cleaner. Do not move this assertion back into
`tests/test_exports.py`; Phase 11 must keep the companion fallback inside its
own artifact.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_web_run_review_e2e.py -k "companion_seeded_snapshot_surfaces_advisories" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_web_run_review_e2e.py` contains `test_phase11_companion_seeded_snapshot_surfaces_advisories`.
    - The advisory assertion is driven by seeded `confidence_flags`, not inferred from `reasons`.
    - The companion case lives in the dedicated Phase 11 module, not only in legacy slice-test files.
    - `uv run pytest -q tests/test_web_run_review_e2e.py -k "companion_seeded_snapshot_surfaces_advisories" -x` exits 0.
  </acceptance_criteria>
  <done>The Phase 11 artifact now covers the advisory clause honestly, without pretending the current live corpus emits stable packet flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cleared live run -> signed packet version metadata | Signed exports are only valid if the live run records the Phase 10 snapshot contract at sign-off. |
| Seeded companion advisory case -> Phase 11 milestone claim | The companion case must remain clearly separate from the live corpus proof so the test suite does not overstate live advisory coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Integrity | live sign-off/export proof | mitigate | Assert the cleared live run produces `packet_versions[].debug_snapshot_path` before calling signed export routes. |
| T-11-04 | Traceability | advisory companion case | mitigate | Keep the seeded advisory assertion inside `tests/test_web_run_review_e2e.py` and label it as the approved fallback for non-live advisory coverage. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_web_run_review_e2e.py -k "can_be_cleared_signed_and_exported" -x`
- [ ] `uv run pytest -q tests/test_web_run_review_e2e.py -k "companion_seeded_snapshot_surfaces_advisories" -x`
- [ ] `uv run pytest -q tests/test_web_run_review_e2e.py tests/test_runs.py tests/test_review.py tests/test_exports.py -x`
</verification>

<success_criteria>

- The dedicated Phase 11 module proves the live run can be cleared and signed
- Signed audit/work-order export routes are reachable from the cleared live run
- The signed snapshot contract is asserted on the live run
- Advisory fallback coverage stays inside the Phase 11 module via one seeded companion case

</success_criteria>
