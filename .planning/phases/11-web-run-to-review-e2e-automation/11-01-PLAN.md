---
phase: 11-web-run-to-review-e2e-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_web_run_review_e2e.py
autonomous: true
requirements: [TST-02, VER-01]
test_ids:
  - tests/test_web_run_review_e2e.py::test_live_run_submission_persists_packet_and_loads_review_surfaces
  - tests/test_web_run_review_e2e.py::test_live_run_blocks_signoff_until_debug_queue_is_cleared
must_haves:
  truths:
    - "Phase 11 owns one dedicated integration artifact that submits a real corpus fixture through POST /runs/new rather than seeding packet data directly."
    - "The live proof writes a real delta_packet.json into a temp OUT_DIR and both the status page plus review/debug surfaces reload that packet-backed state."
    - "The live run proves the Phase 10 debug gate blocks sign-off before any helper clears review rows or debug verdicts."
  artifacts:
    - path: "tests/test_web_run_review_e2e.py"
      provides: "Dedicated Phase 11 live web-flow proof module"
      contains: "test_live_run_submission_persists_packet_and_loads_review_surfaces"
    - path: "tests/test_web_run_review_e2e.py"
      provides: "Blocked-signoff proof for the live run"
      contains: "test_live_run_blocks_signoff_until_debug_queue_is_cleared"
    - path: "tests/conftest.py"
      provides: "Shared Phase 11 isolation helper if needed"
      contains: "huey_immediate"
  key_links:
    - from: "POST /runs/new"
      to: "shop/tasks.py"
      via: "huey_immediate inline run_pipeline_task execution"
      pattern: "run_pipeline_task("
    - from: "shop.services.runs.UPLOADS_DIR"
      to: "shop/tasks.OUT_DIR"
      via: "temp-dir isolated live run"
      pattern: "UPLOADS_DIR"
    - from: "output_dir/delta_packet.json"
      to: "GET /review/{run_id}"
      via: "build_debug_queue_state packet reload"
      pattern: "delta_packet.json"
---

<objective>
Create the dedicated Phase 11 integration harness and prove the live maintainer
workflow from `/runs/new` through packet persistence, status-page rendering,
review/debug entry, and blocked sign-off using a real corpus upload.

Purpose: the roadmap gap is not a missing algorithm check; it is the absence of
one traceable live-web proof artifact. This plan establishes that artifact and
anchors it to the real route/task/persistence/review seams instead of more
slice-only coverage.
</objective>

<execution_context>
@/home/khoa2/.codex/get-shit-done/workflows/execute-plan.md
@/home/khoa2/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-CONTEXT.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-RESEARCH.md
@.planning/phases/11-web-run-to-review-e2e-automation/11-VALIDATION.md
@tests/conftest.py
@tests/test_runs.py
@tests/test_pipeline_task.py
@tests/test_review.py
@shop/routers/runs.py
@shop/services/runs.py
@shop/tasks.py
@shop/routers/review.py
@shop/services/review.py
@assets/part6/revA.pdf
@assets/part6/revB.pdf
@assets/part6/FAIR.xlsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isolated live-run harness utilities for the dedicated Phase 11 module</name>
  <files>
tests/conftest.py,
tests/test_web_run_review_e2e.py
  </files>
  <read_first>
tests/conftest.py,
tests/test_runs.py,
tests/test_pipeline_task.py,
shop/services/runs.py,
shop/tasks.py
  </read_first>
  <action>
Add the minimum shared harness needed for a real `/runs/new` integration proof:

1. Create `tests/test_web_run_review_e2e.py` as the dedicated Phase 11 module.
2. In the new module, add helper(s) that:
   - load `assets/part6/revA.pdf`, `assets/part6/revB.pdf`, and `assets/part6/FAIR.xlsx`
     as multipart upload payloads,
   - create a `sessionmaker(bind=db_engine)` patch target for
     `shop.tasks.SessionLocal`,
   - patch `shop.services.runs.UPLOADS_DIR` and `shop.tasks.OUT_DIR` to
     per-test temp directories,
   - authenticate engineer/admin users through the existing session helpers.
3. Only touch `tests/conftest.py` if a small shared fixture materially reduces
   duplication across multiple Phase 11 tests. Do not refactor unrelated test
   infrastructure.

Keep the harness route-first: the test must still submit through `/runs/new`
and let `huey_immediate` execute the task inline.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_web_run_review_e2e.py -k "submission_persists_packet" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_web_run_review_e2e.py` exists and reads real files from `assets/part6/`.
    - The live-run helper patches `shop.tasks.SessionLocal`, `shop.services.runs.UPLOADS_DIR`, and `shop.tasks.OUT_DIR`.
    - `uv run pytest -q tests/test_web_run_review_e2e.py -k "submission_persists_packet" -x` exits 0.
  </acceptance_criteria>
  <done>The Phase 11 module can drive a real corpus upload through the live route while keeping DB and filesystem side effects isolated.</done>
</task>

<task type="auto">
  <name>Task 2: Prove live submission, packet persistence, review/debug entry, and blocked sign-off on the real gate</name>
  <files>
tests/test_web_run_review_e2e.py
  </files>
  <read_first>
tests/test_web_run_review_e2e.py,
tests/test_runs.py,
tests/test_review.py,
shop/routers/runs.py,
shop/routers/review.py,
shop/services/review.py
  </read_first>
  <action>
Implement two live-flow assertions in `tests/test_web_run_review_e2e.py`:

1. `test_live_run_submission_persists_packet_and_loads_review_surfaces`
   - Submit `assets/part6/*` through `POST /runs/new`.
   - Assert the response redirects to `/runs/{id}`.
   - Assert the persisted `Run` leaves `queued`, writes an `output_dir`, and
     that `${output_dir}/delta_packet.json` exists under the patched temp root.
   - Assert `GET /runs/{id}` returns 200 and exposes the run/review status
     rather than a blank or pre-pipeline state.
   - Assert `GET /review/{id}` returns 200 for the engineer and
     `GET /review/{id}?debug=1` returns 200 for an admin on the same live run.
2. `test_live_run_blocks_signoff_until_debug_queue_is_cleared`
   - Use the same live-run setup and seed/open the review queue.
   - Clear normal pending review items using the least-invasive helper
     available after the route boundary is already proven.
   - Before submitting any debug verdicts, assert
     `POST /review/{id}/sign-off/confirm` redirects with
     `error=debug_exceptions_pending`.

Do not seed `delta_packet.json` by hand in either test. The blocked-signoff
assertion must use the actual live run produced by `/runs/new`.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_web_run_review_e2e.py -k "submission_persists_packet or blocks_signoff_until_debug_queue_is_cleared" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_web_run_review_e2e.py` contains both exact test names listed in frontmatter.
    - The test asserts a real `delta_packet.json` path under a temp output dir, not a seeded fixture file.
    - The blocked sign-off assertion checks for `debug_exceptions_pending`, not only `pending_items`.
    - `uv run pytest -q tests/test_web_run_review_e2e.py -k "submission_persists_packet or blocks_signoff_until_debug_queue_is_cleared" -x` exits 0.
  </acceptance_criteria>
  <done>The dedicated Phase 11 artifact now proves the live run reaches packet-backed review/debug surfaces and that unresolved debug blockers stop sign-off on the real path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test route DB -> Huey task DB | The live run is only meaningful if `/runs/new` and `run_pipeline_task` use the same test database. |
| Temp uploads/out roots -> checked-in repo dirs | The proof must not contaminate shared `uploads/` or `out/` state. |
| Real packet output -> review/debug assertions | The tests must consume the packet generated by the live run, not a seeded substitute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Integrity | live task execution | mitigate | Patch `shop.tasks.SessionLocal` to the same test session factory used by the app fixture before `/runs/new`. |
| T-11-02 | Traceability | live review/sign-off proof | mitigate | Assert the temp `output_dir` contains a real `delta_packet.json` and use the actual sign-off route response for blocker validation. |
</threat_model>

<verification>
- [ ] `uv run pytest -q tests/test_web_run_review_e2e.py -k "submission_persists_packet" -x`
- [ ] `uv run pytest -q tests/test_web_run_review_e2e.py -k "submission_persists_packet or blocks_signoff_until_debug_queue_is_cleared" -x`
</verification>

<success_criteria>

- One dedicated Phase 11 module exists for live web-flow proof
- The module submits a real corpus run through `/runs/new`
- The live run writes a real `delta_packet.json` under isolated temp storage
- The live run reaches both standard review and admin debug surfaces
- Sign-off is blocked on unresolved debug exceptions for the live run

</success_criteria>
