# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M003 | arch | UI aesthetic direction | Industrial dark-mode — dark bg, amber/green status, monospaced accents | User selected "industrial precision" — aerospace tooling software feel, not generic SaaS | Yes — if customer feedback demands light mode |
| D002 | M003 | arch | Navigation layout | Sidebar + top bar | User selected sidebar for section nav + top bar for context; current single top nav too flat for multi-section tool | Yes — if responsive/mobile layout needed |
| D003 | M003 | arch | Review queue interaction model | Full-screen focused mode — one item at a time, keyboard shortcuts A/O/J/K | User selected full-screen; keyboard nav for processing 50-100 items efficiently | Yes — if team prefers dense list |
| D004 | M003 | convention | HTMX swap IDs | Preserved unchanged from M001/M002 | Backend routers and HTMX attributes reference specific IDs; changing them requires backend changes which are out of scope | No — would require coordinated backend change |
| D005 | M003 | scope | Print/export templates | Not redesigned in M003 | audit_packet.html and work_order.html are WeasyPrint PDF templates — print aesthetics are separate from screen UI | Yes — future print redesign milestone |
| D006 | M003 | scope | Interaction patterns | Open to change where better UX exists | User confirmed interactions can change as long as backend can support; not locked to current HTMX patterns | Yes |
| D007 | M003/S01 | arch | Standalone page layout | `_base_standalone.html` — separate template for login/wizard, not extending `base.html` | Login and wizard must not render sidebar chrome; overriding `{% block nav %}` won't hide sidebar since it's outside that block; separate standalone base is the clean solution | No — login/wizard must stay decoupled from sidebar layout |
| D008 | M003/S01 | convention | DaisyUI v5 custom theme definition | Raw `[data-theme="name"]{...}` CSS block in `input.css`, placed after `@plugin "daisyui"` directive, not inside it | DaisyUI v5 @plugin handles semantic class generation; the raw CSS block handles custom color values via CSS custom properties. Attempting to nest theme vars inside @plugin config caused build errors. | No — this is the DaisyUI v5 API contract |
| D009 | M003/S01 | convention | Wizard step progress indicator | Custom flex divs instead of DaisyUI `ul.steps` component | DaisyUI steps uses CSS pseudo-element counter backgrounds that don't render cleanly against `oklch(13% 0.005 260)` (near-black base-100). Raw flex divs with explicit border/background classes give identical visual semantics with full control over theme vars. | Yes — if DaisyUI steps gains dark-mode support or a workaround is found |
| D010 | M003/S02 | convention | Section wrapper pattern | `border border-base-300 bg-base-200 p-4/p-5` div replaces `card bg-base-100 shadow` | DaisyUI card adds white bg, shadow, and rounded corners that produce light-mode artifacts in dark theme. Plain bordered div with explicit bg and padding gives the industrial bordered-section look without DaisyUI card semantics. Applied consistently across dashboard, runs list, new run form. | Yes — if DaisyUI card dark-mode rendering improves |
| D011 | M003/S02 | convention | Alert banner partial pattern | `border-l-4 border-error bg-base-200` left-accent div instead of `alert alert-error` box | `alert alert-error` produces a full-width colored box that is too visually heavy for inline run alert banners in the industrial aesthetic. Left-accent div maintains the error signal while fitting the tight grid. HTMX outerHTML swap ID preserved. | Yes — could revert to alert component if other slices standardize on it |
| D012 | M003/S03 | convention | Status page terminal banners use border-l-4 pattern | `border-l-4 border-{color} bg-base-200` for all status-state banners in status.html, including SSE JS-injected HTML | Unifies with D011 alert banner pattern from S02. Both Jinja2 static blocks AND `injectTerminalBanner()` JS strings must use the same classes — otherwise pre-reload injected banner and post-reload rendered banner look different. | Yes — if full alert boxes preferred later |
| D013 | M003/S04 | convention | Full-screen focused review: JS-managed item visibility | All items rendered into `#item-list`; JS toggles `display:block/none` via class on each card; `currentIndex` tracks position | Keeps HTMX OOB swap chain 100% intact — backend returns same HTML, HTMX updates `#review-item-{char_no}` as before. Display logic is purely client-side. Simpler than virtual scrolling or lazy-loading alternatives. | Yes — if item count exceeds ~200 and DOM weight becomes a concern |
| D014 | M003/S04 | convention | Override panel: plain `<div hidden>` replaces DaisyUI `collapse collapse-arrow` | `<div id="override-panel-{char_no}" hidden>` toggled by JS and `data-override-toggle` button; O key and mouse button share same toggle path | DaisyUI collapse adds animation and chevron that don't fit the focused-mode aesthetic. Plain hidden div is simpler, programmatically controllable by both keyboard handler and click handler, and supports the `hidden` attribute approach without fighting DaisyUI's open/close state. | Yes — if animated accordion preferred for mouse users |
| D015 | M003/S04/T02 | bug | HTMX `afterSwap` + `outerHTML`: `e.detail.target` is detached after swap | Check `e.detail.target.id.startsWith('review-item-')` instead of `itemList.contains(e.detail.target)` | For `hx-swap="outerHTML"`, HTMX detaches the original target element from DOM before firing `afterSwap`. `itemList.contains(e.detail.target)` always returns false, causing auto-advance to never fire. The fix uses the target's string `id` attribute (preserved on detached element) to identify whether the swap was for a review item card. | No — fundamental HTMX outerHTML lifecycle behavior |
